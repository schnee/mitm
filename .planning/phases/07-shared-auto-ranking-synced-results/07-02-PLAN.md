---
phase: 07-shared-auto-ranking-synced-results
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - services/api/src/modules/session/repository.ts
  - services/api/src/modules/ranking/service.ts
  - services/api/src/modules/ranking/repository.ts
  - services/api/src/routes/ranking/upsertRankingInputs.ts
  - services/api/src/routes/ranking/getRankedResults.ts
  - services/api/src/dev-server.ts
  - services/api/tests/ranking-results.test.ts
  - services/api/tests/ranking-inputs.test.ts
autonomous: true
requirements:
  - UX-12
  - UX-13
  - UX-15
  - UX-16
must_haves:
  truths:
    - "When both participants have saved ranking inputs, one canonical ranked list is generated for the whole session"
    - "Both participants converge to identical ordered ranked results from persisted session data"
    - "Concurrent saves do not produce duplicate/conflicting generation jobs"
    - "Generation failures preserve saved location/ranking inputs and expose retry-safe state"
  artifacts:
    - path: "services/api/src/modules/ranking/service.ts"
      provides: "Session-level generation orchestration with auto/refresh mode and idempotent coordination"
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Persisted rankedResults + rankingLifecycle metadata at session scope"
    - path: "services/api/src/routes/ranking/getRankedResults.ts"
      provides: "Optional refresh/recompute route returning persisted canonical list"
    - path: "services/api/tests/ranking-results.test.ts"
      provides: "Coverage for idempotent generation, persistence, refresh, and failure preservation"
  key_links:
    - from: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      to: "services/api/src/modules/ranking/service.ts"
      via: "second save triggers auto generation path"
      pattern: "mode: \"auto\"|generateSessionRankedResults"
    - from: "services/api/src/modules/ranking/service.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "persist canonical results and lifecycle"
      pattern: "upsertSessionRankedResults|setRankingLifecycle"
    - from: "services/api/src/routes/ranking/getRankedResults.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "refresh returns persisted session-scoped order"
      pattern: "rankedResults|generatedAt"
---

<objective>
Implement canonical session-level ranking generation, persistence, and retry-safe refresh with concurrent-save idempotency.

Purpose: Deliver UX-12/UX-13/UX-15/UX-16 without changing fairness algorithm fundamentals or provider integrations.
Output: Single orchestration path (`auto` + `refresh`) with persisted shared results and conflict-safe generation controls.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-RESEARCH.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-01-PLAN.md
@services/api/src/modules/session/repository.ts
@services/api/src/modules/ranking/repository.ts
@services/api/src/modules/ranking/service.ts
@services/api/src/routes/ranking/upsertRankingInputs.ts
@services/api/src/routes/ranking/getRankedResults.ts
@services/api/src/dev-server.ts

<interfaces>
From `services/api/src/modules/ranking/service.ts`:
```ts
export class RankingService {
  async generateRankedResults(sessionId: string): Promise<{
    sessionId: string;
    generatedAt: string;
    results: RankedVenue[];
  }>;
}
```

From `services/api/src/modules/session/repository.ts`:
```ts
getSessionSnapshot(sessionId: string): SessionSnapshot | undefined;
listSessionEvents(sessionId: string, since?: string): SessionEvent[];
recordFunnelEvent(sessionId: string, event: FunnelEventName, metadata?: Record<string, string | number | boolean | null>): void;
```

From `services/api/src/routes/ranking/getRankedResults.ts`:
```ts
getRankedResultsHandler(payload, { service }): Promise<{ status: number; body: unknown }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add session-level ranking lifecycle persistence and idempotent generation coordination</name>
  <files>services/api/src/modules/session/repository.ts, services/api/src/modules/ranking/service.ts, services/api/src/modules/ranking/repository.ts</files>
  <behavior>
    - Only one active generation per session can run at a time under concurrent saves.
    - Successful generation stores canonical `rankedResults` and lifecycle metadata on session.
    - Failed generation stores retryable failed lifecycle state without deleting participant inputs.
  </behavior>
  <action>Implement repository/service primitives from 07 research (`setRankingLifecycle`, `upsertSessionRankedResults`, session-scoped request id/lock semantics) and refactor ranking generation to a single entry point handling `mode: auto|refresh` (UX-12/UX-16). Persist generated results + timestamps so snapshots/events are recovery source (UX-13). On any provider/precondition failure, keep participant location/ranking inputs untouched and set actionable failed lifecycle state (UX-15). Preserve existing scoring, split handling, preference merge, and provider adapter calls exactly as-is.</action>
  <verify>
    <automated>npx vitest run services/api/tests/ranking-results.test.ts</automated>
  </verify>
  <done>Session repository + ranking service enforce idempotent single-flight generation and durable canonical results lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire auto-trigger on second save and refresh endpoint onto the same canonical orchestration path</name>
  <files>services/api/src/routes/ranking/upsertRankingInputs.ts, services/api/src/routes/ranking/getRankedResults.ts, services/api/src/dev-server.ts</files>
  <behavior>
    - Second participant save triggers one automatic generation attempt.
    - Refresh endpoint recomputes through same service path and returns persisted session-level results.
    - Handler responses expose deterministic lifecycle/result payloads for client sync.
  </behavior>
  <action>Update input-save route to call ranking service automatically when readiness flips true, with idempotent early-return when generation already running/ready (UX-12/UX-16). Reposition `POST /v1/ranking/results` as optional refresh/retry endpoint using `mode: refresh` but still returning canonical persisted results (UX-13/UX-14 follow-on). Keep existing HTTP error contracts where possible while adding explicit retryable failure payload details for UX-15.</action>
  <verify>
    <automated>npx vitest run services/api/tests/ranking-inputs.test.ts services/api/tests/ranking-results.test.ts</automated>
  </verify>
  <done>Both auto-trigger and refresh route resolve through one canonical generation path with shared persistence semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand backend tests for concurrent-save idempotency, persisted sync, and retry-safe failure handling</name>
  <files>services/api/tests/ranking-inputs.test.ts, services/api/tests/ranking-results.test.ts</files>
  <behavior>
    - Tests cover one-save waiting, second-save auto generation once, and near-simultaneous save behavior.
    - Tests assert both snapshot and route responses return same persisted order/items.
    - Tests assert failure preserves saved participant ranking/location inputs and supports subsequent refresh retry.
  </behavior>
  <action>Add/extend tests to prove UX-12/UX-13/UX-15/UX-16 outcomes: single canonical list generation, no duplicate generation under concurrent saves, and retry path that does not require re-entering inputs. Include assertions around lifecycle state transitions (`waiting` -> `generating` -> `ready|failed`) and persisted result replacement on refresh success.</action>
  <verify>
    <automated>npx vitest run services/api/tests/ranking-inputs.test.ts services/api/tests/ranking-results.test.ts</automated>
  </verify>
  <done>Automated tests fail on regression for canonical persistence, idempotency, and retry-safe behavior.</done>
</task>

</tasks>

<verification>
- `npx vitest run services/api/tests/ranking-inputs.test.ts services/api/tests/ranking-results.test.ts`
</verification>

<success_criteria>
- UX-12/UX-13: one canonical persisted ranked list exists per session and syncs identically to both users.
- UX-16: concurrent ranking-input saves do not create duplicate/conflicting generation results.
- UX-15: generation failures are recoverable with retry and no loss of previously entered location/ranking progress.
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared-auto-ranking-synced-results/07-02-SUMMARY.md`
</output>
