---
phase: 07-shared-auto-ranking-synced-results
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/modules/ranking/service.ts
  - services/api/src/routes/analytics/getSessionFunnel.ts
  - services/api/tests/funnel-events.test.ts
  - src/app/s/[token]/page.tsx
  - src/components/ranking/RankedResultsList.tsx
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements:
  - UX-17
  - UX-18
  - UX-15
must_haves:
  truths:
    - "Telemetry distinguishes ranking save/wait/generate/succeed/fail/render lifecycle events"
    - "Both desktop and mobile render the same shared ranked list with explainability and reaction affordances preserved"
    - "Failure state copy is actionable and retry-focused without implying data loss"
  artifacts:
    - path: "services/api/src/modules/session/contracts.ts"
      provides: "Extended funnel event union for ranking lifecycle telemetry"
    - path: "services/api/tests/funnel-events.test.ts"
      provides: "Automated telemetry assertions for ranking lifecycle events"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Responsive shared-list rendering that preserves fairness and reaction UX"
    - path: "tests/e2e/ranking-results.spec.ts"
      provides: "Cross-breakpoint e2e validation of shared auto-ranking flow and retry messaging"
  key_links:
    - from: "services/api/src/modules/ranking/service.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "recordFunnelEvent for ranking lifecycle"
      pattern: "ranking_generation_started|ranking_generation_succeeded|ranking_generation_failed"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "snapshot-driven ranked results and retry status messaging"
      pattern: "rankedResults|rankingLifecycle|Refresh ranking"
    - from: "tests/e2e/ranking-results.spec.ts"
      to: "src/app/s/[token]/page.tsx"
      via: "wait->generate->render/failure assertions across viewport projects"
      pattern: "waiting for partner|generating suggestions|retry"
---

<objective>
Harden lifecycle telemetry and responsive shared-results UX so auto-ranking is observable, trustworthy, and consistent across breakpoints.

Purpose: Close UX-17 and UX-18 while finalizing retry messaging behavior from UX-15.
Output: Ranking lifecycle telemetry events, responsive shared-results rendering guarantees, and e2e coverage for full auto-ranking lifecycle.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness-stabilization/04-01-SUMMARY.md
@.planning/phases/06-ux-polish-professional-experience/06-02-SUMMARY.md
@.planning/phases/06-ux-polish-professional-experience/06-03-SUMMARY.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-RESEARCH.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-02-PLAN.md
@services/api/src/modules/session/contracts.ts
@services/api/src/modules/session/repository.ts
@services/api/src/modules/ranking/service.ts
@services/api/src/routes/analytics/getSessionFunnel.ts
@src/app/s/[token]/page.tsx
@src/components/ranking/RankedResultsList.tsx
@tests/e2e/ranking-results.spec.ts

<interfaces>
From `services/api/src/modules/session/contracts.ts`:
```ts
export type FunnelEventName =
  | "session_start"
  | "inputs_set"
  | "results_returned"
  | "decision_confirmed"
  | "result_reacted"
  | "shortlist_opened"
  | "reaction_to_shortlist";
```

From `src/components/ranking/RankedResultsList.tsx`:
```ts
results: RankedVenue[];
onReact?: (venue: RankedVenue, reaction: "accept" | "pass") => void;
onAddToShortlist?: (venue: RankedVenue) => void;
```

From `tests/e2e/ranking-results.spec.ts`:
```ts
test("shows polished ranking readability and actions", async ({ page }) => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit and validate ranking lifecycle telemetry events</name>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, services/api/src/modules/ranking/service.ts, services/api/src/routes/analytics/getSessionFunnel.ts, services/api/tests/funnel-events.test.ts</files>
  <behavior>
    - Telemetry captures `ranking_inputs_saved`, `ranking_waiting_for_partner`, `ranking_generation_started`, `ranking_generation_succeeded`, `ranking_generation_failed`, and `ranking_results_rendered`.
    - Event metadata distinguishes auto generation vs refresh retry.
    - Existing required funnel events remain available and not regressed.
  </behavior>
  <action>Extend funnel event contracts and recording points to cover UX-17 lifecycle events while preserving Phase 4 required telemetry coverage. Emit events at input save, waiting transition, generation start/success/failure, and first render acknowledgment path. Update analytics summary handler/tests so lifecycle events are queryable without removing existing completeness checks.</action>
  <verify>
    <automated>npx vitest run services/api/tests/funnel-events.test.ts</automated>
  </verify>
  <done>Backend telemetry provides full ranking lifecycle visibility and remains backward-compatible with existing funnel analytics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize responsive shared ranked-list rendering and retry messaging</name>
  <files>src/app/s/[token]/page.tsx, src/components/ranking/RankedResultsList.tsx</files>
  <behavior>
    - Ranking stage renders from synced snapshot `rankedResults` state across desktop and mobile.
    - Waiting/generating/failed/ready messaging is concise, actionable, and confirms no data loss on failure.
    - Explainability chips and reaction/shortlist controls remain visible and usable after auto-generation.
  </behavior>
  <action>Polish ranking stage for UX-18 by ensuring responsive card/list behavior and preserving Phase 5/6 explainability + reaction affordances. Update failure copy to explicitly state previously entered locations/ranking inputs are kept and retry is available (UX-15). Keep manual action as secondary `Refresh ranking` only; no primary required run button reintroduced.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Shared ranked results and retry state are consistent, understandable, and responsive across targeted breakpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend e2e coverage for save→wait→generate→render/failure lifecycle across breakpoints</name>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <behavior>
    - E2E verifies waiting-for-partner and generating states appear without required manual run action.
    - E2E verifies shared results render with fairness + reaction + shortlist controls intact.
    - E2E verifies failure messaging and refresh retry path are actionable and preserve progress cues.
  </behavior>
  <action>Expand ranking e2e mocks/assertions to model lifecycle transitions and canonical shared results behavior (UX-17/UX-18). Add explicit assertions that `Run ranking` is not the required primary action, `Refresh ranking` is secondary retry/recompute, and failure copy confirms no re-entry needed for saved inputs. Run on desktop and mobile project targets used in prior phases.</action>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Desktop Safari" --project="Pixel 5"</automated>
  </verify>
  <done>Automated e2e checks cover the full shared auto-ranking lifecycle and responsive behavior regression surface.</done>
</task>

</tasks>

<verification>
- `npx vitest run services/api/tests/funnel-events.test.ts`
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/ranking-results.spec.ts --project="Desktop Safari" --project="Pixel 5"`
</verification>

<success_criteria>
- UX-17: lifecycle telemetry events are emitted and verifiable from backend tests.
- UX-18: shared ranked list remains consistent and action-forward across desktop/mobile breakpoints.
- UX-15 (finalized): error/retry messaging is explicit, actionable, and confirms input/location progress is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared-auto-ranking-synced-results/07-03-SUMMARY.md`
</output>
