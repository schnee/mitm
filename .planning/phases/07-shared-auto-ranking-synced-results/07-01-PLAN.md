---
phase: 07-shared-auto-ranking-synced-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/modules/ranking/repository.ts
  - services/api/src/routes/ranking/upsertRankingInputs.ts
  - src/lib/api/session-client.ts
  - src/hooks/useSessionSync.ts
  - src/components/ranking/RankingInputsForm.tsx
  - src/app/s/[token]/page.tsx
  - services/api/tests/ranking-inputs.test.ts
autonomous: true
requirements:
  - UX-11
  - UX-14
must_haves:
  truths:
    - "After saving ranking inputs, users immediately see a shared next-state message instead of a dead-end idle state"
    - "When only one participant has saved inputs, session state is clearly waiting-for-partner for both participants"
    - "Main flow no longer requires a primary `Run ranking` action to proceed"
  artifacts:
    - path: "services/api/src/modules/session/contracts.ts"
      provides: "Session snapshot/event contracts for rankingInputsReady, rankingLifecycle, and rankedResults"
    - path: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      provides: "Save-input response with lifecycle hint for waiting/generating transitions"
    - path: "src/app/s/[token]/page.tsx"
      provides: "UI stage copy and controls reflecting waiting/generating state without required Run CTA"
    - path: "services/api/tests/ranking-inputs.test.ts"
      provides: "Automated guardrails for save-state orchestration behavior"
  key_links:
    - from: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      to: "services/api/src/modules/ranking/repository.ts"
      via: "save response includes readiness/lifecycle hint"
      pattern: "rankingInputsReady|rankingLifecycle"
    - from: "src/hooks/useSessionSync.ts"
      to: "src/lib/api/session-client.ts"
      via: "event diff merge for ranking lifecycle and results"
      pattern: "rankingLifecycle|rankedResults|rankingInputsReady"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/ranking/RankingInputsForm.tsx"
      via: "onSaved flow updates waiting/generating status"
      pattern: "onSaved|rankingStatus"
---

<objective>
Introduce shared ranking lifecycle contracts and save-state orchestration so ranking progression begins automatically after input save.

Purpose: Deliver UX-11 and UX-14 by replacing manual trigger dependence with server-derived waiting/generating state.
Output: Updated API/session contracts, save-input lifecycle hints, and UI that no longer requires primary `Run ranking`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fair-ranking-recommendation-core/02-02-SUMMARY.md
@.planning/phases/02-fair-ranking-recommendation-core/02-03-SUMMARY.md
@.planning/phases/06-ux-polish-professional-experience/06-03-SUMMARY.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-RESEARCH.md
@services/api/src/modules/session/contracts.ts
@services/api/src/modules/session/repository.ts
@services/api/src/modules/ranking/repository.ts
@services/api/src/routes/ranking/upsertRankingInputs.ts
@src/lib/api/session-client.ts
@src/hooks/useSessionSync.ts
@src/components/ranking/RankingInputsForm.tsx
@src/app/s/[token]/page.tsx

<interfaces>
From `services/api/src/modules/session/contracts.ts`:
```ts
export interface SessionSnapshot {
  inputsReady: boolean;
  shortlist: ShortlistVenue[];
  reactions: VenueReactionSummary[];
  confirmedPlace: ConfirmedPlace | null;
}

export interface SessionEvent {
  eventType: "participant_joined" | "participant_location_confirmed" | "session_updated";
  diff?: Record<string, unknown>;
}
```

From `services/api/src/modules/ranking/repository.ts`:
```ts
upsertRankingInputs(input: {
  sessionId: string;
  participantId: string;
  split: RankingInputState["split"];
  tags: RankingInputState["tags"];
}): RankingInputState
```

From `src/lib/api/session-client.ts`:
```ts
export async function upsertRankingInputs(input: {
  sessionId: string;
  participantId: string;
  split: WillingnessSplit;
  tags: PreferenceTag[];
}): Promise<{ split: WillingnessSplit; tags: PreferenceTag[]; updatedAt: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared ranking lifecycle contracts across API snapshot, events, and client types</name>
  <files>services/api/src/modules/session/contracts.ts, src/lib/api/session-client.ts, src/hooks/useSessionSync.ts</files>
  <behavior>
    - Snapshot payload contains ranking lifecycle state and canonical ranked results fields.
    - Session event diffs can carry ranking lifecycle/result updates without `any` assumptions.
    - Client sync reducer merges lifecycle/result fields consistently from snapshot + events.
  </behavior>
  <action>Add `rankingInputsReady`, `rankingLifecycle`, and `rankedResults` to session contracts and client response types per UX-11 and 07 research contract baseline. Update `useSessionSync` merge logic to consume these fields from `session_updated` diffs, preserving existing shortlist/reaction/confirmation merge behavior. Keep `inputsReady` for location readiness untouched; do not change fairness/provider contracts.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Session sync contracts/types expose ranking lifecycle and shared results state end-to-end with type-safe merge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return ranking save orchestration hints from repository and input-save route</name>
  <files>services/api/src/modules/session/repository.ts, services/api/src/modules/ranking/repository.ts, services/api/src/routes/ranking/upsertRankingInputs.ts</files>
  <behavior>
    - Saving first participant inputs returns waiting-for-partner lifecycle hint.
    - Saving second participant inputs returns generating lifecycle hint and readiness=true.
    - Route response is deterministic and backward-compatible with existing validation/errors.
  </behavior>
  <action>Extend ranking input persistence to compute session-level readiness and emit/save lifecycle hint fields in the handler response (per UX-11). Update event diffs from repository operations so both participants can observe waiting/generating transitions. Preserve existing zod validation and `PARTICIPANT_NOT_FOUND` error mapping. Do not trigger provider calls here yet; this plan only establishes orchestration state signaling.</action>
  <verify>
    <automated>npx vitest run services/api/tests/ranking-inputs.test.ts</automated>
  </verify>
  <done>`POST /v1/ranking/inputs` returns explicit lifecycle hints for waiting/generating and reflects both-participant readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shift ranking UI to auto-orchestration messaging with optional manual refresh posture</name>
  <files>src/components/ranking/RankingInputsForm.tsx, src/app/s/[token]/page.tsx</files>
  <behavior>
    - After save, user sees waiting/generating/failed/ready status from shared lifecycle state.
    - Primary `Run ranking` CTA is removed from the main flow (UX-14).
    - Existing ranking result list/reaction/shortlist flow remains usable when results become available.
  </behavior>
  <action>Update ranking stage copy and controls so save transitions into lifecycle-led states (per UX-11), removing required primary `Run ranking` button (per UX-14). Keep `getRankedResults` wired as optional `Refresh ranking` intent entry point (secondary button only when lifecycle indicates ready/failed). Preserve accessibility/status badge conventions from Phase 6 and keep decision actions unchanged.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Ranking stage no longer blocks on a primary manual trigger and communicates shared waiting/generating progress clearly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx vitest run services/api/tests/ranking-inputs.test.ts`
</verification>

<success_criteria>
- UX-11: save-input actions produce immediate waiting/generating feedback for both participants.
- UX-14 (partial): required `Run ranking` primary action is removed from main flow.
- Session/client contracts now carry ranking lifecycle + shared result fields for downstream implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared-auto-ranking-synced-results/07-01-SUMMARY.md`
</output>
