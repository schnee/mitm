---
phase: 01-discovery-session-input-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260408_phase1_sessions.sql
  - src/lib/contracts/session.ts
  - src/lib/validation/session.ts
  - src/app/api/sessions/route.ts
  - src/app/api/sessions/join/route.ts
  - src/app/s/[token]/page.tsx
  - tests/api/sessions/create-join.test.ts
autonomous: true
requirements: [SESS-01, SESS-02]
must_haves:
  truths:
    - "Host can create a two-person session and receive a usable join link."
    - "Invitee can open join link and join without creating an account."
    - "Invalid, expired, or full sessions are rejected with clear errors."
  artifacts:
    - path: "src/app/api/sessions/route.ts"
      provides: "Create-session endpoint returning join URL"
      exports: ["POST"]
    - path: "src/app/api/sessions/join/route.ts"
      provides: "Join-by-token endpoint for invitee"
      exports: ["POST"]
    - path: "supabase/migrations/20260408_phase1_sessions.sql"
      provides: "Sessions and participants persistence contracts"
      contains: "create table sessions"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "/api/sessions/join"
      via: "POST token join request"
      pattern: "fetch\('/api/sessions/join'"
    - from: "src/app/api/sessions/join/route.ts"
      to: "sessions join_token + participants"
      via: "transactional join checks"
      pattern: "select.*join_token|insert into participants"
---

<objective>
Deliver server-authoritative create/join foundations so two participants can reliably enter the same session using a shareable link.

Purpose: Satisfy the no-account join backbone before any synced collaboration work.
Output: Database contracts, create/join APIs, and join-link entry page wired to those APIs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define session/join persistence and API contracts</name>
  <files>supabase/migrations/20260408_phase1_sessions.sql, src/lib/contracts/session.ts, src/lib/validation/session.ts, tests/api/sessions/create-join.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md, supabase/migrations/ (latest migration if present), src/lib/validation/ (existing schemas if present)</read_first>
  <behavior>
    - Test 1: Creating a session returns session_id + join_url and creates host participant.
    - Test 2: Joining with valid token creates invitee participant with no account requirement.
    - Test 3: Joining expired/full/unknown token returns 4xx and does not create participant rows.
  </behavior>
  <action>Create SQL migration with concrete tables/columns: `sessions(id uuid pk, join_token text unique, status text default 'created', expires_at timestamptz, created_at timestamptz default now())` and `participants(id uuid pk, session_id uuid fk sessions(id), role text check role in ('host','invitee'), joined_at timestamptz default now())`. Add explicit check or transaction logic that blocks more than two participants. Create `src/lib/contracts/session.ts` with exported `SessionStatus`, `CreateSessionResponse`, `JoinSessionRequest`, and `JoinSessionResponse` types. Create `src/lib/validation/session.ts` zod schemas for create/join payloads and token shape (8+ chars). Write failing tests first in `tests/api/sessions/create-join.test.ts`, then make schemas/contracts pass import/type checks.</action>
  <acceptance_criteria>
    - Migration file contains `create table sessions` and `create table participants`.
    - `src/lib/contracts/session.ts` exports `CreateSessionResponse` and `JoinSessionResponse`.
    - `src/lib/validation/session.ts` contains `JoinSessionSchema` and token min length rule.
    - `tests/api/sessions/create-join.test.ts` includes explicit cases for valid join and full/expired rejection.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/api/sessions/create-join.test.ts -x</automated>
  </verify>
  <done>Contracts and migration exist; tests describe and enforce create/join edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement create/join endpoints and token join page</name>
  <files>src/app/api/sessions/route.ts, src/app/api/sessions/join/route.ts, src/app/s/[token]/page.tsx, tests/api/sessions/create-join.test.ts</files>
  <read_first>src/lib/contracts/session.ts, src/lib/validation/session.ts, src/app/api/ (existing route patterns), src/app/s/[token]/page.tsx (if present)</read_first>
  <behavior>
    - Test 1: POST /api/sessions returns 201 with `{sessionId, joinUrl, participantId}` for host.
    - Test 2: POST /api/sessions/join with valid token returns 200 and invitee participant payload.
    - Test 3: Join page with token issues join request and renders success/error state deterministically.
  </behavior>
  <action>Implement `POST /api/sessions` to generate high-entropy token, create session row, insert host participant row, and return absolute join URL `/s/{token}`. Implement `POST /api/sessions/join` to validate token, check `expires_at`, enforce max two participants, insert invitee row, and return canonical session state payload. Update `src/app/s/[token]/page.tsx` to call join endpoint on load with token from route params, persist returned participant/session IDs in client state storage for this session, and render explicit states: joining, joined, invalid/expired/full. Keep flow no-account per SESS-02.</action>
  <acceptance_criteria>
    - `src/app/api/sessions/route.ts` exports `POST` and returns `joinUrl` in JSON.
    - `src/app/api/sessions/join/route.ts` returns 409 for full session and 410 for expired session.
    - `src/app/s/[token]/page.tsx` contains UI branches for `joining`, `joined`, and `join_error`.
    - API tests pass with status assertions for success and rejection paths.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/api/sessions/create-join.test.ts -x</automated>
  </verify>
  <done>Create/join flow is functional and rejects invalid token states predictably.</done>
</task>

</tasks>

<verification>
Run API contract tests and ensure both create and join endpoints return expected status codes and payload shapes.
</verification>

<success_criteria>
- Host can create session and copy join link.
- Invitee joins via link with no account step.
- Invalid/full/expired joins are blocked with actionable responses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-01-SUMMARY.md`
</output>
