---
phase: 01-discovery-session-input-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/validation.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/routes/sessions/createSession.ts
  - services/api/src/routes/sessions/joinSession.ts
  - services/api/tests/session-create-join.test.ts
  - src/lib/api/session-client.ts
  - src/app/s/[token]/page.tsx
  - .env.example
autonomous: true
requirements: [SESS-01, SESS-02]
user_setup:
  - service: google-cloud
    why: "Cloud Run + Firestore backend requires a provisioned GCP project"
    env_vars:
      - name: GCP_PROJECT_ID
        source: "Google Cloud Console -> Project Settings"
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: "Local machine path to service-account JSON with Firestore access"
  - service: cloudflare
    why: "Frontend deployment target is Cloudflare per project decision"
    env_vars:
      - name: NEXT_PUBLIC_API_BASE_URL
        source: "Cloudflare Pages/Workers environment settings"
must_haves:
  truths:
    - "Host can create a session and immediately receive a shareable join URL."
    - "Invitee can join from link without account creation."
    - "Expired/full/invalid join attempts fail with deterministic 4xx responses."
  artifacts:
    - path: "services/api/src/routes/sessions/createSession.ts"
      provides: "Cloud Run create-session endpoint"
      exports: ["createSessionHandler"]
    - path: "services/api/src/routes/sessions/joinSession.ts"
      provides: "Cloud Run join-by-token endpoint"
      exports: ["joinSessionHandler"]
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Firestore-backed create/join persistence"
      contains: "createSession"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Invitee link-entry UX for no-account join"
      min_lines: 40
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/lib/api/session-client.ts"
      via: "join intent request"
      pattern: "joinSessionByToken"
    - from: "services/api/src/routes/sessions/joinSession.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "server-authoritative join checks"
      pattern: "repository\.joinSession"
---

<objective>
Deliver server-created session + no-account join contracts on the Cloud Run + Firestore baseline.

Purpose: Satisfy SESS-01 and SESS-02 while honoring fixed deployment decisions (Cloudflare frontend, Google Cloud backend).
Output: Firestore-backed create/join handlers, typed client contract, and invitee join page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Firestore session contracts and failing create/join tests</name>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/validation.ts, services/api/src/modules/session/repository.ts, services/api/tests/session-create-join.test.ts</files>
  <behavior>
    - Test 1: createSession returns sessionId + joinToken + host participant and persists `status='created'`.
    - Test 2: joinSession(validToken) creates invitee participant without account identity.
    - Test 3: joinSession rejects expired/full/unknown token with stable domain errors mapped to 4xx.
  </behavior>
  <action>Create `contracts.ts` for `SessionStatus`, `SessionRecord`, `ParticipantRecord`, and typed API payloads; create zod request schemas in `validation.ts` (token min 10 chars, explicit role enum host|invitee). Implement repository methods in `repository.ts` against Firestore collections `sessions` and `participants` with server-enforced two-participant cap and `expiresAt`. Write tests first in `services/api/tests/session-create-join.test.ts` for success + expired/full/unknown failures, then implement minimal repository logic to pass.</action>
  <verify>
    <automated>pnpm test services/api/tests/session-create-join.test.ts -x</automated>
  </verify>
  <done>Session contracts/repository exist with passing tests proving deterministic create/join behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Cloud Run handlers and link-based join UX</name>
  <files>services/api/src/routes/sessions/createSession.ts, services/api/src/routes/sessions/joinSession.ts, src/lib/api/session-client.ts, src/app/s/[token]/page.tsx, .env.example, services/api/tests/session-create-join.test.ts</files>
  <behavior>
    - Test 1: `POST /v1/sessions` returns 201 with `sessionId`, `joinUrl`, `participantId`.
    - Test 2: `POST /v1/sessions/join` returns 200 for valid token, 409 for full, 410 for expired.
    - Test 3: `/s/[token]` page calls join API and renders deterministic `joining|joined|join_error` states.
  </behavior>
  <action>Implement Cloud Run route handlers using repository methods from Task 1. `createSessionHandler` generates high-entropy token and returns absolute join URL using `NEXT_PUBLIC_APP_URL`; `joinSessionHandler` maps domain errors to 404/409/410. Add `src/lib/api/session-client.ts` with typed fetch wrappers that target `NEXT_PUBLIC_API_BASE_URL`. Update `src/app/s/[token]/page.tsx` to auto-submit join by route token and persist session/participant IDs in sessionStorage (no account identity). Update `.env.example` with frontend/backend contract vars per cross-cloud decision in STATE.md.</action>
  <verify>
    <automated>pnpm test services/api/tests/session-create-join.test.ts -x</automated>
  </verify>
  <done>Create/join APIs and join page are wired end-to-end and enforce no-account invitee behavior.</done>
</task>

</tasks>

<verification>
Run session create/join tests; verify handlers return deterministic status codes and the join page uses typed client calls.
</verification>

<success_criteria>
- SESS-01: Host receives working join URL for a server-created session.
- SESS-02: Invitee joins from URL without account creation.
- Error paths are stable (404 unknown, 409 full, 410 expired).
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-01-SUMMARY.md`
</output>
