---
phase: 01-discovery-session-input-backbone
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - services/api/src/routes/sessions/getSessionSnapshot.ts
  - services/api/src/routes/sessions/sessionEvents.ts
  - services/api/src/modules/session/state-machine.ts
  - services/api/tests/session-sync.test.ts
  - src/hooks/useSessionSync.ts
  - src/components/session/ParticipantStatus.tsx
  - src/app/s/[token]/page.tsx
  - src/lib/api/session-client.ts
autonomous: true
requirements: [SESS-03]
must_haves:
  truths:
    - "Both participants observe the same canonical session state after join and updates."
    - "Session view hydrates from server snapshot before live updates start."
    - "Reconnect recovers state rather than leaving stale UI."
  artifacts:
    - path: "services/api/src/routes/sessions/getSessionSnapshot.ts"
      provides: "Canonical snapshot endpoint"
      exports: ["getSessionSnapshotHandler"]
    - path: "services/api/src/routes/sessions/sessionEvents.ts"
      provides: "Server-sent events stream for session updates"
      exports: ["sessionEventsHandler"]
    - path: "src/hooks/useSessionSync.ts"
      provides: "Hydrate + subscribe + reconnect hook"
      exports: ["useSessionSync"]
    - path: "services/api/tests/session-sync.test.ts"
      provides: "Synchronization regression coverage"
      contains: "two clients"
  key_links:
    - from: "src/hooks/useSessionSync.ts"
      to: "services/api/src/routes/sessions/getSessionSnapshot.ts"
      via: "initial hydration fetch"
      pattern: "getSessionSnapshot"
    - from: "src/hooks/useSessionSync.ts"
      to: "services/api/src/routes/sessions/sessionEvents.ts"
      via: "SSE subscribe + reconnect"
      pattern: "EventSource"
---

<objective>
Add canonical hydration + live session sync so both participants stay aligned.

Purpose: Prevent trust-breaking desync before location capture and later ranking phases.
Output: Snapshot/SSE handlers, lifecycle transition guard, sync hook, and automated sync tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
@.planning/phases/01-discovery-session-input-backbone/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement snapshot + SSE handlers with lifecycle transition guard</name>
  <files>services/api/src/routes/sessions/getSessionSnapshot.ts, services/api/src/routes/sessions/sessionEvents.ts, services/api/src/modules/session/state-machine.ts, services/api/tests/session-sync.test.ts</files>
  <behavior>
    - Test 1: `GET /v1/sessions/:sessionId` returns canonical snapshot with participant states in deterministic order.
    - Test 2: `GET /v1/sessions/:sessionId/events` emits `participant_joined` and `session_updated` events.
    - Test 3: Illegal transition attempts are rejected by state machine (e.g., expired -> joined).
  </behavior>
  <action>Create `state-machine.ts` with explicit allowed transitions (`created -> joined -> locating -> ranked -> confirmed|expired`) and validator function used by route handlers. Implement snapshot and SSE handlers in Cloud Run service; SSE payloads must include `eventType`, `sessionId`, `updatedAt`, and minimal participant diff. Write tests first in `services/api/tests/session-sync.test.ts`, then implement to pass with stable event ordering and transition validation.</action>
  <verify>
    <automated>pnpm test services/api/tests/session-sync.test.ts -x</automated>
  </verify>
  <done>Canonical read + live event stream exist and illegal lifecycle transitions are blocked server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire hydrate-first sync hook and participant status UI</name>
  <files>src/hooks/useSessionSync.ts, src/components/session/ParticipantStatus.tsx, src/app/s/[token]/page.tsx, src/lib/api/session-client.ts, services/api/tests/session-sync.test.ts</files>
  <behavior>
    - Test 1: UI hydrates from snapshot before opening EventSource.
    - Test 2: Second-client join updates participant status on first client without refresh.
    - Test 3: EventSource drop triggers reconnect and re-hydration.
  </behavior>
  <action>Create `useSessionSync(sessionId)` to call `getSessionSnapshot` then subscribe to SSE via EventSource, with exponential-backoff reconnect and forced snapshot refresh on reconnect success. Update `ParticipantStatus.tsx` to render host/invitee join + location confirmation badges from canonical state. Update `/s/[token]/page.tsx` to show `syncing`, `live`, `reconnecting` indicators and route all reads through `session-client.ts` (no direct fetch literals).</action>
  <verify>
    <automated>pnpm test services/api/tests/session-sync.test.ts -x</automated>
  </verify>
  <done>SESS-03 is met: two clients converge on identical state through hydration + live updates + reconnect recovery.</done>
</task>

</tasks>

<verification>
Run sync tests to verify hydrate-first behavior, live updates, and reconnect recovery without stale state.
</verification>

<success_criteria>
- Host and invitee see the same participant/session state.
- Session page recovers from reconnect without stale indicators.
- Synchronization behavior has repeatable automated coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-02-SUMMARY.md`
</output>
