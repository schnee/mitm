---
phase: 01-discovery-session-input-backbone
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/realtime/session-channel.ts
  - src/hooks/useSessionState.ts
  - src/components/session/ParticipantStatus.tsx
  - src/app/api/sessions/[sessionId]/route.ts
  - src/app/s/[token]/page.tsx
  - tests/realtime/session-sync.test.ts
autonomous: true
requirements: [SESS-03]
must_haves:
  truths:
    - "Both participants see the same session state as joins and updates occur."
    - "Session page hydrates from canonical server state before live updates."
    - "Realtime disconnect/reconnect does not leave participants in a stale terminal state."
  artifacts:
    - path: "src/hooks/useSessionState.ts"
      provides: "Canonical state hydration + realtime subscription hook"
      exports: ["useSessionState"]
    - path: "src/app/api/sessions/[sessionId]/route.ts"
      provides: "Fetch canonical session snapshot"
      exports: ["GET"]
    - path: "tests/realtime/session-sync.test.ts"
      provides: "Synchronization regression coverage"
      contains: "two clients"
  key_links:
    - from: "src/hooks/useSessionState.ts"
      to: "src/app/api/sessions/[sessionId]/route.ts"
      via: "initial hydration fetch"
      pattern: "fetch\(`/api/sessions/\${sessionId}`"
    - from: "src/hooks/useSessionState.ts"
      to: "src/lib/realtime/session-channel.ts"
      via: "subscribe/unsubscribe lifecycle"
      pattern: "subscribeSessionChannel|unsubscribe"
---

<objective>
Add reliable synchronized state so both participants observe the same session lifecycle in near real time.

Purpose: Prevent trust-breaking desync before location capture and later ranking phases.
Output: Session state GET endpoint, realtime channel helper, client sync hook, and synchronization tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
@.planning/phases/01-discovery-session-input-backbone/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical session state read + realtime channel abstraction</name>
  <files>src/app/api/sessions/[sessionId]/route.ts, src/lib/realtime/session-channel.ts, tests/realtime/session-sync.test.ts</files>
  <read_first>src/app/api/sessions/route.ts, src/app/api/sessions/join/route.ts, src/lib/contracts/session.ts, tests/realtime/ (if present)</read_first>
  <behavior>
    - Test 1: GET /api/sessions/{id} returns canonical participants + status snapshot.
    - Test 2: Realtime channel helper emits join and state update events with stable payload shape.
    - Test 3: Unsubscribe cleanup prevents duplicate event handling on remount.
  </behavior>
  <action>Create `GET /api/sessions/[sessionId]` endpoint that returns session status plus both participant records in deterministic order (host then invitee). Build `src/lib/realtime/session-channel.ts` with explicit functions `subscribeSessionChannel(sessionId, onEvent)` and `unsubscribeSessionChannel(subscription)` wrapping Supabase channel calls. Add tests in `tests/realtime/session-sync.test.ts` for snapshot shape, event propagation, and duplicate-subscription guard behavior.</action>
  <acceptance_criteria>
    - `src/app/api/sessions/[sessionId]/route.ts` exports `GET` and returns participants array.
    - `src/lib/realtime/session-channel.ts` exports both subscribe and unsubscribe helpers.
    - Realtime test file includes duplicate event prevention assertion.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/realtime/session-sync.test.ts -x</automated>
  </verify>
  <done>Session snapshot endpoint and realtime wrapper exist with automated sync coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire synchronized participant status UI on join page</name>
  <files>src/hooks/useSessionState.ts, src/components/session/ParticipantStatus.tsx, src/app/s/[token]/page.tsx, tests/realtime/session-sync.test.ts</files>
  <read_first>src/lib/realtime/session-channel.ts, src/app/api/sessions/[sessionId]/route.ts, src/app/s/[token]/page.tsx, src/components/session/ (existing patterns)</read_first>
  <behavior>
    - Test 1: Page hydrates from GET snapshot before subscribing to realtime updates.
    - Test 2: Participant status component updates when invitee joins from second client.
    - Test 3: Reconnect triggers state refresh and keeps both clients aligned.
  </behavior>
  <action>Create `useSessionState(sessionId)` hook that fetches `/api/sessions/{sessionId}` once, then subscribes using `subscribeSessionChannel` and merges updates into canonical local state. Add `ParticipantStatus` component that renders host/invitee presence (`waiting`, `joined`) and last update timestamp. Update `/s/[token]` page to use the hook and show synchronization banner states: `syncing`, `live`, `reconnecting`. Extend tests to assert UI state transitions for second-client join and reconnect refresh.</action>
  <acceptance_criteria>
    - `src/hooks/useSessionState.ts` contains both hydration fetch and realtime subscription lifecycle.
    - `src/components/session/ParticipantStatus.tsx` renders both participant slots explicitly.
    - `/s/[token]/page.tsx` renders `syncing` and `live` indicators.
    - Sync tests pass for hydrate + live update + reconnect scenarios.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/realtime/session-sync.test.ts -x</automated>
  </verify>
  <done>Two clients can observe synchronized session state from canonical snapshot plus realtime updates.</done>
</task>

</tasks>

<verification>
Run synchronization tests and verify join-page state transitions from hydration to live mode with no duplicate updates.
</verification>

<success_criteria>
- Host and invitee see the same participant/session state.
- Session page recovers from reconnect without stale indicators.
- Synchronization behavior has repeatable automated coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-02-SUMMARY.md`
</output>
