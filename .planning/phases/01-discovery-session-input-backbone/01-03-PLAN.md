---
phase: 01-discovery-session-input-backbone
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/lib/validation/location.ts
  - src/app/api/sessions/[sessionId]/location/route.ts
  - src/app/api/sessions/[sessionId]/location/confirm/route.ts
  - src/components/location/LocationCaptureForm.tsx
  - src/components/location/LocationConfirmCard.tsx
  - src/app/s/[token]/page.tsx
  - tests/location/location-flow.test.ts
  - tests/e2e/location-fallback.spec.ts
autonomous: true
requirements: [LOCT-01, LOCT-02, LOCT-03]
must_haves:
  truths:
    - "Each participant can provide location from geolocation or manual address entry."
    - "Geolocation denial/timeout still allows completion via manual entry without dead end."
    - "Each participant explicitly confirms location before session marks inputs ready."
  artifacts:
    - path: "src/components/location/LocationCaptureForm.tsx"
      provides: "Dual input-mode capture UI with error fallback"
      min_lines: 60
    - path: "src/app/api/sessions/[sessionId]/location/confirm/route.ts"
      provides: "Explicit confirmation endpoint"
      exports: ["POST"]
    - path: "tests/e2e/location-fallback.spec.ts"
      provides: "Denial/timeout fallback journey coverage"
      contains: "manual entry"
  key_links:
    - from: "src/components/location/LocationCaptureForm.tsx"
      to: "/api/sessions/[sessionId]/location"
      via: "draft location submit"
      pattern: "fetch\(`/api/sessions/\${sessionId}/location`"
    - from: "src/components/location/LocationConfirmCard.tsx"
      to: "/api/sessions/[sessionId]/location/confirm"
      via: "explicit confirmation action"
      pattern: "fetch\(`/api/sessions/\${sessionId}/location/confirm`"
---

<objective>
Implement consented location capture with robust fallback and explicit confirmation for both participants.

Purpose: Ensure downstream ranking receives trustworthy, user-confirmed inputs without geolocation dead ends.
Output: Location APIs, capture/confirm UI components, and automated tests for success + fallback paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
@.planning/phases/01-discovery-session-input-backbone/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build location capture/confirm API contracts and tests</name>
  <files>src/lib/validation/location.ts, src/app/api/sessions/[sessionId]/location/route.ts, src/app/api/sessions/[sessionId]/location/confirm/route.ts, tests/location/location-flow.test.ts</files>
  <read_first>src/lib/contracts/session.ts, src/lib/validation/session.ts, src/app/api/sessions/[sessionId]/route.ts, tests/location/ (if present)</read_first>
  <behavior>
    - Test 1: Draft location endpoint accepts geolocation payload and manual payload variants.
    - Test 2: Confirmation endpoint sets `confirmed_at` only for existing draft location.
    - Test 3: Invalid payloads (missing lat/lng and empty manual address) are rejected with 4xx.
  </behavior>
  <action>Create `src/lib/validation/location.ts` with zod schemas: `GeolocationDraftSchema` (`mode:'geolocation'`, `lat`, `lng`, optional `label`), `ManualDraftSchema` (`mode:'manual'`, `address`, optional `lat/lng` if geocoded), and `ConfirmLocationSchema` (`participantId`, `sessionId`). Implement `POST /api/sessions/[sessionId]/location` to upsert draft row with `input_mode`, coordinates/address, and `updated_at` (no confirm timestamp). Implement `POST /api/sessions/[sessionId]/location/confirm` to set `confirmed_at=now()` only if draft exists for participant; otherwise return 409 with explicit error. Write location API tests first and keep deterministic status assertions.</action>
  <acceptance_criteria>
    - `src/lib/validation/location.ts` exports draft and confirm schemas.
    - Draft endpoint distinguishes `mode:'geolocation'` and `mode:'manual'`.
    - Confirm endpoint returns 409 when no draft exists.
    - `tests/location/location-flow.test.ts` covers both input modes and confirm precondition.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/location/location-flow.test.ts -x</automated>
  </verify>
  <done>Location APIs enforce capture + explicit confirmation contract with validation coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement geolocation/manual fallback and confirmation UI flow</name>
  <files>src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/app/s/[token]/page.tsx, tests/location/location-flow.test.ts, tests/e2e/location-fallback.spec.ts</files>
  <read_first>src/app/s/[token]/page.tsx, src/hooks/useSessionState.ts, src/components/session/ParticipantStatus.tsx, src/app/api/sessions/[sessionId]/location/route.ts, src/app/api/sessions/[sessionId]/location/confirm/route.ts</read_first>
  <behavior>
    - Test 1: If geolocation succeeds, user can review and confirm captured location.
    - Test 2: If geolocation is denied/timeout, UI immediately presents manual entry and still reaches confirm.
    - Test 3: Session shows per-participant `location_confirmed` status and only marks inputs ready when both confirmed.
  </behavior>
  <action>Create `LocationCaptureForm` with primary "Use current location" action, explicit error handling for `PERMISSION_DENIED`, `POSITION_UNAVAILABLE`, and `TIMEOUT`, and always-visible "Enter address manually" path. Manual submission posts `{mode:'manual', address}` to location draft endpoint. Create `LocationConfirmCard` that shows pending draft location and posts to confirm endpoint. Update `/s/[token]` page to render capture + confirmation for the active participant and display both participants' location confirmation badges via synchronized state. Add e2e test `tests/e2e/location-fallback.spec.ts` that stubs denied geolocation, completes manual entry, confirms location, and verifies no dead-end UI state.</action>
  <acceptance_criteria>
    - `LocationCaptureForm.tsx` handles denied/unavailable/timeout errors with explicit fallback messaging.
    - `LocationConfirmCard.tsx` invokes confirm endpoint and renders confirmed state.
    - `/s/[token]/page.tsx` displays both participants' location confirmation status.
    - `tests/e2e/location-fallback.spec.ts` includes denied-geolocation → manual entry → confirm path and passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/location/location-flow.test.ts -x && pnpm playwright test tests/e2e/location-fallback.spec.ts</automated>
  </verify>
  <done>Participants can complete location input through geolocation or fallback, and explicit confirmation is required.</done>
</task>

</tasks>

<verification>
Run location API tests and e2e fallback test to prove no-dead-end behavior plus explicit confirmation before readiness.
</verification>

<success_criteria>
- Both input modes (geolocation/manual) complete successfully.
- Geolocation failures are recoverable without abandoning flow.
- Confirmed-location gate is enforced for each participant.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-03-SUMMARY.md`
</output>
