---
phase: 01-discovery-session-input-backbone
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - services/api/src/modules/location/validation.ts
  - services/api/src/modules/location/repository.ts
  - services/api/src/routes/location/upsertLocationDraft.ts
  - services/api/src/routes/location/confirmLocation.ts
  - services/api/src/scripts/ensure-firestore-ttl.sh
  - services/api/tests/location-flow.test.ts
  - src/components/location/LocationCaptureForm.tsx
  - src/components/location/LocationConfirmCard.tsx
  - src/app/s/[token]/page.tsx
  - src/lib/api/session-client.ts
  - tests/e2e/location-fallback.spec.ts
autonomous: false
requirements: [LOCT-01, LOCT-02, LOCT-03]
must_haves:
  truths:
    - "Each participant can provide location from geolocation or manual address entry."
    - "Geolocation denial/timeout still allows completion via manual entry without dead end."
    - "Each participant explicitly confirms location before session marks inputs ready."
    - "Precise location records are marked for TTL expiry instead of indefinite retention."
  artifacts:
    - path: "src/components/location/LocationCaptureForm.tsx"
      provides: "Dual input-mode capture UI with error fallback"
      min_lines: 60
    - path: "services/api/src/routes/location/confirmLocation.ts"
      provides: "Explicit confirmation endpoint"
      exports: ["confirmLocationHandler"]
    - path: "services/api/src/scripts/ensure-firestore-ttl.sh"
      provides: "Firestore TTL policy enforcement for precise location fields"
      contains: "expireAt"
    - path: "tests/e2e/location-fallback.spec.ts"
      provides: "Denial/timeout fallback journey coverage"
      contains: "manual entry"
  key_links:
    - from: "src/components/location/LocationCaptureForm.tsx"
      to: "services/api/src/routes/location/upsertLocationDraft.ts"
      via: "draft location submit"
      pattern: "upsertLocationDraft"
    - from: "src/components/location/LocationConfirmCard.tsx"
      to: "services/api/src/routes/location/confirmLocation.ts"
      via: "explicit confirmation action"
      pattern: "confirmLocation"
---

<objective>
Implement consented location capture with robust fallback and explicit confirmation for both participants.

Purpose: Ensure downstream ranking receives trustworthy, user-confirmed inputs without geolocation dead ends and with TTL privacy guardrails.
Output: Location draft/confirm handlers, TTL setup script, fallback UI, automated tests, and one human end-to-end verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-session-input-backbone/01-RESEARCH.md
@.planning/phases/01-discovery-session-input-backbone/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement location draft/confirm API + Firestore TTL guard</name>
  <files>services/api/src/modules/location/validation.ts, services/api/src/modules/location/repository.ts, services/api/src/routes/location/upsertLocationDraft.ts, services/api/src/routes/location/confirmLocation.ts, services/api/src/scripts/ensure-firestore-ttl.sh, services/api/tests/location-flow.test.ts</files>
  <behavior>
    - Test 1: Draft endpoint accepts both `mode='geolocation'` and `mode='manual'` payloads.
    - Test 2: Confirm endpoint sets `confirmedAt` only if a draft exists.
    - Test 3: TTL script validates/creates policy on `participant_locations.expireAt` for precise-location cleanup.
  </behavior>
  <action>Create zod schemas for geolocation/manual drafts and confirm intent in `validation.ts`. Implement repository + handlers to upsert participant draft (`lat`, `lng`, `addressLabel`, `updatedAt`, `expireAt`) and set `confirmedAt` on explicit confirm only. Add `ensure-firestore-ttl.sh` to check/create TTL policy for `expireAt` and fail with actionable output if policy is missing. Write tests first in `services/api/tests/location-flow.test.ts` for both modes, confirm precondition, and readiness gate behavior.</action>
  <verify>
    <automated>pnpm test services/api/tests/location-flow.test.ts -x && bash services/api/src/scripts/ensure-firestore-ttl.sh --check-only</automated>
  </verify>
  <done>Location API contracts pass tests and precise location retention is protected by TTL policy checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement geolocation/manual fallback and confirmation UI flow</name>
  <files>src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/app/s/[token]/page.tsx, src/lib/api/session-client.ts, tests/e2e/location-fallback.spec.ts</files>
  <behavior>
    - Test 1: If geolocation succeeds, user can review and confirm captured location.
    - Test 2: If geolocation is denied/timeout, UI immediately presents manual entry and still reaches confirm.
    - Test 3: Session shows per-participant `location_confirmed` status and only marks inputs ready when both confirmed.
  </behavior>
  <action>Create `LocationCaptureForm` with geolocation first and always-visible manual path. Handle `PERMISSION_DENIED`, `POSITION_UNAVAILABLE`, and `TIMEOUT` with non-blocking fallback messaging. Wire form to typed client methods (`upsertLocationDraft`, `confirmLocation`) in `session-client.ts` (no raw fetch strings). Create `LocationConfirmCard` that blocks "Confirm" until a draft exists. Update `/s/[token]` page to surface both participant confirmation states from `useSessionSync`; enable the phase readiness flag only when both `confirmedAt` timestamps are present. Add Playwright test for denied geolocation -> manual entry -> explicit confirm path.</action>
  <verify>
    <automated>pnpm test services/api/tests/location-flow.test.ts -x && pnpm playwright test tests/e2e/location-fallback.spec.ts</automated>
  </verify>
  <done>Participants can complete location input through geolocation or fallback, and explicit confirmation is required.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dual-client location flow end-to-end</name>
  <files>tests/e2e/location-fallback.spec.ts</files>
  <action>Run automated e2e coverage first, then perform the human dual-browser validation sequence to confirm synchronized confirmation states and fallback UX clarity before phase sign-off.</action>
  <what-built>Location draft + explicit confirmation + synchronized status across both participants</what-built>
  <how-to-verify>
    1. Start host flow and create a session; open join link in second browser profile.
    2. In browser A, deny geolocation and complete manual address entry, then confirm location.
    3. In browser B, use current location and confirm.
    4. Confirm both screens show both participants as location-confirmed and no dead-end/error state remains.
    5. Leave session idle beyond configured TTL window (or run TTL check command) and confirm precise location is marked for expiry.
  </how-to-verify>
  <verify>
    <automated>pnpm playwright test tests/e2e/location-fallback.spec.ts</automated>
  </verify>
  <done>Human verifier confirms both participants can complete and observe confirmed location state without dead ends.</done>
  <resume-signal>Type "approved" to continue, or list observed mismatches.</resume-signal>
</task>

</tasks>

<verification>
Run location API + e2e fallback tests, then complete dual-client human verification before phase sign-off.
</verification>

<success_criteria>
- Both input modes (geolocation/manual) complete successfully.
- Geolocation failures are recoverable without abandoning flow.
- Confirmed-location gate is enforced for each participant.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-session-input-backbone/01-03-SUMMARY.md`
</output>
