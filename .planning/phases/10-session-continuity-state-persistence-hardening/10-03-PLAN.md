---
phase: 10-session-continuity-state-persistence-hardening
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements: [SESS-03, LOCT-02, LOCT-03, UX-11, UX-25]
must_haves:
  truths:
    - "A user can save a location draft, reload, and still confirm location without re-entering data."
    - "A participant who saved preferences sees correct waiting/generating continuity after reload."
    - "One sticky next-action CTA and partner-progress status remain correct across reload continuity scenarios."
  artifacts:
    - path: "tests/e2e/ranking-results.spec.ts"
      provides: "Browser-level continuity regression proof for GAP-01 and GAP-03"
      contains: "reload/rejoin assertions for location confirm and ranking lifecycle continuity"
  key_links:
    - from: "tests/e2e/ranking-results.spec.ts"
      to: "src/app/s/[token]/page.tsx"
      via: "reload assertions on guided-step state + status badges"
      pattern: "page.reload|Waiting for partner|Confirm location"
---

<objective>
Add end-to-end continuity regressions that prove location and preference progression survive refresh/rejoin with correct next actions and partner-progress messaging.

Purpose: Convert Phase 10 continuity fixes into durable browser-runtime evidence tied to GAP-01/GAP-03 closure.
Output: Reload-focused e2e tests guard LOCT/UX continuity behavior from regression.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/10-session-continuity-state-persistence-hardening/10-RESEARCH.md
@.planning/phases/10-session-continuity-state-persistence-hardening/10-02-SUMMARY.md
@tests/e2e/ranking-results.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reload continuity e2e scenario for location confirm gate</name>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <read_first>
    - tests/e2e/ranking-results.spec.ts
    - src/app/s/[token]/page.tsx
    - src/components/location/LocationConfirmCard.tsx
  </read_first>
  <behavior>
    - Test 1: Save manual location draft, reload page, and verify `Confirm location` remains enabled.
    - Test 2: Confirm location post-reload advances to preferences and keeps summary `Location: Confirmed`.
    - Test 3: Manual fallback path remains usable if geolocation route is unavailable.
  </behavior>
  <action>Extend `ranking-results.spec.ts` with a dedicated test fixture that stubs snapshot participants with `locationDraftUpdatedAt` populated for the current participant before confirmation. Include a `page.reload()` step between draft save and confirm click, then assert confirm still succeeds and guided flow advances. Keep selectors aligned with existing status/step attributes (`data-step-id`, status-badge text) to prevent flaky checks (LOCT-02, LOCT-03, SESS-03).</action>
  <acceptance_criteria>
    - Test contains an explicit reload step between draft save and confirmation.
    - Assertions verify confirm button/action remains available post-reload.
    - Assertions verify progression to preferences after post-reload confirm.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5"</automated>
  </verify>
  <done>E2E coverage proves location confirmation continuity survives refresh without forcing draft re-entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reload continuity e2e scenario for preference lifecycle + partner status</name>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <read_first>
    - tests/e2e/ranking-results.spec.ts
    - src/app/s/[token]/page.tsx
    - src/components/session/SessionProgressBar.tsx
    - src/components/session/NextActionRail.tsx
  </read_first>
  <behavior>
    - Test 1: After saving preferences, reload still shows waiting/generating lifecycle state correctly.
    - Test 2: Sticky rail still presents one next-action CTA after reload.
    - Test 3: Partner-progress copy remains explicit (`Waiting for partner` / `Loading shared suggestions`).
  </behavior>
  <action>Add a second reload-focused scenario where ranking inputs are saved, then page reloads while lifecycle is `waiting` or `generating`. Assert that the spots step remains/returns active as appropriate, lifecycle copy remains state-specific, and `.next-action-button` count stays exactly 1. Reuse existing route stubs/events and add minimal new fixture data (participant `rankingInputsUpdatedAt`) rather than creating a separate test harness (UX-11, UX-25, SESS-03).</action>
  <acceptance_criteria>
    - Test asserts lifecycle copy after reload for waiting/generating state.
    - Test asserts exactly one sticky next-action button remains visible post-reload.
    - Test uses participant-level ranking-input continuity fields in fixture payload.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5" --project="Desktop Safari"</automated>
  </verify>
  <done>E2E continuity protection covers both location and preference reload/rejoin paths with partner-progress messaging intact.</done>
</task>

</tasks>

<verification>
Run targeted Playwright continuity tests on mobile + Safari desktop to prove reload stability for location confirm and preference lifecycle progression.
</verification>

<success_criteria>
- GAP-01 broken flow has an executable regression proving post-reload confirm gating correctness.
- GAP-03 broken flow has an executable regression proving preference lifecycle continuity and partner-progress status correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/10-session-continuity-state-persistence-hardening/10-03-SUMMARY.md`
</output>
