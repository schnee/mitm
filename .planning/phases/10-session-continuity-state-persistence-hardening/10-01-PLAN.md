---
phase: 10-session-continuity-state-persistence-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - src/lib/api/session-client.ts
  - services/api/tests/location-flow.test.ts
  - services/api/tests/ranking-inputs.test.ts
  - services/api/tests/session-sync.test.ts
autonomous: true
requirements: [SESS-03, LOCT-02, LOCT-03, UX-13]
must_haves:
  truths:
    - "After refresh/rejoin, session snapshot still exposes each participant's location draft + ranking input progress."
    - "Location confirm gating can be derived from server state, not only same-page local state."
    - "Ranking input completion continuity survives reload because participant-level save timestamps are present in canonical snapshot."
  artifacts:
    - path: "services/api/src/modules/session/contracts.ts"
      provides: "Snapshot/event participant fields for continuity derivation"
      contains: "locationDraftUpdatedAt and rankingInputsUpdatedAt"
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Canonical snapshot projection with participant continuity fields"
      contains: "getSessionSnapshot participant mapping includes draft/inputs timestamps"
    - path: "src/lib/api/session-client.ts"
      provides: "Frontend contract parity for new participant continuity fields"
      contains: "SessionSnapshotParticipant fields for locationDraftUpdatedAt and rankingInputsUpdatedAt"
  key_links:
    - from: "services/api/src/modules/session/repository.ts"
      to: "src/lib/api/session-client.ts"
      via: "snapshot JSON contract"
      pattern: "locationDraftUpdatedAt|rankingInputsUpdatedAt"
    - from: "services/api/tests/session-sync.test.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "snapshot assertions"
      pattern: "getSessionSnapshot"
---

<objective>
Harden canonical session contracts so refresh/rejoin continuity for location and preference progression is derivable from snapshot/event data.

Purpose: Close GAP-01 and the contract half of GAP-03 by removing local-only progression truth sources.
Output: API + frontend snapshot contracts expose participant draft/prefs progress with automated proof.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/10-session-continuity-state-persistence-hardening/10-RESEARCH.md

<interfaces>
From services/api/src/modules/session/contracts.ts:
```ts
export interface ParticipantRecord {
  locationDraftUpdatedAt?: string;
  rankingInputsUpdatedAt?: string;
}

export interface SessionSnapshotParticipant {
  participantId: string;
  role: SessionParticipantRole;
  joinedAt: string;
  locationConfirmedAt: string | null;
}
```

From src/lib/api/session-client.ts:
```ts
export interface SessionSnapshotParticipant {
  participantId: string;
  role: "host" | "invitee";
  joinedAt: string;
  locationConfirmedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend snapshot participant continuity contract</name>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, src/lib/api/session-client.ts</files>
  <read_first>
    - services/api/src/modules/session/contracts.ts
    - services/api/src/modules/session/repository.ts
    - src/lib/api/session-client.ts
  </read_first>
  <behavior>
    - Test 1: Session snapshot participant includes `locationDraftUpdatedAt` when a draft exists.
    - Test 2: Session snapshot participant includes `rankingInputsUpdatedAt` after ranking input save.
    - Test 3: Existing fields (`locationConfirmedAt`, role ordering) remain unchanged.
  </behavior>
  <action>Add `locationDraftUpdatedAt: string | null` and `rankingInputsUpdatedAt: string | null` to `SessionSnapshotParticipant` contracts in both backend and frontend clients. Update `SessionRepository.getSessionSnapshot()` participant projection to emit those fields from participant records with explicit null fallback. Keep field names exactly `locationDraftUpdatedAt` and `rankingInputsUpdatedAt` so downstream UI can derive continuity without local-only flags (SESS-03, LOCT-02, UX-13).</action>
  <acceptance_criteria>
    - `services/api/src/modules/session/contracts.ts` `SessionSnapshotParticipant` contains both new fields as nullable strings.
    - `services/api/src/modules/session/repository.ts` participant mapping includes both fields with `?? null` fallback.
    - `src/lib/api/session-client.ts` `SessionSnapshotParticipant` includes both fields.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/session-sync.test.ts</automated>
  </verify>
  <done>Canonical snapshot and client contracts expose participant draft + ranking-input continuity fields with no type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add continuity-focused backend contract tests</name>
  <files>services/api/tests/location-flow.test.ts, services/api/tests/ranking-inputs.test.ts, services/api/tests/session-sync.test.ts</files>
  <read_first>
    - services/api/tests/location-flow.test.ts
    - services/api/tests/ranking-inputs.test.ts
    - services/api/tests/session-sync.test.ts
    - services/api/src/modules/session/repository.ts
  </read_first>
  <behavior>
    - Test 1: After `upsertLocationDraft`, snapshot participant has non-null `locationDraftUpdatedAt` before confirmation.
    - Test 2: After ranking input save, snapshot participant has non-null `rankingInputsUpdatedAt`.
    - Test 3: Event/snapshot sync tests still pass with deterministic ordering and new fields present.
  </behavior>
  <action>Add assertions in existing API tests to enforce continuity contract behavior: location draft timestamp persisted and visible in snapshot; ranking input timestamp persisted and visible in snapshot for the saving participant; previous ready/waiting lifecycle assertions remain intact. Keep tests in existing files (no new test framework) and use real repository flows to prove end-to-end contract integrity (LOCT-03, UX-13, SESS-03).</action>
  <acceptance_criteria>
    - `location-flow.test.ts` asserts `locationDraftUpdatedAt` transitions from null to non-null after draft save.
    - `ranking-inputs.test.ts` asserts snapshot participants include `rankingInputsUpdatedAt` for saved participant(s).
    - `session-sync.test.ts` includes at least one assertion for new participant continuity fields.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/location-flow.test.ts services/api/tests/ranking-inputs.test.ts services/api/tests/session-sync.test.ts</automated>
  </verify>
  <done>Automated tests prove backend emits continuity-safe participant state required for refresh/rejoin correctness.</done>
</task>

</tasks>

<verification>
Run API continuity tests and confirm snapshot contract includes draft/prefs participant fields without regressions in session sync behavior.
</verification>

<success_criteria>
- Refresh/rejoin consumers can derive whether location draft exists and whether preferences were previously saved from snapshot data alone.
- Continuity contract fields are covered by automated tests and stable across existing route handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/10-session-continuity-state-persistence-hardening/10-01-SUMMARY.md`
</output>
