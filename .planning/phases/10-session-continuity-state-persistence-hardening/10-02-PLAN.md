---
phase: 10-session-continuity-state-persistence-hardening
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/hooks/useSessionSync.ts
  - src/app/s/[token]/page.tsx
  - src/components/location/LocationConfirmCard.tsx
  - src/lib/session-flow.ts
  - src/lib/session-flow.test.ts
autonomous: true
requirements: [SESS-03, LOCT-02, LOCT-03, UX-11, UX-13, UX-25]
must_haves:
  truths:
    - "After reload, user can still confirm location if a server-side draft already exists."
    - "After reload, saved ranking inputs still render correct waiting/generating progression and blocker ownership."
    - "Status copy remains concise and state-specific for waiting/loading/error/success with partner-progress context."
  artifacts:
    - path: "src/app/s/[token]/page.tsx"
      provides: "Snapshot-derived progression and continuity-safe guided flow"
      contains: "no local-only gating dependency for location/preference completion"
    - path: "src/components/location/LocationConfirmCard.tsx"
      provides: "Confirm button gating based on canonical draft presence"
      contains: "canConfirm based on snapshot-derived draft state"
    - path: "src/lib/session-flow.ts"
      provides: "Deterministic active-step/blocker derivation"
      contains: "partner-progress-aware summaries and blockers"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/hooks/useSessionSync.ts"
      via: "snapshot participant continuity fields"
      pattern: "locationDraftUpdatedAt|rankingInputsUpdatedAt"
    - from: "src/components/location/LocationConfirmCard.tsx"
      to: "src/app/s/[token]/page.tsx"
      via: "draft-ready confirm gating prop"
      pattern: "draftSaved|draftReady"
---

<objective>
Refactor session page progression to use canonical snapshot continuity state so reload/rejoin preserves correct location confirmation, preference progression, and partner-status guidance.

Purpose: Close the UI continuity side of GAP-01 and GAP-03 without changing ranking/provider fundamentals.
Output: Guided step flow and status rails remain accurate across refreshes using server-derived progression truth.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/10-session-continuity-state-persistence-hardening/10-RESEARCH.md
@.planning/phases/10-session-continuity-state-persistence-hardening/10-01-SUMMARY.md

<interfaces>
From src/hooks/useSessionSync.ts:
```ts
export function useSessionSync(sessionId: string | null): {
  syncState: "syncing" | "live" | "reconnecting";
  snapshot: SessionSnapshotResponse | null;
  error: string | null;
}
```

From src/lib/session-flow.ts:
```ts
export function deriveSessionFlow(input: DeriveSessionFlowInput): { activeStepId: SessionStepId; steps: SessionFlowStep[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace local-only continuity flags with snapshot-derived progression</name>
  <files>src/app/s/[token]/page.tsx, src/components/location/LocationConfirmCard.tsx</files>
  <read_first>
    - src/app/s/[token]/page.tsx
    - src/components/location/LocationConfirmCard.tsx
    - src/lib/api/session-client.ts
  </read_first>
  <behavior>
    - Test 1: If `me.locationDraftUpdatedAt` exists after hydration, confirm action is enabled without requiring same-render draft save callback.
    - Test 2: If `me.rankingInputsUpdatedAt` exists after hydration, preferences step is treated as saved for current participant.
    - Test 3: Existing transient statuses (loading/error) still update during in-flight actions.
  </behavior>
  <action>In `page.tsx`, derive `myDraftReady` from snapshot participant (`locationDraftUpdatedAt` non-null) OR local optimistic draft save, and pass that to `LocationConfirmCard` for confirm gating. Derive `myPreferencesSaved` from current participant `rankingInputsUpdatedAt` first, then local optimistic fallback; remove dependency on `rankingLifecycleState !== "waiting"` as proxy. Update any location/preference waiting copy to reflect canonical partner-progress context (`Waiting for partner...`) for UX-25. In `LocationConfirmCard`, gate confirm button by a `draftReady` boolean (derived truth) rather than same-render-only callback state (LOCT-02/LOCT-03/SESS-03/UX-11/UX-13).</action>
  <acceptance_criteria>
    - `page.tsx` reads participant `locationDraftUpdatedAt` and `rankingInputsUpdatedAt` from snapshot for progression.
    - `LocationConfirmCard` confirm button enablement no longer depends solely on local callback-only state.
    - `rankingLifecycleState !== "waiting"` is not used as the primary "my preferences saved" inference.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/session-flow.test.ts</automated>
  </verify>
  <done>Refresh/rejoin can recover location/prefs progression from canonical snapshot participant data with correct blocker ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten flow derivation and unit coverage for reload continuity scenarios</name>
  <files>src/lib/session-flow.ts, src/lib/session-flow.test.ts, src/hooks/useSessionSync.ts</files>
  <read_first>
    - src/lib/session-flow.ts
    - src/lib/session-flow.test.ts
    - src/hooks/useSessionSync.ts
  </read_first>
  <behavior>
    - Test 1: Flow remains deterministic when location/prefs are hydrated from snapshot after reload.
    - Test 2: Waiting vs generating states preserve clear partner/self blocker ownership.
    - Test 3: Sync merge logic retains continuity fields from snapshot/events where provided.
  </behavior>
  <action>Update `deriveSessionFlow` summaries/blockers only where needed to reflect canonical continuity states and avoid regressions in shortlist/confirm progression. Add/extend unit tests for reload cases: (a) location draft already exists but not confirmed, (b) participant saved preferences while partner has not, (c) both preferences saved and lifecycle generating/ready. In `useSessionSync`, preserve compatibility with added participant continuity fields from snapshot/event payloads and keep existing SSE/poll fallback behavior unchanged (SESS-03, UX-11, UX-25).</action>
  <acceptance_criteria>
    - `session-flow.test.ts` includes explicit reload continuity scenarios with snapshot-derived progression assumptions.
    - `session-flow.ts` still returns one active step and stable order `location -> preferences -> spots -> shortlist -> confirm`.
    - `useSessionSync.ts` compiles with new participant continuity fields and keeps existing merge contract for other diffs.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/session-flow.test.ts services/api/tests/session-sync.test.ts</automated>
  </verify>
  <done>Unit-level coverage proves continuity logic remains deterministic and partner-progress messaging stays accurate after reload.</done>
</task>

</tasks>

<verification>
Validate flow derivation and sync hydration using unit tests, ensuring no local-only progression dead-ends remain for location or preference steps.
</verification>

<success_criteria>
- Reload does not reset confirmability when location draft already exists.
- Reload does not regress preference save progression into incorrect waiting/idle states.
- Partner-progress and lifecycle copy remains explicit and state-specific.
</success_criteria>

<output>
After completion, create `.planning/phases/10-session-continuity-state-persistence-hardening/10-02-SUMMARY.md`
</output>
