---
phase: 04-launch-readiness-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSessionSync.ts
  - tests/e2e/session-sync-fallback.spec.ts
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Session screen remains synchronized even when SSE/EventSource is unavailable on a browser runtime."
    - "Participants can continue through the core flow without getting stuck in permanent reconnect state."
  artifacts:
    - path: "src/hooks/useSessionSync.ts"
      provides: "Realtime sync with explicit fallback polling path"
      contains: "fallback"
    - path: "tests/e2e/session-sync-fallback.spec.ts"
      provides: "Automated browser proof that flow still updates when EventSource is unavailable"
      min_lines: 50
  key_links:
    - from: "src/hooks/useSessionSync.ts"
      to: "src/lib/api/session-client.ts"
      via: "fallback polling through getSessionEvents/getSessionSnapshot"
      pattern: "getSessionEvents|getSessionSnapshot"
    - from: "tests/e2e/session-sync-fallback.spec.ts"
      to: "src/hooks/useSessionSync.ts"
      via: "browser-level verification of fallback behavior"
      pattern: "EventSource"
---

<objective>
Harden session synchronization for browser-compatibility edge cases that can block the funnel.

Purpose: Reduce PLAT-01 launch risk by preventing SSE-specific failures from breaking session progression.
Output: Fallback-capable sync hook and e2e regression covering EventSource-unavailable behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md
@src/hooks/useSessionSync.ts
@src/lib/api/session-client.ts
@tests/e2e/ranking-results.spec.ts

<interfaces>
From src/hooks/useSessionSync.ts:
```ts
export type SyncState = "syncing" | "live" | "reconnecting";
export function useSessionSync(sessionId: string | null)
```

From src/lib/api/session-client.ts:
```ts
export async function getSessionSnapshot(sessionId: string): Promise<SessionSnapshotResponse>
export async function getSessionEvents(sessionId: string, since?: string): Promise<SessionEventResponse[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EventSource-unavailable fallback polling path in session sync hook</name>
  <read_first>src/hooks/useSessionSync.ts, src/lib/api/session-client.ts, .planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md</read_first>
  <files>src/hooks/useSessionSync.ts</files>
  <action>Refactor `useSessionSync` to support two runtime modes: (a) existing SSE mode when `window.EventSource` exists and stream opens successfully, and (b) fallback polling mode when EventSource is unavailable or fails repeatedly. In fallback mode, call `getSessionEvents` on a fixed interval (1s) and periodically re-hydrate via `getSessionSnapshot` after consecutive poll errors. Keep merge semantics identical to current logic so shortlist/confirmedPlace updates continue working. Ensure `syncState` transitions to `live` in fallback mode after first successful poll to avoid permanent `reconnecting` UI state.</action>
  <acceptance_criteria>- `src/hooks/useSessionSync.ts` contains explicit EventSource availability check (`typeof window.EventSource`).
- `src/hooks/useSessionSync.ts` contains a fallback polling interval setup (`setInterval` or equivalent) when EventSource is unavailable.
- `src/hooks/useSessionSync.ts` keeps `mergeEvent` updates for `shortlist` and `confirmedPlace` in both sync modes.
- `src/hooks/useSessionSync.ts` sets `syncState` to `live` from fallback mode on successful poll.</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Session sync no longer depends solely on SSE support and can maintain live state via polling fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add e2e regression for fallback sync behavior</name>
  <read_first>tests/e2e/ranking-results.spec.ts, src/app/s/[token]/page.tsx, src/hooks/useSessionSync.ts</read_first>
  <files>tests/e2e/session-sync-fallback.spec.ts</files>
  <behavior>
    - Test 1: with EventSource disabled in browser context, session page still loads snapshot and reaches "Inputs ready for ranking.".
    - Test 2: polling route updates make shortlist/decision status visible without SSE stream.
  </behavior>
  <action>Create `tests/e2e/session-sync-fallback.spec.ts` that disables EventSource using `page.addInitScript(() => { window.EventSource = undefined as any; })`, stubs `/v1/sessions/:id` and `/v1/sessions/:id/events` responses, then verifies the participant can continue through ranking and see shortlist/confirmation UI signals without stream transport. Reuse current fixture pattern from `ranking-results.spec.ts` and `decision-confirmation.spec.ts` for deterministic payloads.</action>
  <acceptance_criteria>- `tests/e2e/session-sync-fallback.spec.ts` contains `window.EventSource = undefined`.
- Spec asserts at least one post-sync UI indicator (`Inputs ready for ranking.` or shortlist/confirmation controls) is visible.
- Spec includes event polling route stubs for `/v1/sessions/*/events`.</acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/session-sync-fallback.spec.ts --project=chromium</automated>
  </verify>
  <done>Automated browser regression proves core flow remains usable when SSE is unavailable.</done>
</task>

</tasks>

<verification>
Type-check fallback hook changes and execute targeted fallback e2e regression under Chromium.
</verification>

<success_criteria>
- PLAT-01 blocker class "SSE unavailable / unstable" is mitigated with fallback behavior.
- Browser flow no longer stalls solely because EventSource transport is missing.
- Regression test catches future fallback breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness-stabilization/04-02-SUMMARY.md`
</output>
