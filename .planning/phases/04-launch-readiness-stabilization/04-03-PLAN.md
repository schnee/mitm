---
phase: 04-launch-readiness-stabilization
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - services/api/src/routes/analytics/getSessionFunnel.ts
  - services/api/src/dev-server.ts
  - services/api/tests/funnel-events.test.ts
  - playwright.config.ts
  - tests/e2e/funnel-compatibility.spec.ts
  - package.json
autonomous: true
requirements: [PLAT-01, PLAT-02]
must_haves:
  truths:
    - "Launch-readiness funnel test runs on modern desktop and mobile browser profiles from one command."
    - "Telemetry for required funnel events is queryable per session during staged verification."
    - "Cross-browser staged run can confirm both completion reliability and event completeness before rollout."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Cross-browser project matrix for launch funnel tests"
      contains: "Desktop Chrome"
    - path: "tests/e2e/funnel-compatibility.spec.ts"
      provides: "End-to-end compatibility regression over complete decision funnel"
      min_lines: 90
    - path: "services/api/src/routes/analytics/getSessionFunnel.ts"
      provides: "Read endpoint for per-session funnel event visibility"
      exports: ["getSessionFunnelHandler"]
  key_links:
    - from: "services/api/src/dev-server.ts"
      to: "services/api/src/routes/analytics/getSessionFunnel.ts"
      via: "GET /v1/analytics/funnel/:sessionId route"
      pattern: "\/v1\/analytics\/funnel\/"
    - from: "playwright.config.ts"
      to: "tests/e2e/funnel-compatibility.spec.ts"
      via: "project matrix execution"
      pattern: "projects"
    - from: "tests/e2e/funnel-compatibility.spec.ts"
      to: "services/api/src/routes/analytics/getSessionFunnel.ts"
      via: "verifies telemetry completeness response"
      pattern: "analytics\/funnel"
---

<objective>
Add launch verification harness that proves cross-browser funnel completion and telemetry completeness.

Purpose: Close PLAT-01/PLAT-02 with executable staged checks before broad rollout.
Output: Analytics read endpoint, cross-browser Playwright matrix config, and full-funnel compatibility regression command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md
@services/api/src/dev-server.ts
@services/api/src/modules/session/repository.ts
@tests/e2e/decision-confirmation.spec.ts
@tests/e2e/ranking-results.spec.ts

<interfaces>
From services/api/src/modules/session/repository.ts:
```ts
recordFunnelEvent(sessionId: string, event: FunnelEventName, metadata?: Record<string, unknown>): void
listFunnelEvents(sessionId: string): FunnelEventRecord[]
```

From tests/e2e existing specs:
```ts
test("...", async ({ page }) => { ... })
await page.route("**/v1/...", async (route) => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose per-session funnel telemetry endpoint for staged verification</name>
  <read_first>services/api/src/dev-server.ts, services/api/src/modules/session/repository.ts, services/api/tests/session-create-join.test.ts</read_first>
  <files>services/api/src/routes/analytics/getSessionFunnel.ts, services/api/src/dev-server.ts, services/api/tests/funnel-events.test.ts</files>
  <behavior>
    - Test 1: GET `/v1/analytics/funnel/:sessionId` returns ordered event records for existing session.
    - Test 2: response includes completeness object with all four required event names and boolean status.
    - Test 3: unknown session returns 404 `SESSION_NOT_FOUND`.
  </behavior>
  <action>Create `services/api/src/routes/analytics/getSessionFunnel.ts` with `getSessionFunnelHandler(sessionId, { repository })` returning `{ sessionId, events, completeness }`, where `completeness` has exact keys `session_start`, `inputs_set`, `results_returned`, `decision_confirmed`. Wire route in `dev-server.ts` for `GET /v1/analytics/funnel/:sessionId`. Extend `services/api/tests/funnel-events.test.ts` with route-level assertions for 200/404 and completeness shape to support staged launch checks.</action>
  <acceptance_criteria>- `services/api/src/routes/analytics/getSessionFunnel.ts` exports `getSessionFunnelHandler`.
- `services/api/src/dev-server.ts` contains `/v1/analytics/funnel/` route matching logic.
- `services/api/tests/funnel-events.test.ts` asserts completeness keys for all four funnel events.
- 404 assertion uses `SESSION_NOT_FOUND` for unknown session.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/funnel-events.test.ts -t "analytics funnel route"</automated>
  </verify>
  <done>Session-level telemetry can be queried and checked for funnel completeness before rollout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure cross-browser launch matrix and full-funnel compatibility spec</name>
  <read_first>tests/e2e/decision-confirmation.spec.ts, tests/e2e/ranking-results.spec.ts, package.json, .planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md</read_first>
  <files>playwright.config.ts, tests/e2e/funnel-compatibility.spec.ts, package.json</files>
  <behavior>
    - Test 1: funnel compatibility spec passes on `Desktop Chrome` and `Desktop Safari` projects.
    - Test 2: funnel compatibility spec passes on `Pixel 5` and `iPhone 12` projects.
    - Test 3: spec validates required funnel event completeness via analytics endpoint after decision confirmation.
  </behavior>
  <action>Create `playwright.config.ts` with projects named exactly `Desktop Chrome`, `Desktop Safari`, `Pixel 5`, and `iPhone 12` using Playwright `devices` presets validated in research. Add `tests/e2e/funnel-compatibility.spec.ts` that executes create/join -> location confirm -> ranking -> shortlist -> confirm flow using route fixtures and then calls `/v1/analytics/funnel/:sessionId` (or stubbed equivalent) to assert all four completeness flags are true. Add script `test:e2e:launch` to `package.json` that runs this spec across all four projects (`npx playwright test tests/e2e/funnel-compatibility.spec.ts --project="Desktop Chrome" --project="Desktop Safari" --project="Pixel 5" --project="iPhone 12"`).</action>
  <acceptance_criteria>- `playwright.config.ts` defines four projects named `Desktop Chrome`, `Desktop Safari`, `Pixel 5`, and `iPhone 12`.
- `tests/e2e/funnel-compatibility.spec.ts` contains assertions for decision completion UI and funnel completeness keys.
- `package.json` contains script key `test:e2e:launch` with all four `--project` flags.
- Spec references analytics endpoint path `/v1/analytics/funnel/`.</acceptance_criteria>
  <verify>
    <automated>npm run test:e2e:launch</automated>
  </verify>
  <done>One command verifies cross-browser full-funnel reliability and required telemetry completeness for launch gating.</done>
</task>

</tasks>

<verification>
Run telemetry route tests and full cross-browser launch suite command to validate compatibility and event completeness.
</verification>

<success_criteria>
- PLAT-01: full flow is exercised on modern desktop and mobile browser profiles with automated pass/fail output.
- PLAT-02: telemetry completeness for all required event names is queryable and asserted in automated tests.
- Launch stage has a repeatable command to verify top blockers are resolved before rollout.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness-stabilization/04-03-SUMMARY.md`
</output>
