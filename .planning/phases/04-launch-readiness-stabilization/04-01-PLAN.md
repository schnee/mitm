---
phase: 04-launch-readiness-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/modules/location/repository.ts
  - services/api/src/modules/ranking/service.ts
  - services/api/tests/funnel-events.test.ts
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "Every session records `session_start` when a host creates a session."
    - "When both participant locations are confirmed, the session records `inputs_set` once."
    - "A successful ranking run records `results_returned`, and a successful final confirmation records `decision_confirmed`."
  artifacts:
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Funnel event recording, dedupe, and retrieval for session lifecycle analytics"
      contains: "recordFunnelEvent"
    - path: "services/api/src/modules/session/contracts.ts"
      provides: "Typed funnel event contract names and payload structure"
      contains: "FunnelEventName"
    - path: "services/api/tests/funnel-events.test.ts"
      provides: "Automated proof that required funnel events are captured and deduped"
      min_lines: 60
  key_links:
    - from: "services/api/src/modules/location/repository.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "inputs_set emission when both locations are confirmed"
      pattern: "recordFunnelEvent\(.*inputs_set"
    - from: "services/api/src/modules/ranking/service.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "results_returned emission after successful ranking"
      pattern: "recordFunnelEvent\(.*results_returned"
    - from: "services/api/src/modules/session/repository.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "decision_confirmed emitted inside confirm flow"
      pattern: "decision_confirmed"
---

<objective>
Capture the four required funnel events as backend source-of-truth lifecycle telemetry.

Purpose: Satisfy PLAT-02 with deterministic server-side analytics events that do not depend on client delivery.
Output: Typed funnel event contracts, repository-level recording/deduplication, and API tests proving complete funnel capture.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md
@services/api/src/modules/session/contracts.ts
@services/api/src/modules/session/repository.ts
@services/api/src/modules/location/repository.ts
@services/api/src/modules/ranking/service.ts

<interfaces>
From services/api/src/modules/session/contracts.ts:
```ts
export interface SessionSnapshot {
  sessionId: string;
  status: SessionStatus;
  updatedAt: string;
  participants: SessionSnapshotParticipant[];
  inputsReady: boolean;
  shortlist: ShortlistVenue[];
  confirmedPlace: ConfirmedPlace | null;
}
```

From services/api/src/modules/location/repository.ts:
```ts
confirmLocation(input: { sessionId: string; participantId: string }): { confirmedAt: string; inputsReady: boolean }
```

From services/api/src/modules/ranking/service.ts:
```ts
async generateRankedResults(sessionId: string): Promise<{ sessionId: string; generatedAt: string; results: RankedVenue[] }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed funnel telemetry contracts and repository recording API</name>
  <read_first>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, .planning/phases/04-launch-readiness-stabilization/04-RESEARCH.md</read_first>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts</files>
  <behavior>
    - Test 1: `recordFunnelEvent` stores `session_start` for an existing session and returns events in chronological order.
    - Test 2: duplicate milestone event calls for the same session/event (for `inputs_set` and `decision_confirmed`) result in one stored event.
    - Test 3: querying funnel events for a missing session throws `SESSION_NOT_FOUND`.
  </behavior>
  <action>In `contracts.ts`, add `FunnelEventName` union with exactly `session_start | inputs_set | results_returned | decision_confirmed` and `FunnelEventRecord` interface (`sessionId`, `event`, `occurredAt`, optional metadata map). In `repository.ts`, add `funnelEventsBySessionId` storage, `recordFunnelEvent(sessionId, event, metadata?)`, and `listFunnelEvents(sessionId)` methods. Enforce dedupe for milestone events (`inputs_set`, `decision_confirmed`) to prevent retry double-counting. When `createSession` succeeds, record `session_start` automatically with participant role metadata.</action>
  <acceptance_criteria>- `services/api/src/modules/session/contracts.ts` contains `export type FunnelEventName = "session_start" | "inputs_set" | "results_returned" | "decision_confirmed"`.
- `services/api/src/modules/session/repository.ts` contains `recordFunnelEvent(`.
- `services/api/src/modules/session/repository.ts` contains `listFunnelEvents(`.
- `services/api/src/modules/session/repository.ts` contains `funnelEventsBySessionId` map initialization in constructor/create path.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/funnel-events.test.ts -t "repository funnel contracts"</automated>
  </verify>
  <done>Session repository can record and return required funnel events with deterministic dedupe behavior for milestone events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit inputs/results/decision funnel events from lifecycle transitions</name>
  <read_first>services/api/src/modules/location/repository.ts, services/api/src/modules/ranking/service.ts, services/api/src/modules/session/repository.ts, services/api/tests/ranking-results.test.ts</read_first>
  <files>services/api/src/modules/location/repository.ts, services/api/src/modules/ranking/service.ts, services/api/src/modules/session/repository.ts, services/api/tests/funnel-events.test.ts</files>
  <behavior>
    - Test 1: when second participant confirms location, `inputs_set` exists exactly once for that session.
    - Test 2: successful ranking generation appends `results_returned`.
    - Test 3: successful venue confirmation appends `decision_confirmed` and retries do not duplicate it.
  </behavior>
  <action>In `LocationRepository.confirmLocation`, after existing confirmation logic, call `recordFunnelEvent(sessionId, "inputs_set")` only when `inputsReady` transitions to true. In `RankingService.generateRankedResults`, after successful result generation and status update, call `recordFunnelEvent(sessionId, "results_returned", { resultCount })`. In `SessionRepository.confirmVenue`, after successful confirmation persistence, call `recordFunnelEvent(sessionId, "decision_confirmed", { venueId })`. Create/extend `services/api/tests/funnel-events.test.ts` to prove full required-event lifecycle (`session_start`, `inputs_set`, `results_returned`, `decision_confirmed`) and dedupe semantics required by PLAT-02.</action>
  <acceptance_criteria>- `services/api/src/modules/location/repository.ts` contains `recordFunnelEvent` with `"inputs_set"`.
- `services/api/src/modules/ranking/service.ts` contains `recordFunnelEvent` with `"results_returned"`.
- `services/api/src/modules/session/repository.ts` contains `recordFunnelEvent` with `"decision_confirmed"`.
- `services/api/tests/funnel-events.test.ts` contains all four event names in assertions.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/funnel-events.test.ts</automated>
  </verify>
  <done>All required funnel events are emitted from server lifecycle transitions and validated by automated tests.</done>
</task>

</tasks>

<verification>
Run lifecycle telemetry tests to confirm all four required events are recorded from server-side transitions and duplicate milestone counts are prevented.
</verification>

<success_criteria>
- PLAT-02 telemetry events exist with exact names: `session_start`, `inputs_set`, `results_returned`, `decision_confirmed`.
- Event recording is server-authoritative and not dependent on frontend analytics dispatch.
- Automated tests verify complete funnel event capture for a representative session lifecycle.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness-stabilization/04-01-SUMMARY.md`
</output>
