---
phase: 02-fair-ranking-recommendation-core
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/api/session-client.ts
  - src/components/ranking/RankingInputsForm.tsx
  - src/components/ranking/RankedResultsList.tsx
  - src/app/s/[token]/page.tsx
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements: [FAIR-01, PREF-01, PREF-02, RANK-03]
must_haves:
  truths:
    - "Participants can choose split and tags in the session UI before running ranking."
    - "Participants can trigger ranking and receive a list that includes ETA per participant, category, and open status."
    - "Changing split/tags and rerunning ranking updates the visible order."
  artifacts:
    - path: "src/components/ranking/RankingInputsForm.tsx"
      provides: "UI controls for split + tag inputs"
      min_lines: 50
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Metadata-rich ranked result rendering"
      min_lines: 50
    - path: "src/lib/api/session-client.ts"
      provides: "Typed ranking input/result client calls"
      contains: "getRankedResults"
  key_links:
    - from: "src/components/ranking/RankingInputsForm.tsx"
      to: "src/lib/api/session-client.ts"
      via: "submit split + tags"
      pattern: "upsertRankingInputs"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "render ranking response"
      pattern: "<RankedResultsList"
---

<objective>
Wire Phase 2 backend capabilities into the join/session UI so users can submit ranking inputs and view metadata-complete ranked results.

Purpose: Complete user-visible value path and satisfy RANK-03 display requirements.
Output: Typed client calls, ranking input/result components, session page integration, and e2e skeleton coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fair-ranking-recommendation-core/02-RESEARCH.md
@.planning/phases/02-fair-ranking-recommendation-core/02-02-SUMMARY.md

<interfaces>
From src/lib/api/session-client.ts:
```ts
export async function confirmLocation(input: { sessionId: string; participantId: string }): Promise<{ confirmedAt: string; inputsReady: boolean }>;
```

From services/api/src/routes/ranking/getRankedResults.ts (created in 02-02):
```ts
POST /v1/ranking/results -> { sessionId, generatedAt, results: RankedVenue[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed client with ranking inputs/results APIs</name>
  <read_first>src/lib/api/session-client.ts</read_first>
  <files>src/lib/api/session-client.ts</files>
  <action>Add exported client helpers `upsertRankingInputs(input)` and `getRankedResults(sessionId)` targeting `/v1/ranking/inputs` and `/v1/ranking/results`. Add explicit TS interfaces for `WillingnessSplit`, `PreferenceTag`, and `RankedVenue` including required metadata fields: `etaParticipantA`, `etaParticipantB`, `category`, `openNow`.</action>
  <acceptance_criteria>- `src/lib/api/session-client.ts` exports `upsertRankingInputs`.
- `src/lib/api/session-client.ts` exports `getRankedResults`.
- `RankedVenue` interface includes `etaParticipantA`, `etaParticipantB`, `category`, and `openNow`.</acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Frontend has typed client contracts for posting ranking inputs and requesting ranked results.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ranking input and results UI on session page</name>
  <read_first>src/app/s/[token]/page.tsx, src/components/location/LocationConfirmCard.tsx, src/components/session/ParticipantStatus.tsx</read_first>
  <files>src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx, tests/e2e/ranking-results.spec.ts</files>
  <action>Create `RankingInputsForm.tsx` with fixed split options (`50/50`, `60/40`, `70/30`) and curated tag multi-select (`coffee`, `cocktails`, `vintage shops`, `dessert`, `quiet`). Submit via `upsertRankingInputs` and show deterministic success/error states. Create `RankedResultsList.tsx` that renders each candidate with name, category, `openNow`, and both ETA values. Update `src/app/s/[token]/page.tsx` to show ranking controls only after `inputsReady` is true, trigger `getRankedResults`, and render ordered list. Add Playwright skeleton `tests/e2e/ranking-results.spec.ts` asserting metadata fields are visible after mocked ranking response.</action>
  <acceptance_criteria>- `src/components/ranking/RankingInputsForm.tsx` contains labels `50/50`, `60/40`, and `70/30`.
- `src/components/ranking/RankedResultsList.tsx` renders text labels `ETA (You)` and `ETA (Partner)`.
- `src/app/s/[token]/page.tsx` includes `<RankingInputsForm` and `<RankedResultsList`.
- `tests/e2e/ranking-results.spec.ts` contains test name `shows ETA, category, and open status`.</acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Session UI supports split/tag input and displays ranked results with all required metadata.</done>
</task>

</tasks>

<verification>
Compile TypeScript to verify UI integration contracts, then run e2e test when app server/browser runtime are available.
</verification>

<success_criteria>
- RANK-03 satisfied with ETA/category/open-status rendering per result card.
- Users can modify split/tags and request updated ranking from the same session screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-ranking-recommendation-core/02-03-SUMMARY.md`
</output>
