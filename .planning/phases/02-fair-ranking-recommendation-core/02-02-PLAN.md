---
phase: 02-fair-ranking-recommendation-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - services/api/src/modules/ranking/provider/googleMapsAdapter.ts
  - services/api/src/modules/ranking/scoring.ts
  - services/api/src/modules/ranking/service.ts
  - services/api/src/routes/ranking/getRankedResults.ts
  - services/api/src/dev-server.ts
  - services/api/tests/ranking-results.test.ts
  - .env.example
autonomous: true
requirements: [PREF-02, RANK-01, RANK-02]
user_setup:
  - service: google-maps-platform
    why: "Single provider for candidate search and travel-time matrix"
    env_vars:
      - name: GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials"
      - name: GOOGLE_MAPS_PLACES_RADIUS_METERS
        source: "Application env var (set per environment, e.g., 2500)"
must_haves:
  truths:
    - "System retrieves candidate venues between both participant locations using one configured provider."
    - "Results are ordered by travel-time fairness combined with preference relevance."
    - "Changing tags can change candidate order for the same location pair."
  artifacts:
    - path: "services/api/src/modules/ranking/provider/googleMapsAdapter.ts"
      provides: "Single-provider candidate + travel-time retrieval"
      exports: ["GoogleMapsAdapter"]
    - path: "services/api/src/modules/ranking/scoring.ts"
      provides: "Deterministic fairness + preference scoring"
      exports: ["scoreCandidates"]
    - path: "services/api/src/routes/ranking/getRankedResults.ts"
      provides: "POST ranking endpoint"
      exports: ["getRankedResultsHandler"]
  key_links:
    - from: "services/api/src/routes/ranking/getRankedResults.ts"
      to: "services/api/src/modules/ranking/service.ts"
      via: "ranking orchestration call"
      pattern: "service\.generateRankedResults"
    - from: "services/api/src/modules/ranking/service.ts"
      to: "services/api/src/modules/ranking/provider/googleMapsAdapter.ts"
      via: "provider calls for candidates + durations"
      pattern: "adapter\.(searchCandidates|getTravelDurations)"
---

<objective>
Implement provider-backed ranking generation that combines ETA fairness and preference relevance into deterministic ordering.

Purpose: Deliver the core value path for PREF-02, RANK-01, and RANK-02.
Output: Google Maps adapter, scoring/service modules, ranking route, tests, and env contract.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fair-ranking-recommendation-core/02-RESEARCH.md
@.planning/phases/02-fair-ranking-recommendation-core/02-01-SUMMARY.md

<interfaces>
From services/api/src/modules/ranking/contracts.ts (created in 02-01):
```ts
export interface RankingInputState { split: WillingnessSplit; tags: PreferenceTag[]; updatedAt: string; }
```

From services/api/src/modules/session/contracts.ts:
```ts
export interface ParticipantRecord { participantId: string; role: "host" | "invitee"; lat?: number; lng?: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build provider adapter + fairness scoring with deterministic fixture tests</name>
  <read_first>services/api/src/modules/session/repository.ts, .planning/phases/02-fair-ranking-recommendation-core/02-RESEARCH.md</read_first>
  <files>services/api/src/modules/ranking/provider/googleMapsAdapter.ts, services/api/src/modules/ranking/scoring.ts, services/api/tests/ranking-results.test.ts, .env.example</files>
  <behavior>
    - Test 1: scoring prefers lower ETA burden aligned to selected split.
    - Test 2: equal-ETA candidates reorder when selected tags match candidate categories.
    - Test 3: tie-break remains deterministic (stable order by venueId).
  </behavior>
  <action>Implement `GoogleMapsAdapter` using `@googlemaps/google-maps-services-js` to provide `searchCandidates(center, tags, radiusMeters)` and `getTravelDurations(origins, destinations)`. In `scoring.ts`, implement weighted score formula using exact weights: fairness 0.7, preference 0.3 for `50_50`; fairness 0.8, preference 0.2 for `60_40` and `70_30` (per MVP bias toward fairness). Add deterministic tie-break by `venueId` when scores match. Add env keys in `.env.example`: `GOOGLE_MAPS_API_KEY=`, `GOOGLE_MAPS_PLACES_RADIUS_METERS=2500`.</action>
  <acceptance_criteria>- `.env.example` contains `GOOGLE_MAPS_API_KEY=`.
- `services/api/src/modules/ranking/scoring.ts` contains `fairnessWeight` and `preferenceWeight` constants.
- `services/api/tests/ranking-results.test.ts` includes case text `reorder when tags change`.</acceptance_criteria>
  <verify>
    <automated>pnpm test services/api/tests/ranking-results.test.ts -x</automated>
  </verify>
  <done>Adapter + scoring logic exist with deterministic behavior validated by automated fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /v1/ranking/results orchestration route</name>
  <read_first>services/api/src/dev-server.ts, services/api/src/routes/sessions/getSessionSnapshot.ts, services/api/src/modules/ranking/repository.ts</read_first>
  <files>services/api/src/modules/ranking/service.ts, services/api/src/routes/ranking/getRankedResults.ts, services/api/src/dev-server.ts, services/api/tests/ranking-results.test.ts</files>
  <behavior>
    - Test 1: endpoint blocks with 409 when both locations are not confirmed.
    - Test 2: endpoint returns 200 with ranked candidates when inputs + locations are ready.
    - Test 3: result ordering changes when split or tag set changes against same candidate fixture.
  </behavior>
  <action>Implement `generateRankedResults(sessionId)` in `service.ts`: load participant confirmed locations + ranking inputs from repositories, call adapter for candidates/durations, run scoring, and return top 10. Implement `getRankedResultsHandler` with payload `{ sessionId }`, returning 409 `INPUTS_NOT_READY` when readiness preconditions fail and 200 `{ sessionId, generatedAt, results }` otherwise. Wire `POST /v1/ranking/results` in `dev-server.ts` and keep route-level validation + error mapping explicit.</action>
  <acceptance_criteria>- `services/api/src/routes/ranking/getRankedResults.ts` contains `INPUTS_NOT_READY` error mapping.
- `services/api/src/dev-server.ts` contains path literal `/v1/ranking/results`.
- `services/api/src/modules/ranking/service.ts` exports `generateRankedResults`.</acceptance_criteria>
  <verify>
    <automated>pnpm test services/api/tests/ranking-results.test.ts -x</automated>
  </verify>
  <done>Ranking endpoint reliably returns provider-backed sorted candidates or deterministic readiness errors.</done>
</task>

</tasks>

<verification>
Run ranking-results tests to confirm readiness gating, provider/scoring integration, and deterministic ordering.
</verification>

<success_criteria>
- RANK-01 satisfied with single-provider candidate retrieval.
- RANK-02 satisfied with ETA fairness + preference scoring.
- PREF-02 satisfied by observable reordering when preference tags change.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-ranking-recommendation-core/02-02-SUMMARY.md`
</output>
