---
phase: 02-fair-ranking-recommendation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/ranking/contracts.ts
  - services/api/src/modules/ranking/validation.ts
  - services/api/src/modules/ranking/repository.ts
  - services/api/src/routes/ranking/upsertRankingInputs.ts
  - services/api/src/dev-server.ts
  - services/api/tests/ranking-inputs.test.ts
autonomous: true
requirements: [FAIR-01, PREF-01]
must_haves:
  truths:
    - "Each participant can set one willingness split option (50/50, 60/40, 70/30)."
    - "Participants can set preference tags before ranking runs."
    - "Invalid split/tag inputs are blocked with deterministic 4xx responses."
  artifacts:
    - path: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      provides: "Validated ranking-input API handler"
      exports: ["upsertRankingInputsHandler"]
    - path: "services/api/src/modules/ranking/contracts.ts"
      provides: "Typed split/tag contracts shared by API and scoring"
      contains: "WillingnessSplit"
    - path: "services/api/tests/ranking-inputs.test.ts"
      provides: "Automated contract tests for split/tag persistence"
      min_lines: 40
  key_links:
    - from: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      to: "services/api/src/modules/ranking/repository.ts"
      via: "validated upsert"
      pattern: "repository\.upsertRankingInputs"
    - from: "services/api/src/dev-server.ts"
      to: "services/api/src/routes/ranking/upsertRankingInputs.ts"
      via: "POST /v1/ranking/inputs route binding"
      pattern: "\/v1\/ranking\/inputs"
---

<objective>
Create the server-side input contract for willingness split and preference tags so ranking has deterministic, validated inputs.

Purpose: Implement FAIR-01 and PREF-01 as authoritative API-backed session inputs, not client-only state.
Output: New ranking input contracts, repository layer, route handler, and passing API tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fair-ranking-recommendation-core/02-RESEARCH.md

<interfaces>
From services/api/src/modules/session/contracts.ts:
```ts
export type SessionStatus = "created" | "joined" | "locating" | "ranked" | "confirmed" | "expired";
export interface SessionSnapshot { sessionId: string; status: SessionStatus; participants: SessionSnapshotParticipant[]; inputsReady: boolean; }
```

From services/api/src/modules/location/repository.ts:
```ts
confirmLocation(input: { sessionId: string; participantId: string }): { confirmedAt: string; inputsReady: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ranking-input contracts and RED tests</name>
  <read_first>services/api/src/modules/session/contracts.ts, services/api/tests/location-flow.test.ts</read_first>
  <files>services/api/src/modules/ranking/contracts.ts, services/api/src/modules/ranking/validation.ts, services/api/tests/ranking-inputs.test.ts</files>
  <behavior>
    - Test 1: split accepts only `50_50`, `60_40`, `70_30`.
    - Test 2: tags accept curated values only (`coffee`, `cocktails`, `vintage_shops`, `dessert`, `quiet`).
    - Test 3: invalid split/tag payload returns 400 with `INVALID_RANKING_INPUTS`.
  </behavior>
  <action>Create `contracts.ts` with `WillingnessSplit`, `PreferenceTag`, and `RankingInputState` types. Create zod schema in `validation.ts` enforcing the exact split enum and max 5 unique tags from curated list. Write failing tests in `ranking-inputs.test.ts` that call the upcoming handler with valid and invalid payloads per D-FAIR-01 / D-PREF-01 requirements from ROADMAP.</action>
  <acceptance_criteria>- `services/api/src/modules/ranking/contracts.ts` contains `export type WillingnessSplit = "50_50" | "60_40" | "70_30"`.
- `services/api/src/modules/ranking/validation.ts` contains `z.enum(["50_50","60_40","70_30"])`.
- `services/api/tests/ranking-inputs.test.ts` contains test case name `INVALID_RANKING_INPUTS`.</acceptance_criteria>
  <verify>
    <automated>pnpm test services/api/tests/ranking-inputs.test.ts -x</automated>
  </verify>
  <done>Typed contracts and tests prove only allowed willingness/tag inputs are accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ranking-input persistence and POST route wiring</name>
  <read_first>services/api/src/dev-server.ts, services/api/src/routes/location/upsertLocationDraft.ts, services/api/src/modules/session/repository.ts</read_first>
  <files>services/api/src/modules/ranking/repository.ts, services/api/src/routes/ranking/upsertRankingInputs.ts, services/api/src/dev-server.ts, services/api/tests/ranking-inputs.test.ts</files>
  <behavior>
    - Test 1: `POST /v1/ranking/inputs` returns 200 and echoes persisted split/tags for valid session participant.
    - Test 2: unknown session or participant returns 404 `PARTICIPANT_NOT_FOUND`.
    - Test 3: invalid payload remains 400 `INVALID_RANKING_INPUTS`.
  </behavior>
  <action>Implement `RankingRepository` backed by `SessionRepository` storage to persist `{ split, tags, updatedAt }` per participant per session. Add `upsertRankingInputsHandler` that validates payload, calls repository, and maps domain errors to 404. Wire new route in `dev-server.ts` at `POST /v1/ranking/inputs` (same JSON/CORS pattern as existing routes). Keep ranking-input state session-scoped and ephemeral (no account-level persistence).</action>
  <acceptance_criteria>- `services/api/src/routes/ranking/upsertRankingInputs.ts` exports `upsertRankingInputsHandler`.
- `services/api/src/dev-server.ts` contains path literal `/v1/ranking/inputs`.
- Running the ranking-inputs test file exits with status 0.</acceptance_criteria>
  <verify>
    <automated>pnpm test services/api/tests/ranking-inputs.test.ts -x</automated>
  </verify>
  <done>Participants can store willingness split and preference tags through a validated API endpoint.</done>
</task>

</tasks>

<verification>
Run ranking input tests to confirm split/tag validation and persistence behavior for valid + invalid payloads.
</verification>

<success_criteria>
- FAIR-01 satisfied with enforced split options.
- PREF-01 satisfied with persisted participant tags.
- Endpoint behavior deterministic across success and 4xx paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fair-ranking-recommendation-core/02-01-SUMMARY.md`
</output>
