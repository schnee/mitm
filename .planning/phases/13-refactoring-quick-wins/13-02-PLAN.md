---
phase: 13-refactoring-quick-wins
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - services/api/src/lib/error-codes.ts
  - services/api/src/routes/sessions/createSession.ts
  - services/api/src/routes/sessions/joinSession.ts
  - services/api/src/routes/location/upsertLocationDraft.ts
  - services/api/src/routes/location/confirmLocation.ts
  - services/api/src/routes/ranking/upsertRankingInputs.ts
  - services/api/src/routes/decision/upsertShortlistVenue.ts
  - services/api/src/routes/decision/confirmVenue.ts
autonomous: true
requirements:
  - REFACTOR-02

must_haves:
  truths:
    - "Error codes are centralized in a single location"
    - "Frontend and backend use the same error code constants"
    - "New error codes can be added in one place"
  artifacts:
    - path: "services/api/src/lib/error-codes.ts"
      provides: "Shared error code constants"
      min_lines: 30
  key_links:
    - from: "services/api/src/routes/sessions/createSession.ts"
      to: "services/api/src/lib/error-codes.ts"
      via: "import"
      pattern: "import.*error-codes"
---

<objective>
Create shared error codes constants that can be used by both frontend and backend to ensure consistent error handling.

Purpose: Centralize error code definitions for consistency and easier maintenance.
Output: services/api/src/lib/error-codes.ts with shared error code constants.
</objective>

<context>
@.planning/PROJECT.md
@src/lib/api/session-client.ts

# Backend routes that return errors
@services/api/src/routes/sessions/createSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error-codes.ts in services/api/src/lib/</name>
  <files>services/api/src/lib/error-codes.ts</files>
  <action>
Create services/api/src/lib/error-codes.ts with shared error code constants:

Define error code categories:
- SESSION errors (create, join, not found, expired)
- LOCATION errors (permission denied, timeout, invalid coordinates, address not found)
- RANKING errors (inputs not ready, generation failed, provider error)
- DECISION errors (shortlist full, already shortlisted, confirmation failed)
- VALIDATION errors (invalid split, invalid tags, missing required fields)
- SERVER errors (internal error, database error, external service error)

For each error code, provide:
- code: string (e.g., "SESSION_NOT_FOUND")
- message: string (human-readable)
- httpStatus: number (4xx or 5xx)

Export as a typed object or enum for TypeScript safety.
  </action>
  <verify>
    <automated>tsc --noEmit services/api/src/lib/error-codes.ts</automated>
  </verify>
  <done>services/api/src/lib/error-codes.ts exists with 10+ error codes</done>
</task>

<task type="auto">
  <name>Task 2: Update backend routes to use shared error codes</name>
  <files>
  - services/api/src/routes/sessions/createSession.ts
  - services/api/src/routes/sessions/joinSession.ts
  - services/api/src/routes/location/upsertLocationDraft.ts
  - services/api/src/routes/location/confirmLocation.ts
  </files>
  <action>
Update the following backend routes to import and use error codes from services/api/src/lib/error-codes.ts:
- createSession.ts: Use SESSION_CREATE_FAILED for internal errors
- joinSession.ts: Use SESSION_NOT_FOUND, SESSION_EXPIRED for not found/expired cases
- upsertLocationDraft.ts: Use LOCATION_INVALID_COORDINATES, LOCATION_ADDRESS_NOT_FOUND
- confirmLocation.ts: Use LOCATION_NOT_CONFIRMED

Replace hardcoded error messages with error code references where appropriate.
  </action>
  <verify>
    <automated>tsc --noEmit services/api/src/</automated>
  </verify>
  <done>Backend routes import from error-codes.ts</done>
</task>

</tasks>

<verification>
- [ ] Error codes centralized in single file
- [ ] Backend routes use shared error codes
- [ ] No hardcoded error strings in routes
</verification>

<success_criteria>
- services/api/src/lib/error-codes.ts exists with categorized error codes
- Backend routes import and use error codes
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactoring-quick-wins/13-02-SUMMARY.md`
</output>