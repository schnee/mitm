---
phase: 13-refactoring-quick-wins
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/hooks/useMapsConfig.ts
  - src/lib/env.ts
  - .env.example
autonomous: true
requirements:
  - REFACTOR-03
  - REFACTOR-04

must_haves:
  truths:
    - "Maps configuration is accessible via a reusable hook"
    - "SESSION_TTL_HOURS environment variable is documented and used"
    - "Maps API key and configuration are available from a single source"
  artifacts:
    - path: "src/hooks/useMapsConfig.ts"
      provides: "Maps configuration hook"
      min_lines: 15
  key_links:
    - from: "src/components/Map.tsx"
      to: "src/hooks/useMapsConfig.ts"
      via: "import"
      pattern: "import.*useMapsConfig"
---

<objective>
Create a reusable useMapsConfig hook that provides Google Maps configuration to components, and update environment documentation with SESSION_TTL_HOURS.

Purpose: Centralize Maps configuration for consistency and add configurable session TTL.
Output: src/hooks/useMapsConfig.ts hook and updated .env.example.
</objective>

<context>
@.planning/PROJECT.md
@src/lib/env.ts (from plan 13-01)
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useMapsConfig hook</name>
  <files>src/hooks/useMapsConfig.ts</files>
  <action>
Create src/hooks/useMapsConfig.ts with a reusable hook:

```typescript
"use client";

import { useMemo } from "react";
import { GOOGLE_MAPS_API_KEY, GOOGLE_MAPS_PLACES_RADIUS_METERS } from "../lib/env";

export interface MapsConfig {
  apiKey: string;
  placesRadius: number;
  libraries?: ("places" | "marker" | "geometry")[];
}

export function useMapsConfig(): MapsConfig {
  return useMemo(() => ({
    apiKey: GOOGLE_MAPS_API_KEY ?? "",
    placesRadius: GOOGLE_MAPS_PLACES_RADIUS_METERS ?? 2500,
    libraries: ["places", "marker", "geometry"]
  }), [GOOGLE_MAPS_API_KEY, GOOGLE_MAPS_PLACES_RADIUS_METERS]);
}
```

This hook provides centralized Maps configuration that can be used by any map component.
  </action>
  <verify>
    <automated>tsc --noEmit src/hooks/useMapsConfig.ts</automated>
  </verify>
  <done>src/hooks/useMapsConfig.ts exists and exports a hook returning MapsConfig</done>
</task>

<task type="auto">
  <name>Task 2: Update env.ts with SESSION_TTL_HOURS and add to .env.example</name>
  <files>src/lib/env.ts, .env.example</files>
  <action>
Update src/lib/env.ts (created in 13-01) to ensure SESSION_TTL_HOURS is exported:

```typescript
export const SESSION_TTL_HOURS = parseInt(process.env.SESSION_TTL_HOURS ?? "24", 10);
```

Add to .env.example:
```
SESSION_TTL_HOURS=24
```

This makes session TTL configurable for operations teams.
  </action>
  <verify>
<automated>grep -q "SESSION_TTL_HOURS" .env.example && grep -q "SESSION_TTL_HOURS" src/lib/env.ts</automated>
  </verify>
  <done>SESSION_TTL_HOURS documented in .env.example</done>
</task>

</tasks>

<verification>
- [ ] useMapsConfig hook created
- [ ] SESSION_TTL_HOURS documented in .env.example
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
- useMapsConfig.ts provides MapsConfig interface
- SESSION_TTL_HOURS available in env.ts and documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactoring-quick-wins/13-03-SUMMARY.md`
</output>