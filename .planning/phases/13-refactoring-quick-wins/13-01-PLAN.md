---
phase: 13-refactoring-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/lib/api/session-client.ts
  - src/hooks/useSessionSync.ts
  - .env.example
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-02
  - REFACTOR-03
  - REFACTOR-04
  - REFACTOR-05

must_haves:
  truths:
    - "Environment variables are defined in a single location and reused across all files"
    - "Error codes are centralized and shared between frontend and backend"
    - "Maps configuration is accessible via a reusable hook"
    - "Session TTL is configurable via environment variable"
    - "Polling interval is increased to reduce server load"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "Centralized environment variable configuration"
      min_lines: 20
    - path: "services/api/src/lib/error-codes.ts"
      provides: "Shared error code constants"
      min_lines: 30
    - path: "src/hooks/useMapsConfig.ts"
      provides: "Maps configuration hook"
      min_lines: 15
  key_links:
    - from: "src/lib/api/session-client.ts"
      to: "src/lib/env.ts"
      via: "import"
      pattern: "import.*env.*from"
    - from: "src/hooks/useSessionSync.ts"
      to: "src/lib/env.ts"
      via: "import"
      pattern: "import.*env.*from"
---

<objective>
Refactor codebase to eliminate code duplication, centralize configuration, and improve performance through quick wins.

Purpose: Improve code quality and maintainability without changing functionality.
Output: Centralized env config, shared error codes, maps config hook, configurable TTL, optimized polling.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Current codebase patterns
@src/lib/api/session-client.ts
@src/hooks/useSessionSync.ts
@services/api/src/modules/ranking/contracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized environment config (src/lib/env.ts)</name>
  <files>src/lib/env.ts</files>
  <action>
Create src/lib/env.ts with centralized environment variable configuration:
- Export API_BASE_URL from process.env.NEXT_PUBLIC_API_BASE_URL with default "http://localhost:8080"
- Export GOOGLE_MAPS_API_KEY from process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
- Export GOOGLE_MAPS_PLACES_RADIUS_METERS from process.env.GOOGLE_MAPS_PLACES_RADIUS_METERS with default 2500
- Export SESSION_TTL_HOURS from process.env.SESSION_TTL_HOURS with default 24
- Export API_PORT from process.env.API_PORT with default 8080
- Export APP_URL from process.env.APP_URL
- Export FALLBACK_POLLING_INTERVAL_MS with default 3000 (increased from 1000)

Use TypeScript with proper type annotations.
  </action>
  <verify>
    <automated>tsc --noEmit src/lib/env.ts</automated>
  </verify>
  <done>src/lib/env.ts exists with 5+ exported environment variables</done>
</task>

<task type="auto">
  <name>Task 2: Update session-client.ts to use centralized env</name>
  <files>src/lib/api/session-client.ts</files>
  <action>
Update src/lib/api/session-client.ts:
- Remove the line: const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8080";
- Add import: import { API_BASE_URL } from "../lib/env";
- This deduplicates the API_BASE_URL definition that was also in useSessionSync.ts
  </action>
  <verify>
    <automated>grep -q "from.*lib/env" src/lib/api/session-client.ts && tsc --noEmit src/lib/api/session-client.ts</automated>
  </verify>
  <done>session-client.ts imports API_BASE_URL from src/lib/env.ts</done>
</task>

<task type="auto">
  <name>Task 3: Update useSessionSync.ts to use centralized env</name>
  <files>src/hooks/useSessionSync.ts</files>
  <action>
Update src/hooks/useSessionSync.ts:
- Remove hardcoded process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8080" from line 213
- Add import: import { API_BASE_URL, FALLBACK_POLLING_INTERVAL_MS } from "../lib/env";
- Update the EventSource URL to use imported API_BASE_URL
- Update the fallback polling interval (line 196) to use FALLBACK_POLLING_INTERVAL_MS (now 3000ms instead of 1000ms)
  </action>
  <verify>
    <automated>grep -q "FALLBACK_POLLING_INTERVAL_MS" src/hooks/useSessionSync.ts && tsc --noEmit src/hooks/useSessionSync.ts</automated>
  </verify>
  <done>useSessionSync.ts uses centralized env with increased polling interval (3s)</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- [ ] All tasks complete without breaking existing functionality
- [ ] TypeScript compiles without errors
- [ ] No hardcoded environment variables remain in session-client.ts or useSessionSync.ts
</verification>

<success_criteria>
- Environment variables centralized in src/lib/env.ts
- session-client.ts imports from env.ts
- useSessionSync.ts imports from env.ts with increased polling interval
- FALLBACK_POLLING_INTERVAL_MS defaults to 3000ms
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactoring-quick-wins/13-01-SUMMARY.md`
</output>