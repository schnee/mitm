---
phase: 11-map-first-provider-integration-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ranking/RankedResultsList.tsx
  - src/components/ranking/RankedSpotsMap.tsx
  - src/app/s/[token]/page.tsx
  - src/lib/session-flow.ts
autonomous: true
requirements:
  - UX-23
  - UX-24
  - UX-26
must_haves:
  truths:
    - "Marker click highlights matching list card with smooth scroll"
    - "List 'Focus on map' action selects marker and pans map"
    - "Selected venue preserves across shortlist/confirm changes"
    - "Selected venue preserves across ranking refresh when still present"
  artifacts:
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Selected venue focus entry and callback props"
    - path: "src/components/ranking/RankedSpotsMap.tsx"
      provides: "Marker selection callback and selectedMarker prop"
    - path: "src/app/s/[token]/page.tsx"
      provides: "selectedVenueId state and map/list wiring"
  key_links:
    - from: "RankedSpotsMap.tsx (marker click)"
      to: "RankedResultsList.tsx (list highlight)"
      via: "onMarkerSelect callback sets selectedVenueId"
    - from: "RankedResultsList.tsx (focus button)"
      to: "RankedSpotsMap.tsx (center on marker)"
      via: "onVenueFocus callback with map pan"
---

<objective>
Wire two-way map/list synchronization: marker click selects corresponding list card with smooth scroll; list focus action centers map on marker. Preserve selected venue across shortlist/confirm changes and ranking refresh when venue still exists.

Purpose: Complete D-06 through D-09 from CONTEXT.md — marker/list synchronization and selected venue persistence.
Output: Bidirectional sync callbacks, selected state preservation, smooth scroll/pan animations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-map-first-provider-integration-completion/11-CONTEXT.md
@src/components/ranking/RankedResultsList.tsx
@src/components/ranking/RankedSpotsMap.tsx
@src/app/s/[token]/page.tsx
@src/lib/session-flow.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement marker click to list highlight with smooth scroll</name>
  <read_first>src/components/ranking/RankedResultsList.tsx, src/components/ranking/RankedSpotsMap.tsx</read_first>
  <files>src/components/ranking/RankedResultsList.tsx, src/components/ranking/RankedSpotsMap.tsx</files>
  <behavior>
    - Clicking marker highlights corresponding list card
    - List auto-scrolls to highlighted card with smooth animation
    - Visual feedback shows selected card with ring/accent styling
  </behavior>
  <action>In `RankedSpotsMap.tsx`, the existing `onMarkerSelect` callback already receives venueId. Extend it to call the page-level `selectedVenueId` setter. In `RankedResultsList.tsx`, add logic to scroll the selected card into view with `scrollIntoView({ behavior: 'smooth', block: 'center' })` when `selectedVenueId` matches a list item. Add `.result-card-selected` class with ring/accent styling per D-06.</action>
  <acceptance_criteria>
    - Clicking a marker triggers `onMarkerSelect` with the venue ID
    - Selected list card has `.result-card-selected` class
    - List auto-scrolls to selected card with smooth animation
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Marker click synchronizes to list with highlight and smooth scroll.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement list focus action to pan map to marker</name>
  <read_first>src/components/ranking/RankedResultsList.tsx, src/components/ranking/RankedSpotsMap.tsx</read_first>
  <files>src/components/ranking/RankedResultsList.tsx, src/components/ranking/RankedSpotsMap.tsx</files>
  <behavior>
    - "Focus on map" button in list card centers map on marker
    - Map pans to marker location preserving current zoom level
    - Marker receives visual highlight after pan
  </behavior>
  <action>In `RankedResultsList.tsx`, add an action button/link labeled "Focus on map" in each card. Pass `onVenueFocus` prop (distinct from `onAddToShortlist`) that triggers map pan. In `RankedSpotsMap.tsx`, use the `useMap` hook's `panTo` or `setCenter` to animate map to the venue's lat/lng. Per D-07, preserve current zoom level during pan.</action>
  <acceptance_criteria>
    - List cards contain "Focus on map" action button
    - Clicking triggers map pan animation
    - Map zoom level is preserved during pan
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>List focus action pans map to marker while preserving zoom.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve selected venue across state changes and ranking refresh</name>
  <read_first>src/app/s/[token]/page.tsx, src/components/ranking/RankedResultsList.tsx</read_first>
  <files>src/app/s/[token]/page.tsx</files>
  <behavior>
    - Selected venue persists when shortlist/confirm changes (if venue still exists)
    - Selected venue persists across ranking refresh when still in results
    - If selected venue no longer exists, clear selection or select first ranked
  </behavior>
  <action>In `page.tsx`, maintain `selectedVenueId` state that survives shortlist and confirm actions. When results refresh, validate that `selectedVenueId` still exists in new results array. Per D-08: If still exists, keep selection. Per D-09: If no longer exists (e.g., venue filtered out), clear and set to first venue in new results.</action>
  <acceptance_criteria>
    - Selected venue state survives shortlist/confirm mutations
    - Selected venue state survives ranking refresh if venue still present
    - Selection clears appropriately if venue no longer exists
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Selected venue state is preserved appropriately across session mutations.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- Manual verification: Test marker click scrolls to list, list focus pans to marker, selection persists
</verification>

<success_criteria>
- D-06: Marker click highlights matching list card with smooth scroll
- D-07: List "Focus on map" action selects marker, pans map, preserves zoom
- D-08: Selected venue preserves across shortlist/confirm when venue still exists
- D-09: Selected venue clears appropriately when no longer present in results
</success_criteria>

<output>
After completion, create `.planning/phases/11-map-first-provider-integration-completion/11-02-SUMMARY.md`
</output>