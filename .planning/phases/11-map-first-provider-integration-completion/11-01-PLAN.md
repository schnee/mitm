---
phase: 11-map-first-provider-integration-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ranking/RankedSpotsMap.tsx
  - package.json
  - src/app/globals.css
autonomous: true
requirements:
  - UX-23
  - UX-24
  - UX-26
user_setup:
  - service: google-maps-platform
    why: "Provider-backed interactive map for ranked spots"
    env_vars:
      - name: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials"
must_haves:
  truths:
    - "Provider-backed map renders with Google Maps API when key is present"
    - "Map auto-fits bounds to all ranked venues on load"
    - "Markers use provider rendering (not fallback grid buttons)"
    - "Simple custom markers (no clustering) for this phase"
  artifacts:
    - path: "src/components/ranking/RankedSpotsMap.tsx"
      provides: "Provider-backed map component with interactive markers"
    - path: "package.json"
      provides: "@vis.gl/react-google-maps dependency"
  key_links:
    - from: "src/components/ranking/RankedSpotsMap.tsx"
      to: "@vis.gl/react-google-maps"
      via: "Map, APIProvider, Marker imports"
      pattern: "APIProvider|Map google"
---

<objective>
Replace fallback marker-grid UI with provider-backed Google Maps rendering. Install @vis.gl/react-google-maps, integrate Map component with proper API provider, implement simple custom markers, and auto-fit map bounds to all ranked venues on load.

Purpose: Complete D-01 through D-04 from CONTEXT.md — provider-backed map as the normal/default path.
Output: Provider-backed map component, installed dependencies, marker auto-fit behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-map-first-provider-integration-completion/11-CONTEXT.md
@src/components/ranking/RankedSpotsMap.tsx
@src/lib/api/session-client.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @vis.gl/react-google-maps and create provider-backed map component</name>
  <read_first>src/components/ranking/RankedSpotsMap.tsx, src/lib/api/session-client.ts, package.json</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, package.json</files>
  <behavior>
    - @vis.gl/react-google-maps is installed and resolves
    - RankedSpotsMap renders actual Google Maps when API key is present
    - Map renders within the panel container structure
  </behavior>
  <action>Install `@vis.gl/react-google-maps` with pnpm. Refactor `RankedSpotsMap.tsx` to use APIProvider + Map from @vis.gl/react-google-maps instead of the fallback button-grid. Keep the same external props contract: `{ results, shortlistVenueIds, confirmedVenueId, selectedVenueId, onMarkerSelect }`. Render map inside the existing panel structure with `mapContainerClassName="map-panel"`. Handle missing key by showing the existing fallback message but still attempt provider load (per D-05, list-only flow is non-blocking with warning UI).</action>
  <acceptance_criteria>
    - `package.json` contains `@vis.gl/react-google-maps` version
    - `src/components/ranking/RankedSpotsMap.tsx` imports from `@vis.gl/react-google-maps`
    - `src/components/ranking/RankedSpotsMap.tsx` contains `<APIProvider>` and `<Map>` components
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && npx tsc --noEmit</automated>
  </verify>
  <done>Provider-backed map renders when NEXT_PUBLIC_GOOGLE_MAPS_API_KEY is configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement simple custom markers with state styling and auto-fit bounds</name>
  <read_first>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</files>
  <behavior>
    - Markers render as Map pins using Marker component from @vis.gl/react-google-maps
    - Auto-fit map bounds to all ranked venues on results load
    - Marker colors reflect state: default, selected, shortlisted, confirmed
  </behavior>
  <action>In `RankedSpotsMap.tsx`, add `<Marker>` components for each venue from results prop. Use existing `markerClassName` logic to determine marker state and apply color accordingly: default (gray), selected (blue ring), shortlisted (golden), confirmed (green, terminal). Per D-03, auto-fit bounds using `useMap` hook's fitBounds after markers load. Keep simple custom markers per D-04 (no clustering).</action>
  <acceptance_criteria>
    - `src/components/ranking/RankedSpotsMap.tsx` contains `<Marker>` components
    - `src/components/ranking/RankedSpotsMap.tsx` implements auto-fit bounds logic
    - Marker colors match state hierarchy: confirmed > shortlisted > selected > default
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Markers render on provider map with correct state colors and map auto-fits to all venues.</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS for provider map container and marker state visual treatment</name>
  <read_first>src/app/globals.css</read_first>
  <files>src/app/globals.css</files>
  <behavior>
    - Map container has appropriate dimensions and styling
    - Marker state colors are visually distinct and accessible
  </behavior>
  <action>Add CSS for `.map-panel` container with min-height: 300px, width: 100%. Ensure marker colors from CSS match the provider marker colors: `--marker-default: #6b7280`, `--marker-selected: #3b82f6`, `--marker-shortlisted: #f59e0b`, `--marker-confirmed: #10b981`. Confirm is terminal/dominant per D-10.</action>
  <acceptance_criteria>
    - `src/app/globals.css` contains `.map-panel` styling
    - `src/app/globals.css` defines marker state color variables
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Map container has proper dimensions and marker states are visually distinct.</done>
</task>

</tasks>

<verification>
- `pnpm install && npx tsc --noEmit`
- Manual verification: Visit spots step with ranked results, verify Google Maps renders
</verification>

<success_criteria>
- D-01: RankedSpotsMap component contract unchanged, internals replaced with provider
- D-02: Using @vis.gl/react-google-maps
- D-03: Auto-fit bounds to all ranked venues on load
- D-04: Simple custom markers only
- D-05: If key missing/invalid, list-only flow non-blocking with warning UI
</success_criteria>

<output>
After completion, create `.planning/phases/11-map-first-provider-integration-completion/11-01-SUMMARY.md`
</output>