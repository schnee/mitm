---
phase: 11-map-first-provider-integration-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ranking/RankedSpotsMap.tsx
  - src/components/ranking/RankedResultsList.tsx
  - src/app/s/[token]/page.tsx
  - src/lib/telemetry.ts
  - src/app/globals.css
autonomous: false
requirements:
  - UX-23
  - UX-24
  - UX-26
must_haves:
  truths:
    - "Confirmed marker is terminal and visually dominant"
    - "Shortlisted marker has color priority with selected ring"
    - "List cards mirror marker states with badges"
    - "Keyboard roving focus across markers with Enter/Space"
    - "Polite live-region announces selection/state changes"
    - "5-second map load timeout triggers fallback + retry"
    - "Telemetry events for map_load_failed, map_fallback_shown, map_retry_clicked, map_recovered"
  artifacts:
    - path: "src/components/ranking/RankedSpotsMap.tsx"
      provides: "Marker state hierarchy and keyboard/accessibility support"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "State badges mirrored from map"
    - path: "src/lib/telemetry.ts"
      provides: "Map degradation/recovery event emission"
  key_links:
    - from: "RankedSpotsMap.tsx (confirmed)"
      to: "RankedResultsList.tsx (confirmed badge)"
      via: "confirmedVenueId prop shared"
    - from: "RankedSpotsMap.tsx (shortlisted)"
      to: "RankedResultsList.tsx (shortlisted badge)"
      via: "shortlistVenueIds prop shared"
---

<objective>
Implement marker list state hierarchy: confirmed is terminal/dominant, shortlisted has color priority, selected shows ring. Mirror states to list cards with badges. Implement accessibility: roving keyboard focus, live-region announcements, 44x44 tap targets. Add failure handling: 5-second load timeout, run-time error handling, and telemetry events for map states.

Purpose: Complete D-10 through D-21 from CONTEXT.md — marker state parity, accessibility, and failure handling.
Output: State hierarchy, accessibility features, timeout handling, telemetry events.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-map-first-provider-integration-completion/11-CONTEXT.md
@src/components/ranking/RankedSpotsMap.tsx
@src/components/ranking/RankedResultsList.tsx
@src/app/s/[token]/page.tsx
@src/lib/telemetry.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement marker state hierarchy (confirmed terminal, shortlisted priority, selected ring)</name>
  <read_first>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</files>
  <behavior>
    - Confirmed venue marker is terminal (no shortlisted/selected visuals layered on top)
    - Shortlisted marker shows color priority before selected
    - Selected marker shows ring accent to indicate current focus
  </behavior>
  <action>Use existing `markerClassName` logic with state precedence: confirmed > shortlisted > selected > default. Confirmed state is terminal per D-10 — do not layer shortlisted/selected on confirmed. Shortlisted has color priority per D-11. Selected shows ring accent. Ensure provider Marker component uses appropriate colors and sizes to reflect this hierarchy visually.</action>
  <acceptance_criteria>
    - Confirmed marker ignores shortlisted/selected states
    - Shortlisted marker displays before selected marker
    - Selected marker shows ring/accent styling
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Marker state hierarchy matches D-10, D-11 requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror marker states to list cards with explicit badges</name>
  <read_first>src/components/ranking/RankedResultsList.tsx</read_first>
  <files>src/components/ranking/RankedResultsList.tsx, src/app/globals.css</files>
  <behavior>
    - List cards show selected/shortlisted/confirmed badges
    - Badges are visually distinct and match map marker states
  </behavior>
  <action>In `RankedResultsList.tsx`, add badge indicators for each card based on the same state logic: if `confirmedVenueId === venueId` show "Confirmed" badge, else if `shortlistVenueIds.includes(venueId)` show "Shortlisted" badge, else if `selectedVenueId === venueId` show "Selected" badge. Add CSS with appropriate badge colors: confirmed (green), shortlisted (gold), selected (blue ring).</action>
  <acceptance_criteria>
    - List cards show matching state badges
    - Badge colors match map marker colors
    - Badge precedence matches marker hierarchy
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>List cards mirror marker states with explicit badges per D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement accessibility (roving keyboard focus, live-region, 44x44 tap targets)</name>
  <read_first>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</files>
  <behavior>
    - Keyboard can navigate markers with arrow keys (roving focus)
    - Enter/Space selects focused marker
    - Live-region announces selection and state changes
    - All tap targets meet 44x44 minimum
  </behavior>
  <action>Per D-14: Implement roving tabIndex across markers (only one focused at a time, arrow keys move focus). Per D-15: Add hidden live-region container that announces state changes with `aria-live="polite"`. Per D-16: Add CSS ensuring all interactive elements meet 44x44 min touch target (markers, focus buttons, action links). Per D-17: Ensure every core map action has equivalent list action.</action>
  <acceptance_criteria>
    - Arrow keys navigate between markers
    - Enter/Space selects focused marker (or activates button)
    - Live-region announces selection changes
    - Touch targets meet 44x44 minimum
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Accessibility features per D-14, D-15, D-16, D-17 requirements.</done>
</task>

<task type="auto">
  <name>Task 4: Implement map load timeout and runtime error handling</name>
  <read_first>src/components/ranking/RankedSpotsMap.tsx, src/lib/telemetry.ts</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, src/lib/telemetry.ts</files>
  <behavior>
    - 5-second timeout triggers fallback UI with retry button
    - Runtime errors keep list interactive, show warning, disable broken map
    - Telemetry emits: map_load_failed, map_fallback_shown, map_retry_clicked, map_recovered
  </behavior>
  <action>Per D-18: Add 5-second timeout using setTimeout in useEffect; if map hasn't loaded by then, switch to fallback and show retry button. Per D-19: Wrap map in try/catch; on runtime error, keep list interactive, show persistent warning banner, disable map interactions but don't block flow. Per D-20: Add telemetry calls for each state: map_load_failed (load timeout or error), map_fallback_shown (after timeout), map_retry_clicked (on retry), map_recovered (successful reload after failure).</action>
  <acceptance_criteria>
    - Timeout fallback shows retry button
    - Runtime errors show warning but keep list working
    - Telemetry emits four events correctly
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Failure handling per D-18, D-19, D-20.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- Manual verification: Check marker states, accessibility, timeout behavior
</verification>

<success_criteria>
- D-10: Confirmed marker is terminal and visually dominant
- D-11: Shortlisted state has color priority, selected shows ring
- D-12: List cards mirror marker states with badges
- D-13: Confirmation triggers one-time pan, then leaves under user control
- D-14: Roving keyboard focus with arrow keys
- D-15: Polite live-region announcements
- D-16: 44x44 tap targets
- D-17: Parity — every core map action has equivalent list action
- D-18: 5-second timeout triggers fallback + retry
- D-19: Runtime errors keep list, show warning, disable map
- D-20: Telemetry events for degradation/recovery
- D-21: Phase 11 completion = provider-backed map as normal path
</success_criteria>

<output>
After completion, create `.planning/phases/11-map-first-provider-integration-completion/11-03-SUMMARY.md`
</output>