---
phase: 03-shared-shortlist-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - services/api/src/modules/session/repository.ts
  - services/api/src/routes/decision/upsertShortlistVenue.ts
  - services/api/src/routes/decision/confirmVenue.ts
  - services/api/src/routes/decision/validation.ts
  - services/api/src/dev-server.ts
  - services/api/tests/decision-routes.test.ts
autonomous: true
requirements: [DECS-01, DECS-02]
must_haves:
  truths:
    - "Both participants can add shortlisted venues through API endpoints backed by shared session state."
    - "Only one final confirmed place can be set per session."
    - "Conflicting second confirmation attempts return deterministic conflict responses."
  artifacts:
    - path: "services/api/src/routes/decision/upsertShortlistVenue.ts"
      provides: "Validated shortlist mutation endpoint"
      exports: ["upsertShortlistVenueHandler"]
    - path: "services/api/src/routes/decision/confirmVenue.ts"
      provides: "Conflict-safe final confirmation endpoint"
      exports: ["confirmVenueHandler"]
    - path: "services/api/tests/decision-routes.test.ts"
      provides: "API tests covering 200/400/404/409 behaviors"
      min_lines: 80
  key_links:
    - from: "services/api/src/dev-server.ts"
      to: "services/api/src/routes/decision/upsertShortlistVenue.ts"
      via: "POST /v1/decision/shortlist route binding"
      pattern: "\/v1\/decision\/shortlist"
    - from: "services/api/src/routes/decision/confirmVenue.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "repository confirm call"
      pattern: "confirmVenue"
---

<objective>
Expose shortlist and final-confirmation capabilities through validated API routes with race-safe confirmation behavior.

Purpose: Implement DECS-02 and complete API path for DECS-01 using deterministic conflict handling.
Output: Decision route handlers, validation schemas, server wiring, and passing API tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-shortlist-confirmation/03-RESEARCH.md
@.planning/phases/03-shared-shortlist-confirmation/03-01-PLAN.md

<interfaces>
From services/api/src/modules/session/contracts.ts (from 03-01):
```ts
export interface ShortlistVenue { venueId: string; name: string; category: string; openNow: boolean | null; lat: number; lng: number; etaParticipantA: number; etaParticipantB: number; addedByParticipantId: string; addedAt: string; }
export interface ConfirmedPlace { venueId: string; name: string; category: string; lat: number; lng: number; navigationUrl: string; confirmedByParticipantId: string; confirmedAt: string; }
```

From services/api/src/dev-server.ts:
```ts
sendJson(response, result.status, result.body);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for shortlist and confirmation routes</name>
  <read_first>services/api/src/dev-server.ts, services/api/src/routes/ranking/upsertRankingInputs.ts, services/api/tests/ranking-inputs.test.ts, services/api/src/modules/session/repository.ts</read_first>
  <files>services/api/tests/decision-routes.test.ts</files>
  <behavior>
    - Test 1: valid shortlist payload returns 200 and stored shortlist item.
    - Test 2: invalid shortlist payload returns 400 `INVALID_DECISION_PAYLOAD`.
    - Test 3: first confirm succeeds, same venue confirm is idempotent 200, different venue confirm returns 409 `PLACE_ALREADY_CONFIRMED`.
  </behavior>
  <action>Create `services/api/tests/decision-routes.test.ts` with failing route-handler tests using session fixtures. Include explicit assertions for exact response bodies: `{ error: "INVALID_DECISION_PAYLOAD" }`, `{ error: "SESSION_NOT_FOUND" }`, and `{ error: "PLACE_ALREADY_CONFIRMED" }`.</action>
  <acceptance_criteria>- `services/api/tests/decision-routes.test.ts` contains test name `rejects conflicting second confirmation`.
- `services/api/tests/decision-routes.test.ts` contains string `PLACE_ALREADY_CONFIRMED`.
- `services/api/tests/decision-routes.test.ts` contains string `INVALID_DECISION_PAYLOAD`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/decision-routes.test.ts</automated>
  </verify>
  <done>Failing tests define required API behavior for shortlist and conflict-safe confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement decision route handlers, validation, and server wiring</name>
  <read_first>services/api/src/modules/session/repository.ts, services/api/src/dev-server.ts, services/api/tests/decision-routes.test.ts</read_first>
  <files>services/api/src/modules/session/repository.ts, services/api/src/routes/decision/upsertShortlistVenue.ts, services/api/src/routes/decision/confirmVenue.ts, services/api/src/routes/decision/validation.ts, services/api/src/dev-server.ts, services/api/tests/decision-routes.test.ts</files>
  <behavior>
    - Test 1: `POST /v1/decision/shortlist` validates payload and persists shortlist in session.
    - Test 2: `POST /v1/decision/confirm` only allows one final place per session.
    - Test 3: confirming with unknown session or participant returns deterministic 404.
  </behavior>
  <action>Add zod schemas in `validation.ts` for shortlist and confirm payloads with required keys: `sessionId`, `participantId`, `venueId`; shortlist payload also requires `name`, `category`, `openNow`, `lat`, `lng`, `etaParticipantA`, `etaParticipantB`. In `repository.ts`, add `confirmVenue(input)` that sets first confirmation, returns existing confirmation for same `venueId`, and throws explicit domain error code for different second `venueId`. Build handlers mapping errors to `400 INVALID_DECISION_PAYLOAD`, `404 SESSION_NOT_FOUND`, and `409 PLACE_ALREADY_CONFIRMED`. Wire both endpoints in `dev-server.ts` at exact paths `/v1/decision/shortlist` and `/v1/decision/confirm`.</action>
  <acceptance_criteria>- `services/api/src/dev-server.ts` contains `/v1/decision/shortlist` and `/v1/decision/confirm`.
- `services/api/src/routes/decision/confirmVenue.ts` contains `PLACE_ALREADY_CONFIRMED`.
- `services/api/src/routes/decision/upsertShortlistVenue.ts` exports `upsertShortlistVenueHandler`.
- `npx vitest run services/api/tests/decision-routes.test.ts` exits 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/decision-routes.test.ts</automated>
  </verify>
  <done>API supports shortlist mutations and conflict-safe final confirmation with deterministic status codes.</done>
</task>

</tasks>

<verification>
Run decision route tests to prove endpoint validation, persistence, and confirmation conflict safety.
</verification>

<success_criteria>
- DECS-01 covered through API shortlist writes/read visibility.
- DECS-02 covered with single final confirmation and race-safe conflict response.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-shortlist-confirmation/03-02-SUMMARY.md`
</output>
