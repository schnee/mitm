---
phase: 03-shared-shortlist-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/tests/decision-state.test.ts
autonomous: true
requirements: [DECS-01]
must_haves:
  truths:
    - "Both participants can add ranked venues to one shared shortlist."
    - "Shortlist updates are session-authoritative and visible in canonical session snapshot data."
  artifacts:
    - path: "services/api/src/modules/session/contracts.ts"
      provides: "Shortlist and confirmed-place contract shapes"
      contains: "ShortlistVenue"
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Session-owned shortlist state operations"
      contains: "upsertShortlistVenue"
    - path: "services/api/tests/decision-state.test.ts"
      provides: "Domain tests for shortlist behavior"
      min_lines: 60
  key_links:
    - from: "services/api/src/modules/session/repository.ts"
      to: "services/api/src/modules/session/contracts.ts"
      via: "typed shortlist/confirmation state"
      pattern: "ShortlistVenue|ConfirmedPlace"
---

<objective>
Establish shared decision-state contracts and repository behavior for shortlist actions.

Purpose: Implement DECS-01 at the domain layer so both participants operate on the same server-authoritative shortlist.
Output: Extended session contracts, shortlist repository methods, and passing domain tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-shortlist-confirmation/03-RESEARCH.md
@.planning/phases/02-fair-ranking-recommendation-core/02-03-SUMMARY.md

<interfaces>
From services/api/src/modules/session/contracts.ts:
```ts
export interface SessionSnapshot {
  sessionId: string;
  status: SessionStatus;
  updatedAt: string;
  participants: SessionSnapshotParticipant[];
  inputsReady: boolean;
}
```

From services/api/src/modules/session/repository.ts:
```ts
getSessionSnapshot(sessionId: string): SessionSnapshot | undefined;
listSessionEvents(sessionId: string, since?: string): SessionEvent[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for shared shortlist domain behavior</name>
  <read_first>services/api/src/modules/session/repository.ts, services/api/src/modules/session/contracts.ts, services/api/tests/location-flow.test.ts</read_first>
  <files>services/api/tests/decision-state.test.ts</files>
  <behavior>
    - Test 1: adding shortlist item stores one shared entry with metadata (`venueId`, `name`, `category`, `openNow`, `etaParticipantA`, `etaParticipantB`).
    - Test 2: adding same `venueId` again updates existing record (no duplicates).
    - Test 3: snapshot exposes shortlist array shared across both participants.
  </behavior>
  <action>Create `services/api/tests/decision-state.test.ts` with failing tests that instantiate `SessionRepository`, create/join a session, and exercise shortlist APIs. Use fixed fixture venue values: `venueId: "coffee-spot"`, `name: "Coffee Spot"`, `category: "cafe"`, `openNow: true`, `etaParticipantA: 12`, `etaParticipantB: 14`, `lat: 40.72`, `lng: -73.99`.</action>
  <acceptance_criteria>- `services/api/tests/decision-state.test.ts` contains test name `stores one shared shortlist entry`.
- `services/api/tests/decision-state.test.ts` contains test name `updates existing shortlist item when venueId repeats`.
- `services/api/tests/decision-state.test.ts` references `getSessionSnapshot`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/decision-state.test.ts</automated>
  </verify>
  <done>Failing tests define expected shared-shortlist behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shortlist contracts and repository methods</name>
  <read_first>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, services/api/tests/decision-state.test.ts</read_first>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, services/api/tests/decision-state.test.ts</files>
  <behavior>
    - Test 1: repository method `upsertShortlistVenue` writes shortlist item into session state.
    - Test 2: repeated `venueId` does not create duplicate shortlist records.
    - Test 3: session snapshot includes `shortlist` and `confirmedPlace` fields.
  </behavior>
  <action>In `contracts.ts`, add `ShortlistVenue` and `ConfirmedPlace` interfaces with exact fields from research. Extend `SessionSnapshot` to include `shortlist: ShortlistVenue[]` and `confirmedPlace: ConfirmedPlace | null`. In `repository.ts`, add private map storage for shortlist and confirmed place by session, implement `upsertShortlistVenue(input)` keyed by `venueId` (replace existing entry on repeat), and return updated shortlist. Update `getSessionSnapshot` to return both new fields and keep existing `inputsReady` semantics unchanged.</action>
  <acceptance_criteria>- `services/api/src/modules/session/contracts.ts` contains `export interface ShortlistVenue`.
- `services/api/src/modules/session/contracts.ts` contains `confirmedPlace: ConfirmedPlace | null` inside `SessionSnapshot`.
- `services/api/src/modules/session/repository.ts` contains method name `upsertShortlistVenue`.
- `npx vitest run services/api/tests/decision-state.test.ts` exits 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/decision-state.test.ts</automated>
  </verify>
  <done>Session repository supports shared shortlist state and exposes it in canonical snapshots.</done>
</task>

</tasks>

<verification>
Run domain tests to verify shared shortlist add/update behavior and snapshot visibility.
</verification>

<success_criteria>
- DECS-01 is satisfied at domain level with one shared shortlist per session.
- Session snapshot includes shortlist data needed by API/UI layers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-shortlist-confirmation/03-01-SUMMARY.md`
</output>
