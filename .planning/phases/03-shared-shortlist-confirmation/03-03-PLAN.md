---
phase: 03-shared-shortlist-confirmation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/lib/api/session-client.ts
  - src/hooks/useSessionSync.ts
  - src/components/ranking/RankedResultsList.tsx
  - src/components/decision/ShortlistPanel.tsx
  - src/components/decision/ConfirmedPlaceCard.tsx
  - src/app/s/[token]/page.tsx
  - tests/e2e/decision-confirmation.spec.ts
autonomous: true
requirements: [DECS-01, DECS-02, DECS-03]
must_haves:
  truths:
    - "Both participants can add ranked venues to one shared shortlist from the session UI."
    - "Both participants see one final confirmed place when confirmation occurs."
    - "Confirmed place renders a working external map link for navigation handoff."
  artifacts:
    - path: "src/components/decision/ShortlistPanel.tsx"
      provides: "Shared shortlist rendering with confirm actions"
      min_lines: 50
    - path: "src/components/decision/ConfirmedPlaceCard.tsx"
      provides: "Final destination state and navigation handoff link"
      min_lines: 40
    - path: "src/lib/api/session-client.ts"
      provides: "Typed decision API + snapshot contracts"
      contains: "confirmVenue"
  key_links:
    - from: "src/components/ranking/RankedResultsList.tsx"
      to: "src/app/s/[token]/page.tsx"
      via: "Add-to-shortlist callback"
      pattern: "onAddToShortlist"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/lib/api/session-client.ts"
      via: "decision API calls"
      pattern: "upsertShortlistVenue|confirmVenue"
    - from: "src/components/decision/ConfirmedPlaceCard.tsx"
      to: "external maps"
      via: "navigationUrl anchor"
      pattern: "target=\"_blank\""
---

<objective>
Integrate shared shortlist, final confirmation, and navigation handoff into the session UI.

Purpose: Complete DECS-01/02/03 in the user-visible flow after ranking results are shown.
Output: Typed decision client APIs, decision UI components, page wiring, and e2e coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-shortlist-confirmation/03-RESEARCH.md
@.planning/phases/03-shared-shortlist-confirmation/03-02-PLAN.md
@src/app/s/[token]/page.tsx

<interfaces>
From src/lib/api/session-client.ts:
```ts
export interface SessionSnapshotResponse {
  sessionId: string;
  status: "created" | "joined" | "locating" | "ranked" | "confirmed" | "expired";
  updatedAt: string;
  participants: SessionSnapshotParticipant[];
  inputsReady: boolean;
}
```

From src/components/ranking/RankedResultsList.tsx:
```ts
export function RankedResultsList({ results }: { results: RankedVenue[] })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed client and sync hook for decision state</name>
  <read_first>src/lib/api/session-client.ts, src/hooks/useSessionSync.ts, services/api/src/modules/session/contracts.ts</read_first>
  <files>src/lib/api/session-client.ts, src/hooks/useSessionSync.ts</files>
  <action>In `session-client.ts`, add exported types `ShortlistVenue` and `ConfirmedPlace`, extend `SessionSnapshotResponse` with `shortlist` and `confirmedPlace`, and add functions `upsertShortlistVenue(input)` -> POST `/v1/decision/shortlist`, `confirmVenue(input)` -> POST `/v1/decision/confirm`. In `useSessionSync.ts`, update `mergeEvent` logic so `session_updated` events carrying `diff.shortlist` or `diff.confirmedPlace` update snapshot fields without waiting for full reload.</action>
  <acceptance_criteria>- `src/lib/api/session-client.ts` exports `upsertShortlistVenue`.
- `src/lib/api/session-client.ts` exports `confirmVenue`.
- `src/lib/api/session-client.ts` contains `shortlist:` and `confirmedPlace:` in `SessionSnapshotResponse`.
- `src/hooks/useSessionSync.ts` contains `diff.shortlist` and `diff.confirmedPlace` handling.</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Frontend data contracts and realtime merge logic support shortlist and confirmation state.</done>
</task>

<task type="auto">
  <name>Task 2: Add shortlist + confirmation UI and navigation handoff</name>
  <read_first>src/app/s/[token]/page.tsx, src/components/ranking/RankedResultsList.tsx, tests/e2e/ranking-results.spec.ts, src/lib/api/session-client.ts</read_first>
  <files>src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx, src/app/s/[token]/page.tsx, tests/e2e/decision-confirmation.spec.ts</files>
  <action>Update `RankedResultsList.tsx` to accept `onAddToShortlist(venue)` callback and render an `Add to shortlist` button for each venue. Create `ShortlistPanel.tsx` to render shared shortlist items and per-item `Confirm this place` button. Create `ConfirmedPlaceCard.tsx` to show final place name/category, confirming participant, and an anchor with `href={navigationUrl}`, `target="_blank"`, and visible label `Open in Maps`. Update `src/app/s/[token]/page.tsx` to wire: add shortlisted item via `upsertShortlistVenue`, show live shortlist from `sync.snapshot.shortlist`, confirm selected venue via `confirmVenue`, and render `ConfirmedPlaceCard` when `sync.snapshot.confirmedPlace` exists. Add `tests/e2e/decision-confirmation.spec.ts` that stubs decision endpoints and asserts shortlist item appears, confirmation UI appears, and `Open in Maps` link is visible.</action>
  <acceptance_criteria>- `src/components/ranking/RankedResultsList.tsx` contains button text `Add to shortlist`.
- `src/components/decision/ConfirmedPlaceCard.tsx` contains text `Open in Maps` and `target="_blank"`.
- `src/app/s/[token]/page.tsx` includes `<ShortlistPanel` and `<ConfirmedPlaceCard`.
- `tests/e2e/decision-confirmation.spec.ts` contains test name `shows shared shortlist and confirmed map handoff`.</acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Users can shortlist, confirm one final place, and launch external navigation from the confirmed destination card.</done>
</task>

</tasks>

<verification>
Type-check frontend integration and run e2e decision-flow coverage when app/browser runtime are available.
</verification>

<success_criteria>
- DECS-01: shortlist actions available and shared on session screen.
- DECS-02: one final confirmed place is rendered for both participants.
- DECS-03: confirmed place includes external map deep-link handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-shortlist-confirmation/03-03-SUMMARY.md`
</output>
