---
phase: 13-map-first-persistent-ux
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Session page uses PersistentMap in single column layout"
    - "Stage transitions trigger map + overlay changes"
  artifacts:
    - path: "src/app/s/[token]/page.tsx"
      provides: "Integrated page with persistent map"
files_modified:
  - src/app/s/[token]/page.tsx
  - src/lib/session-flow.ts
---

# Plan 13-03: Session Flow Integration

## Task

Integrate PersistentMap into session page, replace step-by-step cards with overlay-based flow.

## Implementation

1. Update session-flow.ts:
   - Add `stage` field to track map stage
   - Derive stage from step: location→setup, spots→spots, shortlist→shortlist, confirm→confirm
   - Keep step tracking for progress, add stage for map

2. Update session page (page.tsx):
   - Remove SetupCard, RankedSpotsMap, RankedResultsList
   - Add PersistentMap with stage prop
   - Add overlay panels driven by stage
   - Single column layout: map (left/top) + overlay panel (right/bottom)

3. Remove:
   - NextActionRail component usage
   - Step-by-step card rendering
   - Old map components

## Layout Structure

```
Desktop (>768px):
┌─────────────────────────────────────┐
│           PersistentMap             │
│  [markers layer]                    │
├─────────────────────────────────────┤
│        Overlay Panel               │  <- slides in/out
└─────────────────────────────────────┘

Mobile:
┌─────────────┐
│ PersistentMap│
│ [markers]   │
├─────────────┤
│Overlay panel│
└─────────────┘
```