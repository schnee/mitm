---
phase: 08-mobile-first-guided-flow-map-driven-decisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/session-flow.ts
  - src/app/s/[token]/page.tsx
  - src/app/globals.css
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements:
  - UX-19
  - UX-20
must_haves:
  truths:
    - "Only one primary workflow step is expanded at a time on mobile"
    - "Completed steps collapse into compact summaries that still show completion state"
    - "Users can move through Location -> Preferences -> Spots -> Shortlist -> Confirm without long-scroll ambiguity"
  artifacts:
    - path: "src/lib/session-flow.ts"
      provides: "Deterministic step contracts and flow-derivation helpers"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Guided step rendering with single-active-step orchestration"
    - path: "src/app/globals.css"
      provides: "Stepper and collapsed-summary mobile styles"
    - path: "tests/e2e/ranking-results.spec.ts"
      provides: "Regression checks for step progression + collapsed summaries"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/lib/session-flow.ts"
      via: "derived active/completed/blocked step state"
      pattern: "deriveSessionFlow\(|activeStepId"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/app/globals.css"
      via: "stepper/summary classNames"
      pattern: "guided-step|step-summary"
---

<objective>
Implement the guided single-active-step session flow and completed-step auto-collapse behavior.

Purpose: Deliver UX-19 and UX-20 to reduce mobile scroll depth and keep users focused on one actionable section.
Output: Shared flow derivation utility, guided stepper rendering, collapsed summaries, and e2e coverage for progression behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-01-SUMMARY.md
@.planning/phases/07-shared-auto-ranking-synced-results/07-03-SUMMARY.md
@.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-RESEARCH.md
@src/app/s/[token]/page.tsx
@src/lib/api/session-client.ts
@src/hooks/useSessionSync.ts
@src/app/globals.css

<interfaces>
From `src/lib/api/session-client.ts`:
```ts
export interface SessionSnapshotResponse {
  inputsReady: boolean;
  rankingInputsReady: boolean;
  rankingLifecycle: RankingLifecycleResponse;
  rankedResults: RankedVenue[];
  shortlist: ShortlistVenue[];
  confirmedPlace: ConfirmedPlace | null;
}
```

From `src/app/s/[token]/page.tsx`:
```ts
const rankingLifecycleState: RankingLifecycleState = sync.snapshot?.rankingLifecycle?.state ?? "waiting";
const rankedResults = snapshotRankedResults.length >= localRankedResults.length ? snapshotRankedResults : localRankedResults;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic session flow contracts for guided-step orchestration</name>
  <read_first>src/lib/api/session-client.ts, src/app/s/[token]/page.tsx, .planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-RESEARCH.md</read_first>
  <files>src/lib/session-flow.ts</files>
  <behavior>
    - Given session snapshot + local UI context, flow utility returns one active step id.
    - Completed steps are flagged with compact summary labels.
    - Step order remains fixed: location -> preferences -> spots -> shortlist -> confirm.
  </behavior>
  <action>Create `src/lib/session-flow.ts` exporting explicit types and helpers with concrete signatures: `export type SessionStepId = "location" | "preferences" | "spots" | "shortlist" | "confirm"`, `export interface SessionFlowStep { id: SessionStepId; title: string; completed: boolean; blockedBy: "self" | "partner" | null; summary: string; }`, and `export function deriveSessionFlow(input: {...}): { activeStepId: SessionStepId; steps: SessionFlowStep[] }`. Encode UX-19/UX-20 rules directly: exactly one active step, completed steps produce non-empty summaries (e.g. `Location: Confirmed`), blocked steps identify ownership. Keep logic pure with no React imports.</action>
  <acceptance_criteria>
    - `src/lib/session-flow.ts` contains `export type SessionStepId = "location" | "preferences" | "spots" | "shortlist" | "confirm"`.
    - `src/lib/session-flow.ts` contains `export function deriveSessionFlow(`.
    - `src/lib/session-flow.ts` contains string literals `Location: Confirmed`, `Preferences: Saved`, `Shortlist:`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Flow derivation contract exists and can drive guided-step rendering without ad-hoc conditionals in page.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor session page to render one expanded guided step with collapsed completed summaries</name>
  <read_first>src/app/s/[token]/page.tsx, src/lib/session-flow.ts, src/app/globals.css</read_first>
  <files>src/app/s/[token]/page.tsx, src/app/globals.css</files>
  <behavior>
    - Only the active step panel is expanded at any given time.
    - Completed steps render summary rows and can be reviewed without re-expanding by default.
    - Existing flow actions (save preferences, shortlist, confirm) remain functional.
  </behavior>
  <action>In `page.tsx`, derive flow state using `deriveSessionFlow(...)` and replace linear stage rendering with step sections keyed by `SessionStepId`. Keep LocationCaptureForm/LocationConfirmCard in `location`; RankingInputsForm + shared lifecycle panel in `preferences`; RankedResultsList in `spots`; ShortlistPanel in `shortlist`; ConfirmedPlaceCard in `confirm`. For completed non-active steps, render compact summary rows with class `step-summary` and hide full body via conditional render. Add CSS classes in `globals.css` for `.guided-stepper`, `.guided-step`, `.guided-step.active`, `.step-summary`, `.step-summary-complete`, with mobile-first spacing and no animation per UX focus on clarity.</action>
  <acceptance_criteria>
    - `src/app/s/[token]/page.tsx` imports `deriveSessionFlow` from `src/lib/session-flow.ts`.
    - `src/app/s/[token]/page.tsx` contains `guided-stepper` class usage.
    - `src/app/s/[token]/page.tsx` contains conditional render for `activeStepId`.
    - `src/app/globals.css` contains selectors `.guided-stepper`, `.guided-step.active`, `.step-summary`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Session page presents a guided single-active-step flow with collapsed completed summaries and preserved existing action behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mobile e2e regression for guided-step progression and collapse behavior</name>
  <read_first>tests/e2e/ranking-results.spec.ts, src/app/s/[token]/page.tsx</read_first>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <behavior>
    - Mobile test validates only one expanded step at a time.
    - Completed step summary appears after progression.
    - Existing ranking flow assertions continue to pass.
  </behavior>
  <action>Extend `tests/e2e/ranking-results.spec.ts` with a Pixel 5 scenario asserting guided-flow markers and collapse outcomes: verify `location` step starts active, after location confirmation `preferences` becomes active and `location` renders `Location: Confirmed` summary, and after ranking save `spots` step becomes active. Reuse existing fixtures/mocks; do not introduce new API mocks beyond current ranking-results test harness.</action>
  <acceptance_criteria>
    - `tests/e2e/ranking-results.spec.ts` contains assertions for `Location: Confirmed`.
    - `tests/e2e/ranking-results.spec.ts` contains assertions referencing active guided step state.
    - Existing ranking-results assertions remain in file (not removed).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5"</automated>
  </verify>
  <done>Automated mobile coverage proves single-active-step behavior and completed-step collapse without regressing ranking flow coverage.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5"`
</verification>

<success_criteria>
- UX-19 implemented with one active step at a time.
- UX-20 implemented with compact completed-step summaries.
- Guided-step behavior covered by mobile e2e assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-01-SUMMARY.md`
</output>
