---
phase: 08-mobile-first-guided-flow-map-driven-decisioning
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/components/ranking/RankedSpotsMap.tsx
  - src/components/ranking/RankedResultsList.tsx
  - src/app/s/[token]/page.tsx
  - src/lib/session-flow.ts
  - src/app/globals.css
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements:
  - UX-23
  - UX-24
  - UX-26
user_setup:
  - service: google-maps-platform
    why: "Client-rendered interactive map for ranked spots"
    env_vars:
      - name: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials"
must_haves:
  truths:
    - "Ranked spots are visible on map and list, with two-way synchronized selection"
    - "Shortlisted venues and confirmed venue have distinct marker states"
    - "Core map/list interactions remain keyboard-accessible and responsive"
  artifacts:
    - path: "src/components/ranking/RankedSpotsMap.tsx"
      provides: "Interactive map panel with selectable markers"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "List-to-map synchronization hooks and selected-row treatment"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Map-first layout wiring and shortlist/confirmed marker state propagation"
    - path: "tests/e2e/ranking-results.spec.ts"
      provides: "Regression checks for map/list sync + marker state parity"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/ranking/RankedSpotsMap.tsx"
      via: "selectedVenueId + shortlist/confirmed state props"
      pattern: "selectedVenueId|confirmedPlace|shortlist"
    - from: "src/components/ranking/RankedResultsList.tsx"
      to: "src/components/ranking/RankedSpotsMap.tsx"
      via: "shared selection callbacks"
      pattern: "onVenueFocus|onMarkerSelect"
---

<objective>
Deliver map-first ranked spots with synchronized map/list interactions and marker-state parity for shortlist/confirmation.

Purpose: Complete UX-23, UX-24, and UX-26 by making decisioning spatially intuitive without regressing accessibility and responsive behavior.
Output: New interactive map component, synchronized list integration, marker state styling, and mobile/desktop e2e validation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-01-PLAN.md
@.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-02-PLAN.md
@src/app/s/[token]/page.tsx
@src/components/ranking/RankedResultsList.tsx
@src/lib/api/session-client.ts
@src/app/globals.css

<interfaces>
From `src/lib/api/session-client.ts`:
```ts
export interface RankedVenue {
  venueId: string;
  name: string;
  lat: number;
  lng: number;
}

export interface ShortlistVenue { venueId: string; }
export interface ConfirmedPlace { venueId: string; }
```

From `src/components/ranking/RankedResultsList.tsx`:
```ts
export function RankedResultsList({ results, onAddToShortlist, shortlistVenueIds }: { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build map component with explicit marker states and keyboard interactions</name>
  <read_first>src/lib/api/session-client.ts, src/components/ranking/RankedResultsList.tsx, src/app/globals.css</read_first>
  <files>src/components/ranking/RankedSpotsMap.tsx, src/app/globals.css</files>
  <behavior>
    - Marker state distinguishes default, selected, shortlisted, and confirmed.
    - Marker selection can be triggered via pointer and keyboard.
    - Map remains usable on mobile and desktop breakpoints.
  </behavior>
  <action>Create `RankedSpotsMap.tsx` client component with props `{ results, shortlistVenueIds, confirmedVenueId, selectedVenueId, onMarkerSelect }`. Render Google Maps JS map using `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`; if key missing, show actionable fallback message and still allow list-only flow. For markers, apply exact state classes: `marker-default`, `marker-selected`, `marker-shortlisted`, `marker-confirmed` (confirmed supersedes shortlist). Add keyboard support (`tabIndex=0`, Enter/Space selects marker) and aria labels including venue name + state. Add CSS classes for map container and marker state color/contrast meeting UX-26 baseline.</action>
  <acceptance_criteria>
    - `src/components/ranking/RankedSpotsMap.tsx` exports `RankedSpotsMap`.
    - `src/components/ranking/RankedSpotsMap.tsx` references `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`.
    - `src/components/ranking/RankedSpotsMap.tsx` contains state identifiers `marker-default`, `marker-selected`, `marker-shortlisted`, `marker-confirmed`.
    - `src/app/globals.css` contains selectors for all four marker classes and map container.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Interactive map component exists with stateful markers, keyboard operability, and fallback handling when API key is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire map-first layout and two-way map/list synchronization into guided flow page</name>
  <read_first>src/app/s/[token]/page.tsx, src/components/ranking/RankedResultsList.tsx, src/components/ranking/RankedSpotsMap.tsx, src/lib/session-flow.ts</read_first>
  <files>src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx, src/lib/session-flow.ts, src/app/globals.css</files>
  <behavior>
    - Tapping list item focuses corresponding map marker.
    - Selecting map marker highlights corresponding list item.
    - Shortlist and confirmed updates immediately change marker/list states.
  </behavior>
  <action>Update `RankedResultsList.tsx` to accept `selectedVenueId` and `onVenueFocus` props; add explicit selected-row class (`result-card-selected`) and focus action button (`Focus on map`). In `page.tsx`, maintain `selectedVenueId` state and render map panel before list in Spots step (map-first ordering). Pass shortlist ids and confirmed id into `RankedSpotsMap`; pass selection callbacks both directions so list click sets marker and marker click sets list selection. Update `session-flow` summaries to include map-aware copy (`Spots: map + list synced`). Add responsive CSS so mobile shows stacked map then list; desktop uses two-column split with preserved tap target minimum 44px.</action>
  <acceptance_criteria>
    - `src/components/ranking/RankedResultsList.tsx` includes `selectedVenueId` and `onVenueFocus` props.
    - `src/app/s/[token]/page.tsx` imports and renders `RankedSpotsMap` before `RankedResultsList` in spots step.
    - `src/app/s/[token]/page.tsx` contains shared `selectedVenueId` state and two-way callback wiring.
    - `src/app/globals.css` contains `.result-card-selected` and responsive map/list layout selectors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Spots stage is map-first with deterministic two-way synchronization and shortlist/confirmed state parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add e2e accessibility/responsive regression coverage for map/list synchronization</name>
  <read_first>tests/e2e/ranking-results.spec.ts, src/components/ranking/RankedSpotsMap.tsx, src/components/ranking/RankedResultsList.tsx</read_first>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <behavior>
    - E2E verifies list->marker and marker->list synchronization.
    - E2E verifies shortlisted and confirmed marker state changes.
    - E2E verifies keyboard fallback for marker selection.
  </behavior>
  <action>Extend `ranking-results.spec.ts` with scenarios for Pixel 5 and Desktop Safari asserting: selecting a list row updates marker-selected state, selecting marker updates list selected styling, shortlist action updates marker-shortlisted class, confirm action updates marker-confirmed class, and Enter key on focused marker triggers selection. Keep existing flow assertions for create/join/location/preferences/shortlist/confirm to satisfy no-regression acceptance criteria.</action>
  <acceptance_criteria>
    - `tests/e2e/ranking-results.spec.ts` contains assertions for `marker-selected`, `marker-shortlisted`, and `marker-confirmed`.
    - `tests/e2e/ranking-results.spec.ts` contains keyboard interaction assertion for Enter/Space marker selection.
    - `tests/e2e/ranking-results.spec.ts` runs on both `Pixel 5` and `Desktop Safari` projects.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5" --project="Desktop Safari"</automated>
  </verify>
  <done>Automated coverage verifies map/list sync, marker state parity, and accessibility fallback behavior across mobile and desktop.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5" --project="Desktop Safari"`
</verification>

<success_criteria>
- UX-23: Map-first ranked spots view has two-way list/marker synchronization.
- UX-24: Marker styling reflects shortlist and confirmed states with confirmed lock precedence.
- UX-26: Core map/list interactions remain responsive and keyboard-accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-03-SUMMARY.md`
</output>
