---
phase: 08-mobile-first-guided-flow-map-driven-decisioning
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/session/SessionProgressBar.tsx
  - src/components/session/NextActionRail.tsx
  - src/components/session/ParticipantStatus.tsx
  - src/app/s/[token]/page.tsx
  - src/app/globals.css
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements:
  - UX-21
  - UX-22
  - UX-25
must_haves:
  truths:
    - "Both participants can always see who is blocking the next step"
    - "Mobile users always see exactly one next-action CTA without scrolling"
    - "Waiting/loading/error/success messaging reflects partner progress and lifecycle state"
  artifacts:
    - path: "src/components/session/SessionProgressBar.tsx"
      provides: "Sticky two-person progress bar with blocker ownership"
    - path: "src/components/session/NextActionRail.tsx"
      provides: "Sticky mobile single-next-action CTA rail"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Wiring of derived flow status + CTA into guided page"
    - path: "src/app/globals.css"
      provides: "Sticky layout and responsive spacing for status + CTA rails"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/session/SessionProgressBar.tsx"
      via: "derived step/blocker props"
      pattern: "SessionProgressBar|blockedBy"
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/session/NextActionRail.tsx"
      via: "single next action binding"
      pattern: "NextActionRail|nextAction"
---

<objective>
Add sticky two-person status visibility and mobile next-action CTA guidance with state-specific partner-progress messaging.

Purpose: Deliver UX-21, UX-22, and UX-25 so users always know who is blocked and what to do next.
Output: Sticky status/CTA components, wired page messaging states, and e2e coverage for blocker and state transitions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-01-PLAN.md
@src/lib/session-flow.ts
@src/app/s/[token]/page.tsx
@src/components/session/ParticipantStatus.tsx
@src/app/globals.css

<interfaces>
From `src/lib/session-flow.ts`:
```ts
export interface SessionFlowStep {
  id: SessionStepId;
  title: string;
  completed: boolean;
  blockedBy: "self" | "partner" | null;
  summary: string;
}
```

From `src/lib/api/session-client.ts`:
```ts
export interface SessionSnapshotParticipant {
  participantId: string;
  role: "host" | "invitee";
  locationConfirmedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sticky session progress and next-action components from flow contracts</name>
  <read_first>src/lib/session-flow.ts, src/components/session/ParticipantStatus.tsx, src/app/globals.css</read_first>
  <files>src/components/session/SessionProgressBar.tsx, src/components/session/NextActionRail.tsx, src/components/session/ParticipantStatus.tsx, src/app/globals.css</files>
  <behavior>
    - Progress bar shows You and Partner completion + blocker ownership.
    - CTA rail exposes exactly one action label and disabled/loading/error variants.
    - Components preserve keyboard and screen-reader semantics.
  </behavior>
  <action>Create `SessionProgressBar.tsx` with props `{ steps, activeStepId, meRole, partnerRole }` and explicit labels `You`/`Partner` plus blocker copy (`Waiting for partner...`, `Your turn...`). Create `NextActionRail.tsx` with props `{ label, onClick, disabled, status }` and render sticky bottom action on mobile (`position: sticky; bottom: 0`). Update `ParticipantStatus.tsx` to use concise phase-specific badges (joined/location/preferences/results/confirmed) instead of location-only status so UX-21/25 are visible before map stage. Add CSS selectors `.session-progress-sticky`, `.next-action-rail`, `.next-action-button`, `.status-inline-partner`.</action>
  <acceptance_criteria>
    - `src/components/session/SessionProgressBar.tsx` exists and exports `SessionProgressBar`.
    - `src/components/session/NextActionRail.tsx` exists and exports `NextActionRail`.
    - `src/components/session/ParticipantStatus.tsx` contains copy for partner progress beyond location-only state.
    - `src/app/globals.css` contains `.session-progress-sticky` and `.next-action-rail` selectors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Reusable sticky status + next-action components are ready and aligned with guided flow contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sticky status and CTA rails into session page with state-specific messaging</name>
  <read_first>src/app/s/[token]/page.tsx, src/components/session/SessionProgressBar.tsx, src/components/session/NextActionRail.tsx, src/lib/session-flow.ts</read_first>
  <files>src/app/s/[token]/page.tsx</files>
  <behavior>
    - Session page renders sticky status bar and sticky next-action CTA on mobile.
    - Waiting/loading/error/success messages include partner-progress context.
    - Next action always aligns to active guided step.
  </behavior>
  <action>In `page.tsx`, import and render `SessionProgressBar` above guided steps and `NextActionRail` at mobile footer. Derive one concrete `nextAction` object from `activeStepId`: `location => "Confirm location"`, `preferences => "Save preferences"`, `spots => "Review ranked spots"`, `shortlist => "Pick shortlist spot"`, `confirm => "Confirm final place"`. Replace generic status strings with partner-aware variants, e.g. `Waiting for partner to confirm location`, `Loading shared suggestions from both saved preferences`, `Error: partner-save sync delayed, retry available`, `Success: both participants can proceed to shortlist`. Keep existing action handlers and API calls unchanged.</action>
  <acceptance_criteria>
    - `src/app/s/[token]/page.tsx` imports `SessionProgressBar` and `NextActionRail`.
    - `src/app/s/[token]/page.tsx` contains all five explicit next-action labels.
    - `src/app/s/[token]/page.tsx` contains partner-aware waiting/loading/error/success copy.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Users can always identify current blocker ownership and next action without searching the page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend e2e coverage for blocker visibility and sticky next-action behavior</name>
  <read_first>tests/e2e/ranking-results.spec.ts, src/app/s/[token]/page.tsx</read_first>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <behavior>
    - Test verifies sticky status text updates when partner is pending.
    - Test verifies one visible primary next-action CTA on mobile.
    - Test verifies success/error lifecycle copy remains actionable.
  </behavior>
  <action>Extend `ranking-results.spec.ts` with mobile assertions that the sticky progress bar is visible while scrolling, the CTA rail shows exactly one primary action at each stage, and blocker text updates from `Your turn` to `Waiting for partner` based on fixture progression. Assert lifecycle copy strings for loading/success/error states in ranking flow to satisfy UX-25 and prevent vague regressions.</action>
  <acceptance_criteria>
    - `tests/e2e/ranking-results.spec.ts` contains assertions for `Your turn` and `Waiting for partner`.
    - `tests/e2e/ranking-results.spec.ts` contains assertion that only one primary CTA is visible.
    - `tests/e2e/ranking-results.spec.ts` contains assertions for loading/success/error status copy.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5"</automated>
  </verify>
  <done>Automated tests verify blocker transparency and sticky next-action guidance behavior on mobile.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/ranking-results.spec.ts --project="Pixel 5"`
</verification>

<success_criteria>
- UX-21: Sticky two-person progress and blocker ownership is visible throughout the session.
- UX-22: Sticky mobile CTA always presents one next action.
- UX-25: Waiting/loading/error/success copy includes partner-progress context.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-first-guided-flow-map-driven-decisioning/08-02-SUMMARY.md`
</output>
