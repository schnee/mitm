---
phase: 05-ux-refresh-decision-confidence
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/routes/analytics/getSessionFunnel.ts
  - services/api/tests/funnel-events.test.ts
  - tests/e2e/funnel-compatibility.spec.ts
  - package.json
autonomous: true
requirements:
  - UXEX-01
  - UXEX-02
must_haves:
  truths:
    - "UX refresh behavior is regression-tested in the full funnel"
    - "New interaction telemetry is available for optimization decisions"
    - "Explainability and reactions are verifiably present in staged flow runs"
  artifacts:
    - path: "services/api/src/routes/analytics/getSessionFunnel.ts"
      provides: "Session-level interaction telemetry summary including reaction metrics"
    - path: "services/api/tests/funnel-events.test.ts"
      provides: "Automated backend verification for new telemetry fields"
    - path: "tests/e2e/funnel-compatibility.spec.ts"
      provides: "End-to-end assertions for explainability + reaction UX"
  key_links:
    - from: "services/api/src/modules/session/repository.ts"
      to: "services/api/src/routes/analytics/getSessionFunnel.ts"
      via: "recorded funnel and interaction events"
      pattern: "result_reacted|reaction_to_shortlist|shortlist_opened"
    - from: "tests/e2e/funnel-compatibility.spec.ts"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "UI control labels and explainability text assertions"
      pattern: "Fairness delta|Accept|Pass"
---

<objective>
Validate the refreshed UX end-to-end and expose interaction telemetry needed to optimize decision confidence.

Purpose: Ensure Phase 5 changes are measurable and safe by adding targeted telemetry and regression checks.
Output: Updated analytics shape, backend telemetry tests, and e2e UX flow verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-refresh-decision-confidence/05-RESEARCH.md
@services/api/src/modules/session/contracts.ts
@services/api/src/modules/session/repository.ts
@services/api/src/routes/analytics/getSessionFunnel.ts
@services/api/tests/funnel-events.test.ts
@tests/e2e/funnel-compatibility.spec.ts

<interfaces>
From `services/api/src/modules/session/contracts.ts`:
```ts
export type FunnelEventName = "session_start" | "inputs_set" | "results_returned" | "decision_confirmed";

export interface FunnelEventRecord {
  sessionId: string;
  event: FunnelEventName;
  occurredAt: string;
  metadata?: Record<string, FunnelEventMetadataValue>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture Phase 5 interaction telemetry in analytics pipeline</name>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, services/api/src/routes/analytics/getSessionFunnel.ts, services/api/tests/funnel-events.test.ts</files>
  <read_first>services/api/src/modules/session/contracts.ts, services/api/src/modules/session/repository.ts, services/api/src/routes/analytics/getSessionFunnel.ts, services/api/tests/funnel-events.test.ts</read_first>
  <behavior>
    - Telemetry includes counts for reaction activity and shortlist transition velocity.
    - Existing required funnel completeness (`session_start`, `inputs_set`, `results_returned`, `decision_confirmed`) remains unchanged.
    - New interaction fields are returned by analytics endpoint for one session.
  </behavior>
  <action>Extend telemetry contracts to include supplemental interaction event names (`result_reacted`, `shortlist_opened`, `reaction_to_shortlist`) while preserving existing required funnel events. Record these from repository paths affected by reactions/shortlist updates (reaction upsert and shortlist mutation). Update `getSessionFunnel.ts` to return a new `interactionSummary` object with at least: `reactionCount`, `acceptCount`, `passCount`, `firstReactionAt`, `firstShortlistAt`, and `reactionToShortlistSeconds` when both timestamps exist. Add/adjust `funnel-events.test.ts` assertions for this response shape (per UXEX-01 and UXEX-02 optimization loop goal).</action>
  <acceptance_criteria>
    - `contracts.ts` contains new interaction event names while retaining original four funnel names.
    - `getSessionFunnel.ts` response includes `interactionSummary` with all listed keys.
    - `funnel-events.test.ts` asserts `interactionSummary` values in a deterministic scenario.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/funnel-events.test.ts</automated>
  </verify>
  <done>Analytics endpoint reports reaction and shortlist timing signals alongside required funnel completeness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade e2e funnel spec for explainability and reaction UX</name>
  <files>tests/e2e/funnel-compatibility.spec.ts, package.json</files>
  <read_first>tests/e2e/funnel-compatibility.spec.ts, package.json, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx</read_first>
  <behavior>
    - E2E flow checks explainability copy appears after ranking runs.
    - E2E flow executes `Accept`/`Pass` interactions and still reaches confirmation.
    - E2E analytics assertion includes both funnel completeness and interaction summary values.
  </behavior>
  <action>Update `funnel-compatibility.spec.ts` route stubs and assertions to match new ranking fields and reaction endpoint (`/v1/decision/reaction`). Add UI checks for `Fairness delta` text and reaction controls (`Accept`, `Pass`) before shortlist/confirm actions. Extend final analytics fetch assertion to validate returned `interactionSummary` keys. Keep the existing command name in `package.json` (`test:e2e:launch`) and ensure it still runs this upgraded spec without additional manual steps.</action>
  <acceptance_criteria>
    - E2E spec contains route mock for `/v1/decision/reaction`.
    - E2E spec asserts visibility of `Fairness delta`, `Accept`, and `Pass`.
    - E2E analytics assertion checks `interactionSummary` object presence/keys.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/funnel-compatibility.spec.ts --project="Desktop Chrome"</automated>
  </verify>
  <done>Full funnel regression now verifies Phase 5 UX behaviors and interaction telemetry outputs.</done>
</task>

</tasks>

<verification>
- `npx vitest run services/api/tests/funnel-events.test.ts`
- `npx playwright test tests/e2e/funnel-compatibility.spec.ts --project="Desktop Chrome"`
</verification>

<success_criteria>
- Interaction telemetry needed for post-refresh optimization is queryable per session.
- Full funnel regression validates explainability and reaction UX without breaking confirmation.
- Phase 5 outcomes are measurable and production-ready for UX tuning.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-refresh-decision-confidence/05-03-SUMMARY.md`
</output>
