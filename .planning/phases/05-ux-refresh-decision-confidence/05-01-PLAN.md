---
phase: 05-ux-refresh-decision-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/modules/ranking/contracts.ts
  - services/api/src/modules/ranking/service.ts
  - services/api/tests/ranking-results.test.ts
  - src/lib/api/session-client.ts
  - src/components/ranking/RankedResultsList.tsx
autonomous: true
requirements:
  - UXEX-01
must_haves:
  truths:
    - "User sees a fairness explanation for each ranked venue"
    - "User can compare why one venue ranks above another"
    - "Explainability details are present in the same ranked results flow used today"
  artifacts:
    - path: "services/api/src/modules/ranking/contracts.ts"
      provides: "Explainability fields in ranking response contract"
    - path: "src/lib/api/session-client.ts"
      provides: "Client-side typed explainability fields on RankedVenue"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Explainability-first visual hierarchy and rationale rendering"
  key_links:
    - from: "services/api/src/modules/ranking/service.ts"
      to: "src/lib/api/session-client.ts"
      via: "POST /v1/ranking/results payload shape"
      pattern: "fairnessScore|preferenceScore|totalScore|fairnessDeltaMinutes"
    - from: "src/lib/api/session-client.ts"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "RankedVenue type usage"
      pattern: "type RankedVenue"
---

<objective>
Add explainability payloads and UI rendering so each ranked venue clearly communicates fairness rationale.

Purpose: Deliver UXEX-01 by making trust-building rationale visible without changing ranking algorithm behavior.
Output: Typed explainability fields from API through client and updated ranked results UI hierarchy.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-refresh-decision-confidence/05-RESEARCH.md
@services/api/src/modules/ranking/contracts.ts
@services/api/src/modules/ranking/service.ts
@src/lib/api/session-client.ts
@src/components/ranking/RankedResultsList.tsx

<interfaces>
From `services/api/src/modules/ranking/contracts.ts`:
```ts
export interface RankedVenue {
  venueId: string;
  name: string;
  lat: number;
  lng: number;
  category: string;
  openNow: boolean | null;
  etaParticipantA: number;
  etaParticipantB: number;
}
```

From `src/lib/api/session-client.ts`:
```ts
export interface RankedVenue {
  venueId: string;
  name: string;
  lat: number;
  lng: number;
  etaParticipantA: number;
  etaParticipantB: number;
  category: string;
  openNow: boolean | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ranking contracts with explainability metrics</name>
  <files>services/api/src/modules/ranking/contracts.ts, services/api/src/modules/ranking/service.ts, services/api/tests/ranking-results.test.ts</files>
  <read_first>services/api/src/modules/ranking/contracts.ts, services/api/src/modules/ranking/service.ts, services/api/tests/ranking-results.test.ts</read_first>
  <behavior>
    - Test: ranked results route returns explainability fields (`fairnessScore`, `preferenceScore`, `totalScore`, `fairnessDeltaMinutes`) for each result.
    - Test: `fairnessDeltaMinutes` equals absolute difference between `etaParticipantA` and `etaParticipantB`.
    - Test: existing ranking ordering assertions remain green (no algorithm swap).
  </behavior>
  <action>Update `RankedVenue` interface in ranking contracts to include `fairnessScore: number`, `preferenceScore: number`, `totalScore: number`, and `fairnessDeltaMinutes: number`. In `RankingService.generateRankedResults`, populate these fields from `scoreCandidates` output and compute `fairnessDeltaMinutes` with `Math.abs(etaParticipantA - etaParticipantB)`. Add/adjust route-level test assertions in `ranking-results.test.ts` to require these fields in response objects (per UXEX-01).</action>
  <acceptance_criteria>
    - `services/api/src/modules/ranking/contracts.ts` contains all four explainability properties on `RankedVenue`.
    - `services/api/src/modules/ranking/service.ts` maps all four fields into the returned `results` array.
    - `services/api/tests/ranking-results.test.ts` asserts explainability fields in response payload.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/ranking-results.test.ts</automated>
  </verify>
  <done>Ranking API returns explainability metrics for each ranked venue and tests enforce the shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render explainability-first ranked list hierarchy</name>
  <files>src/lib/api/session-client.ts, src/components/ranking/RankedResultsList.tsx</files>
  <read_first>src/lib/api/session-client.ts, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx</read_first>
  <behavior>
    - Ranked list displays a compact fairness breakdown per venue with readable labels.
    - Empty state text remains visible when no results exist.
    - Existing shortlist action still works and remains disabled for already-shortlisted venues.
  </behavior>
  <action>Mirror the backend `RankedVenue` contract updates in `src/lib/api/session-client.ts`. Redesign `RankedResultsList.tsx` hierarchy to show: venue header, primary trust line (`Fairness delta: X min`), score chips (`Fairness`, `Preference`, `Total`), then ETA/category/open status details. Keep current `onAddToShortlist` callback contract and disabled-state logic unchanged. Include concise microcopy explaining lower fairness delta means more balanced travel burden (per UXEX-01).</action>
  <acceptance_criteria>
    - Client `RankedVenue` type includes four explainability fields.
    - `RankedResultsList.tsx` contains visible labels: `Fairness delta`, `Fairness`, `Preference`, and `Total`.
    - `RankedResultsList.tsx` still renders `Add to shortlist` button using `onAddToShortlist` prop.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Users see explainability metrics in ranked results without breaking shortlist interactions.</done>
</task>

</tasks>

<verification>
Run backend and frontend checks:
- `npx vitest run services/api/tests/ranking-results.test.ts`
- `npx tsc --noEmit`
</verification>

<success_criteria>
- Ranked results API includes explainability fields for every result.
- Ranked results UI presents fairness rationale in a clear top-first hierarchy.
- Existing ranking flow and shortlist action remain operational.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-refresh-decision-confidence/05-01-SUMMARY.md`
</output>
