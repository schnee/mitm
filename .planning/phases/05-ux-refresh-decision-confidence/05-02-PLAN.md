---
phase: 05-ux-refresh-decision-confidence
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - services/api/src/modules/session/contracts.ts
  - services/api/src/modules/session/repository.ts
  - services/api/src/routes/decision/validation.ts
  - services/api/src/routes/decision/upsertVenueReaction.ts
  - services/api/src/dev-server.ts
  - services/api/tests/decision-routes.test.ts
  - services/api/tests/decision-state.test.ts
  - src/lib/api/session-client.ts
  - src/hooks/useSessionSync.ts
  - src/components/ranking/RankedResultsList.tsx
  - src/app/s/[token]/page.tsx
autonomous: true
requirements:
  - UXEX-02
must_haves:
  truths:
    - "Each participant can quickly react accept/pass on ranked venues"
    - "Reaction state stays shared and synchronized between both participants"
    - "Reactions help users move from results toward shortlist faster"
  artifacts:
    - path: "services/api/src/routes/decision/upsertVenueReaction.ts"
      provides: "Server endpoint for accept/pass reaction upsert"
    - path: "services/api/src/modules/session/repository.ts"
      provides: "Shared reaction persistence and snapshot inclusion"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Accept/pass controls with state feedback"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Reaction action wiring to API and sync snapshot"
  key_links:
    - from: "src/components/ranking/RankedResultsList.tsx"
      to: "src/lib/api/session-client.ts"
      via: "upsertVenueReaction API call"
      pattern: "upsertVenueReaction"
    - from: "services/api/src/routes/decision/upsertVenueReaction.ts"
      to: "services/api/src/modules/session/repository.ts"
      via: "repository.upsertVenueReaction"
      pattern: "upsertVenueReaction"
    - from: "services/api/src/modules/session/repository.ts"
      to: "src/hooks/useSessionSync.ts"
      via: "session_updated diff/snapshot reactions"
      pattern: "reactions"
---

<objective>
Implement conflict-safe accept/pass reactions as shared server state and surface it in ranked results UX.

Purpose: Deliver UXEX-02 with fast decision gestures that are synchronized across participants.
Output: Reaction contracts, API route, snapshot sync, and UI controls for accept/pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-refresh-decision-confidence/05-RESEARCH.md
@services/api/src/modules/session/contracts.ts
@services/api/src/modules/session/repository.ts
@services/api/src/routes/decision/validation.ts
@services/api/src/dev-server.ts
@src/lib/api/session-client.ts
@src/hooks/useSessionSync.ts
@src/components/ranking/RankedResultsList.tsx
@src/app/s/[token]/page.tsx

<interfaces>
From `services/api/src/modules/session/contracts.ts`:
```ts
export interface SessionSnapshot {
  sessionId: string;
  status: SessionStatus;
  updatedAt: string;
  participants: SessionSnapshotParticipant[];
  inputsReady: boolean;
  shortlist: ShortlistVenue[];
  confirmedPlace: ConfirmedPlace | null;
}
```

From `src/lib/api/session-client.ts`:
```ts
export interface SessionSnapshotResponse {
  sessionId: string;
  status: "created" | "joined" | "locating" | "ranked" | "confirmed" | "expired";
  updatedAt: string;
  participants: SessionSnapshotParticipant[];
  inputsReady: boolean;
  shortlist: ShortlistVenue[];
  confirmedPlace: ConfirmedPlace | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reaction contracts and server API endpoint</name>
  <files>services/api/src/modules/session/contracts.ts, services/api/src/routes/decision/validation.ts, services/api/src/routes/decision/upsertVenueReaction.ts, services/api/src/dev-server.ts</files>
  <read_first>services/api/src/modules/session/contracts.ts, services/api/src/routes/decision/validation.ts, services/api/src/routes/decision/upsertShortlistVenue.ts, services/api/src/dev-server.ts</read_first>
  <behavior>
    - Reaction payload with invalid enum value is rejected with 400.
    - Valid payload stores participant reaction (`accept` or `pass`) for the target venue.
    - Endpoint responds with updated reaction aggregate for the venue.
  </behavior>
  <action>Create `VenueReaction` contract (`sessionId`, `venueId`, `participantId`, `reaction`, `reactedAt`) and add `reactions` shape to `SessionSnapshot`. Extend decision validation with `venueReactionSchema` requiring `reaction` in `{"accept","pass"}`. Add `upsertVenueReaction.ts` route handler mirroring existing decision route error handling (`INVALID_DECISION_PAYLOAD`, `SESSION_NOT_FOUND`). Wire `POST /v1/decision/reaction` in `dev-server.ts` to this handler (per UXEX-02).</action>
  <acceptance_criteria>
    - `services/api/src/modules/session/contracts.ts` defines `VenueReaction` and snapshot `reactions` field.
    - `services/api/src/routes/decision/validation.ts` contains `venueReactionSchema` with enum `accept`/`pass`.
    - `services/api/src/dev-server.ts` contains `if (method === "POST" && url.pathname === "/v1/decision/reaction")` route wiring.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Reaction endpoint and contracts exist with strict validation and server route wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and sync shared reactions in session repository</name>
  <files>services/api/src/modules/session/repository.ts, services/api/tests/decision-state.test.ts, services/api/tests/decision-routes.test.ts</files>
  <read_first>services/api/src/modules/session/repository.ts, services/api/tests/decision-state.test.ts, services/api/tests/decision-routes.test.ts</read_first>
  <behavior>
    - Upserting same participant+venue reaction overwrites prior value (idempotent update).
    - Snapshot returns per-venue reaction aggregates and current participant reaction lookup data.
    - Session event diff includes reaction updates so syncing clients can merge state.
  </behavior>
  <action>Add repository storage for reactions keyed by session and venue. Implement `upsertVenueReaction` with participant/session validation, idempotent replacement for same participant+venue, and returned aggregate data (`acceptCount`, `passCount`, `reactionsByParticipant`). Include reaction state in `getSessionSnapshot` response and `session_updated` event diff. Extend decision repository/route tests to cover valid upsert, invalid payload, update semantics, and snapshot visibility (per UXEX-02).</action>
  <acceptance_criteria>
    - `repository.ts` exports `upsertVenueReaction` and snapshot includes reaction data.
    - `decision-state.test.ts` verifies overwrite/idempotent reaction behavior.
    - `decision-routes.test.ts` verifies route-level 200 and 400/404 handling for reactions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run services/api/tests/decision-state.test.ts services/api/tests/decision-routes.test.ts</automated>
  </verify>
  <done>Reaction state is server-authoritative, conflict-safe, and reflected in snapshots/events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire accept/pass UI controls and session-sync merge</name>
  <files>src/lib/api/session-client.ts, src/hooks/useSessionSync.ts, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx</files>
  <read_first>src/lib/api/session-client.ts, src/hooks/useSessionSync.ts, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx</read_first>
  <behavior>
    - User can click `Accept` or `Pass` per venue and see immediate state feedback.
    - Sync updates from other participant reflect in venue reaction counts without reload.
    - Existing shortlist flow remains functional alongside reactions.
  </behavior>
  <action>Add `upsertVenueReaction` client API function and reaction types in `session-client.ts`. Update `useSessionSync` merge logic to apply `reactions` from `session_updated` diff. Extend `RankedResultsList` props to accept reaction state and callbacks, rendering `Accept` and `Pass` buttons plus count badges. In `page.tsx`, call reaction API on click, merge local optimistic reaction feedback, and derive concise status text (`You accepted`, `Partner passed`, etc.) while keeping existing shortlist/confirm behavior unchanged (per UXEX-02).</action>
  <acceptance_criteria>
    - `session-client.ts` exports `upsertVenueReaction` function and reaction types.
    - `RankedResultsList.tsx` contains `Accept` and `Pass` button labels.
    - `useSessionSync.ts` merges `reactions` from `session_updated` events.
    - `page.tsx` invokes `upsertVenueReaction` in ranking interaction handlers.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Participants can react quickly and see shared reaction state update in-session.</done>
</task>

</tasks>

<verification>
- `npx vitest run services/api/tests/decision-state.test.ts services/api/tests/decision-routes.test.ts`
- `npx tsc --noEmit`
</verification>

<success_criteria>
- Accept/pass reactions are stored and synchronized between participants.
- Reaction endpoint validation and conflict behavior are covered by automated tests.
- Ranked results UI supports quick reactions without breaking shortlist confirmation flow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-refresh-decision-confidence/05-02-SUMMARY.md`
</output>
