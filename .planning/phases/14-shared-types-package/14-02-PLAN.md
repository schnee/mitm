---
phase: 14-shared-types-package
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - services/api/src/modules/ranking/contracts.ts
  - src/lib/api/session-client.ts
  - package.json
autonomous: true
requirements:
  - REFACTOR-06

must_haves:
  truths:
    - "Backend imports types from @mitm/types"
    - "Frontend imports types from @mitm/types"
    - "Type definitions are no longer duplicated"
  artifacts:
    - path: "services/api/src/modules/ranking/contracts.ts"
      provides: "Backend type imports"
    - path: "src/lib/api/session-client.ts"
      provides: "Frontend type imports"
  key_links:
    - from: "services/api/src/modules/ranking/contracts.ts"
      to: "@mitm/types"
      via: "import"
      pattern: "import.*@mitm/types"
---

<objective>
Update frontend and backend to import types from the shared @mitm/types package, eliminating type duplication.

Purpose: Ensure both frontend and backend use the same type definitions.
Output: Updated imports in session-client.ts and ranking/contracts.ts.
</objective>

<context>
@packages/types/src/index.ts (from plan 14-01)
@src/lib/api/session-client.ts
@services/api/src/modules/ranking/contracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update backend to use @mitm/types</name>
  <files>services/api/src/modules/ranking/contracts.ts</files>
  <action>
Update services/api/src/modules/ranking/contracts.ts to import from @mitm/types:

1. Remove local type definitions for PreferenceTag, WillingnessSplit, RankedVenue
2. Add import:
```typescript
import type { PreferenceTag, WillingnessSplit, RankedVenue } from "@mitm/types";
```

3. Keep RankingInputState, RankingCandidate, ScoredCandidate as local types if needed for internal API processing
4. Ensure package.json has workspace reference to packages/types

Update package.json if needed:
```json
{
  "workspaces": [
    "packages/*"
  ]
}
```
Or add dependency in services/api/package.json:
```json
{
  "dependencies": {
    "@mitm/types": "workspace:*"
  }
}
```
  </action>
  <verify>
    <automated>cd services/api && tsc --noEmit src/modules/ranking/contracts.ts</automated>
  </verify>
  <done>Backend imports types from @mitm/types</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend to use @mitm/types</name>
  <files>src/lib/api/session-client.ts</files>
  <action>
Update src/lib/api/session-client.ts to import from @mitm/types:

1. Remove local type definitions for PreferenceTag (line 106), WillingnessSplit (line 105), RankedVenue (lines 108-121)
2. Add import:
```typescript
import type { PreferenceTag, WillingnessSplit, RankedVenue } from "@mitm/types";
```

3. Keep local types that are frontend-specific (CreateSessionResponse, JoinSessionResponse, etc.)
4. Update root package.json to add workspace:
```json
{
  "workspaces": [
    "packages/*",
    "services/*"
  ]
}
```

Or add dependency:
```json
{
  "dependencies": {
    "@mitm/types": "workspace:*"
  }
}
```
  </action>
  <verify>
    <automated>tsc --noEmit src/lib/api/session-client.ts</automated>
  </verify>
  <done>Frontend imports types from @mitm/types</done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compilation</name>
  <files>package.json</files>
  <action>
Verify TypeScript compilation across the project:

1. Ensure workspace references are correctly configured
2. Run tsc --noEmit at root to verify no type errors
3. Fix any import path issues

Run: `pnpm install && tsc --noEmit`
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
  </verify>
  <done>Project compiles without type errors</done>
</task>

</tasks>

<verification>
- [ ] Backend uses @mitm/types
- [ ] Frontend uses @mitm/types
- [ ] No type duplication in codebase
- [ ] TypeScript compiles
</verification>

<success_criteria>
- @mitm/types imported in services/api/src/modules/ranking/contracts.ts
- @mitm/types imported in src/lib/api/session-client.ts
- No local duplicate type definitions
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-types-package/14-02-SUMMARY.md`
</output>