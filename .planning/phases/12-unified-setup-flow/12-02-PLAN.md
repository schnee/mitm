---
phase: 12-unified-setup-flow
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/lib/session-flow.ts
  - src/app/s/[token]/page.tsx
  - src/lib/session-flow.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Session flow shows 4 steps (location, spots, shortlist, confirm) not 5"
    - "SetupCard renders in location step replacing separate location+preferences"
    - "Next action label reflects unified setup: 'Complete setup'"
    - "Tests verify 4-step configuration passes"
  artifacts:
    - path: "src/lib/session-flow.ts"
      provides: "Updated session step derivation"
      exports: ["deriveSessionFlow"]
    - path: "src/lib/session-flow.test.ts"
      provides: "Updated tests for 4-step flow"
  key_links:
    - from: "page.tsx location step"
      to: "SetupCard"
      via: "conditional render"
      pattern: "step.id === \"location\".*SetupCard"
---

<objective>
Integrate SetupCard into session page and update session flow to use unified setup step.

Purpose: Replace separate location + preferences steps with single unified setup card.
Output: Session page renders SetupCard in location step, session-flow derived steps omit preferences.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/session-flow.ts
@src/app/s/[token]/page.tsx

# Session flow derivation patterns:
# - deriveSessionFlow returns activeStepId and steps array
# - Each step has id, title, completed, blockedBy, summary
# - Current steps: location, preferences, spots, shortlist, confirm

# Page integration patterns:
# - Steps rendered via guided-stepper section
# - Each step.id triggers conditional rendering
# - activeStepId determines scroll-into-view for CTA rail
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update session-flow.ts to use unified setup step</name>
  <files>src/lib/session-flow.ts</files>
  <action>
Update session-flow.ts:
- Remove "preferences" from STEP_ORDER array
- Remove preferences step from steps array
- Update spotsBlockedBy logic: now depends on myLocationConfirmed instead of myPreferencesSaved
- Rename active step label from "Preferences" to "Setup" or keep as "Location" since setup replaces it
- Update deriveSessionFlow input: remove myPreferencesSaved, partnerPreferencesSaved
- Keep backward compatible: if preferencesSaved fields are passed, derive from location completion instead

After change, steps should be: location, spots, shortlist, confirm (4 steps instead of 5)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="session-flow" --passWithNoTests 2>&1 | head -20</automated>
  </verify>
  <done>SessionFlowStep[] has 4 steps: location, spots, shortlist, confirm</done>
</task>

<task type="auto">
  <name>Task 2: Replace separate location+preferences rendering with SetupCard</name>
  <files>src/app/s/[token]/page.tsx</files>
  <action>
Update src/app/s/[token]/page.tsx:
- Import SetupCard from src/components/setup/SetupCard
- In the location step render block (step.id === "location"):
  - Replace LocationCaptureForm + RankingInputsForm conditional with SetupCard
  - SetupCard takes sessionId, participantId, and onSetupComplete callback
- Remove the separate preferences step rendering (step.id === "preferences" block)
- Update nextActionLabel: if activeStepId === "location" show "Complete setup" instead of "Confirm location"
- Update myPreferencesSaved state derivation: remove the setMyPreferencesSaved(true) callback, derive from location completion instead

The SetupCard already calls upsertLocationDraft then upsertRankingInputs in sequence.
  </action>
  <verify>
  </verify>
  <done>Session page uses unified SetupCard in location step, no separate preferences step rendered</done>
</task>

<task type="auto">
  <name>Task 3: Update session-flow tests for 4-step flow</name>
  <files>src/lib/session-flow.test.ts</files>
  <action>
Update session-flow.test.ts:
- Remove tests for "preferences" step
- Update test assertions: expect 4 steps not 5
- Update test names and descriptions to reflect unified setup flow
- Keep tests for location→spots→shortlist→confirm progression
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="session-flow" --passWithNoTests 2>&1 | head -30</automated>
  </verify>
  <done>session-flow tests pass with 4-step configuration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | SetupCard calls existing APIs - no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | S | Flow state derivation | mitigate | Tests verify step order and blocking logic |
</threat_model>

<verification>
session-flow.test.ts passes, page renders SetupCard in location step
</verification>

<success_criteria>
Combined setup replaces separate location+preferences steps, tests updated accordingly
</success_criteria>

<output>
After completion, create `.planning/phases/12-unified-setup-flow/12-02-SUMMARY.md`
</output>