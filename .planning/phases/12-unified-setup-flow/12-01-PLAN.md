---
phase: 12-unified-setup-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/setup/SetupCard.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can set location via interactive map click"
    - "User can select travel willingness split (50/50, 60/40, 70/30)"
    - "User can select preference tags (max 5)"
    - "Single Continue button submits both location and preferences"
  artifacts:
    - path: "src/components/setup/SetupCard.tsx"
      provides: "Combined location capture + preferences UI"
      exports: ["SetupCard"]
  key_links:
    - from: "SetupCard"
      to: "upsertLocationDraft"
      via: "on click handler"
      pattern: "upsertLocationDraft.*mode.*manual"
    - from: "SetupCard"
      to: "upsertRankingInputs"
      via: "sequential call after location"
      pattern: "upsertRankingInputs.*split.*tags"
---

<objective>
Create unified SetupCard component that combines location capture and meetup preferences in a single UI step.

Purpose: Reduce friction by combining two separate steps into one seamless setup experience.
Output: New SetupCard component with map, location picker, travel split selector, and preference tags.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/location/LocationCaptureForm.tsx
@src/components/ranking/RankingInputsForm.tsx

# Component patterns to follow:
# - Use same status badge patterns as existing components
# - Map integration via @vis.gl/react-google-maps (from Phase 11)
# - Travel split options: 50_50, 60_40, 70_30
# - Preference tags: coffee, lunch, dinner, cocktails, dessert, museum, walk_and_talk, vintage_shops, quiet
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SetupCard component directory and types</name>
  <files>src/components/setup/SetupCard.tsx</files>
  <action>
Create src/components/setup/SetupCard.tsx with:
- Import Map components from @vis.gl/react-google-maps
- Import upsertLocationDraft and upsertRankingInputs from session-client
- Props: sessionId, participantId, onSetupComplete callback
- Internal state: selectedLat, selectedLng, travelSplit (default 60_40), selectedTags (default [coffee])
- Map centered on geolocation with click-to-set marker
- Travel split radio selector (50/50, 60/40, 70/30)
- Preference tag chips (multi-select, max 5)
- Single "Continue" button that:
  1. Calls upsertLocationDraft with mode:manual, lat, lng, addressLabel:"Map-click"
  2. On success, calls upsertRankingInputs with split and tags
  3. Calls onSetupComplete with rankingLifecycle response
- Status messaging: loading while saving, success/error feedback
- Use existing status badge classes (status-loading, status-success, status-error, status-idle)
  </action>
  <verify>
    <automated>ls -la src/components/setup/</automated>
  </verify>
  <done>SetupCard.tsx exists with Map integration, split selector, tag chips, and unified submit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user clicks on map, selects preferences - validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | S | SetupCard user input | mitigate | Server validates lat/lng bounds, split enum, tag array |
| T-12-02 | I | Preference data in transit | mitigate | Already using HTTPS + same auth as existing endpoints |
</threat_model>

<verification>
SetupCard.tsx created in src/components/setup/ directory
</verification>

<success_criteria>
Unified setup component combines location capture + preferences in one step with single Continue action
</success_criteria>

<output>
After completion, create `.planning/phases/12-unified-setup-flow/12-01-SUMMARY.md`
</output>