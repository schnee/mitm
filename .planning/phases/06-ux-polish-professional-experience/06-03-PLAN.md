---
phase: 06-ux-polish-professional-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/page.tsx
  - src/app/s/[token]/page.tsx
  - src/components/location/LocationCaptureForm.tsx
  - src/components/location/LocationConfirmCard.tsx
  - src/components/session/ParticipantStatus.tsx
  - src/components/ranking/RankingInputsForm.tsx
  - src/components/ranking/RankedResultsList.tsx
  - src/components/decision/ShortlistPanel.tsx
  - src/components/decision/ConfirmedPlaceCard.tsx
  - tests/e2e/location-fallback.spec.ts
  - tests/e2e/funnel-compatibility.spec.ts
autonomous: true
requirements:
  - UX-08
  - UX-09
  - UX-10
must_haves:
  truths:
    - "Users see consistent loading, empty, error, and success feedback throughout startup-to-confirmation"
    - "Core flow remains usable and readable on mobile and desktop breakpoints"
    - "Primary actions are keyboard operable with visible focus and labeled controls"
  artifacts:
    - path: "src/app/s/[token]/page.tsx"
      provides: "Cross-stage state messaging and responsive section composition"
    - path: "src/components/location/LocationCaptureForm.tsx"
      provides: "Accessible location entry with clear fallback/error messaging"
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Accessible action controls with focus-visible and status announcements"
    - path: "tests/e2e/funnel-compatibility.spec.ts"
      provides: "Cross-device flow assertions for polished state and action continuity"
  key_links:
    - from: "src/hooks/useSessionSync.ts"
      to: "src/app/s/[token]/page.tsx"
      via: "syncState + error surfaced in UI"
      pattern: "syncState|error"
    - from: "src/components/location/LocationCaptureForm.tsx"
      to: "src/components/location/LocationConfirmCard.tsx"
      via: "draftSaved gating and state messaging"
      pattern: "draftSaved|Confirm location"
    - from: "tests/e2e/funnel-compatibility.spec.ts"
      to: "src/app/s/[token]/page.tsx"
      via: "multi-device interaction flow assertions"
      pattern: "Desktop Safari|Pixel 5|iPhone 12"
---

<objective>
Standardize state feedback, responsive behavior, and accessibility baseline across the full two-person flow.

Purpose: Deliver UX-08/UX-09/UX-10 as a cross-cutting polish pass on top of plans 06-01 and 06-02.
Output: Consistent state language, responsive refinements, accessibility upgrades, and cross-device automated validation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-professional-experience/06-RESEARCH.md
@src/app/page.tsx
@src/app/s/[token]/page.tsx
@src/components/location/LocationCaptureForm.tsx
@src/components/location/LocationConfirmCard.tsx
@src/components/session/ParticipantStatus.tsx
@src/components/ranking/RankingInputsForm.tsx
@src/components/ranking/RankedResultsList.tsx
@src/components/decision/ShortlistPanel.tsx
@src/components/decision/ConfirmedPlaceCard.tsx
@tests/e2e/funnel-compatibility.spec.ts

<interfaces>
From `src/hooks/useSessionSync.ts`:
```ts
export function useSessionSync(sessionId: string | null): {
  syncState: "syncing" | "live" | "reconnecting";
  snapshot: SessionSnapshotResponse | null;
  error: string | null;
};
```

From `src/components/location/LocationConfirmCard.tsx`:
```ts
draftSaved: boolean;
inputsReady: boolean;
onConfirmed: (confirmedAt: string) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize state feedback and responsive layout across startup and session flow</name>
  <files>src/app/page.tsx, src/app/s/[token]/page.tsx, src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/components/session/ParticipantStatus.tsx, src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx</files>
  <read_first>src/app/page.tsx, src/app/s/[token]/page.tsx, src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/components/session/ParticipantStatus.tsx, src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx, src/app/globals.css</read_first>
  <behavior>
    - Loading/empty/error/success language is consistent and stage-specific across startup, location, ranking, shortlist, and confirmation.
    - Layout remains readable from mobile to desktop with no collapsed/overlapping primary actions.
    - Session sync reconnect/error states are visible to users, not silent.
  </behavior>
  <action>Apply a shared state-copy convention (Idle, Loading, Waiting, Success, Error) across all listed components, replacing terse/inconsistent phrasing. Add responsive wrappers/class names to ensure stacked mobile layout and wider desktop composition. Surface `useSessionSync().error` and reconnect state in `src/app/s/[token]/page.tsx` with non-blocking warning UI. Keep all existing business logic, API calls, and decision constraints unchanged (per Phase 6 scope decision in STATE.md).</action>
  <acceptance_criteria>
    - `src/app/s/[token]/page.tsx` displays visible UI for sync reconnect/error states.
    - Location, ranking, shortlist, and confirmation components each contain explicit empty/error/success text blocks.
    - Updated components use responsive container classes that distinguish narrow/mobile and wider layouts.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>State feedback and responsive structure are consistent and trustworthy across the full flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply accessibility baseline upgrades to primary flow actions</name>
  <files>src/app/page.tsx, src/app/s/[token]/page.tsx, src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx</files>
  <read_first>src/app/page.tsx, src/app/s/[token]/page.tsx, src/components/location/LocationCaptureForm.tsx, src/components/location/LocationConfirmCard.tsx, src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx, src/app/globals.css</read_first>
  <behavior>
    - Keyboard users can tab through all core actions in logical order.
    - Focus ring is visible on interactive controls.
    - Inputs/buttons have explicit labels or accessible names for screen readers.
  </behavior>
  <action>Add/verify semantic labels (`label`, `aria-label`, `aria-live` where status messages update), ensure button/link names are descriptive, and apply shared `focus-visible` classes to all primary controls. Update status message containers to use polite/live regions for async updates where appropriate. Preserve existing button enable/disable logic and side effects.</action>
  <acceptance_criteria>
    - Interactive controls in all listed files expose explicit accessible names.
    - Async status areas in startup/session pages include `aria-live` usage where state text changes.
    - Focus-visible styling is applied consistently to primary buttons and links.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Core flow meets Phase 6 accessibility baseline without altering behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand cross-device e2e checks for polished states and accessibility flow</name>
  <files>tests/e2e/location-fallback.spec.ts, tests/e2e/funnel-compatibility.spec.ts</files>
  <read_first>tests/e2e/location-fallback.spec.ts, tests/e2e/funnel-compatibility.spec.ts, src/app/page.tsx, src/app/s/[token]/page.tsx</read_first>
  <behavior>
    - Location fallback spec validates polished fallback/error copy and confirm progression.
    - Funnel spec validates critical action flow and key state text on desktop and mobile projects.
    - Keyboard accessibility path (Tab + Enter/Space on key CTA) is asserted at least once.
  </behavior>
  <action>Update `location-fallback.spec.ts` and `funnel-compatibility.spec.ts` to assert the new standardized state messages and accessibility affordances (focus-visible target via keyboard navigation, labeled primary controls). Keep route stubs deterministic. Run funnel spec across existing multi-device projects to cover UX-09, and include at least one keyboard-only interaction assertion to back UX-10 baseline.
  </action>
  <acceptance_criteria>
    - `location-fallback.spec.ts` asserts polished fallback and confirmation status copy.
    - `funnel-compatibility.spec.ts` includes keyboard interaction assertion for a primary action.
    - Funnel spec command executes with at least one desktop and one mobile project target.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/location-fallback.spec.ts tests/e2e/funnel-compatibility.spec.ts --project="Desktop Safari" --project="Pixel 5"</automated>
  </verify>
  <done>Cross-device and accessibility baseline expectations are enforced by automated tests.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/location-fallback.spec.ts tests/e2e/funnel-compatibility.spec.ts --project="Desktop Safari" --project="Pixel 5"`
</verification>

<success_criteria>
- State feedback is consistent across startup, ranking, shortlist, and confirmation.
- Responsive behavior is validated on desktop and mobile projects.
- Keyboard/focus/accessibility baseline is implemented and tested for core actions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-professional-experience/06-03-SUMMARY.md`
</output>
