---
phase: 06-ux-polish-professional-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/s/[token]/page.tsx
  - src/components/ranking/RankingInputsForm.tsx
  - src/components/ranking/RankedResultsList.tsx
  - src/components/decision/ShortlistPanel.tsx
  - src/components/decision/ConfirmedPlaceCard.tsx
  - tests/e2e/ranking-results.spec.ts
autonomous: true
requirements:
  - UX-07
must_haves:
  truths:
    - "Participants can scan ranking information quickly and understand fairness rationale without hunting"
    - "Decision actions (accept/pass/shortlist/confirm) are obvious, readable, and fast to execute"
    - "Negotiation-to-confirmation flow remains intact while visual hierarchy improves"
  artifacts:
    - path: "src/components/ranking/RankedResultsList.tsx"
      provides: "Action-forward ranked cards with readable explainability and reaction controls"
    - path: "src/app/s/[token]/page.tsx"
      provides: "Re-ordered negotiation surface with clearer stage progression and status rail"
    - path: "src/components/decision/ShortlistPanel.tsx"
      provides: "Polished shortlist presentation with clear confirm affordance"
    - path: "tests/e2e/ranking-results.spec.ts"
      provides: "Automated readability/action assertions for ranking UX"
  key_links:
    - from: "src/app/s/[token]/page.tsx"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "results + reaction props"
      pattern: "onReact|reactionPendingVenueIds|reactionStatusByVenueId"
    - from: "src/components/ranking/RankedResultsList.tsx"
      to: "src/components/decision/ShortlistPanel.tsx"
      via: "shortlist action path"
      pattern: "onAddToShortlist|Confirm this place"
    - from: "tests/e2e/ranking-results.spec.ts"
      to: "src/components/ranking/RankedResultsList.tsx"
      via: "readability and action labels"
      pattern: "Fairness delta|Accept|Pass|Add to shortlist"
---

<objective>
Redesign the negotiation/ranking experience so users can read rationale faster and take decision actions with less friction.

Purpose: Deliver UX-07 while preserving existing ranking/session contracts and behavior.
Output: Refined ranking and decision UI hierarchy plus updated e2e assertions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-refresh-decision-confidence/05-01-SUMMARY.md
@.planning/phases/05-ux-refresh-decision-confidence/05-02-SUMMARY.md
@.planning/phases/06-ux-polish-professional-experience/06-RESEARCH.md
@src/app/s/[token]/page.tsx
@src/components/ranking/RankingInputsForm.tsx
@src/components/ranking/RankedResultsList.tsx
@src/components/decision/ShortlistPanel.tsx
@src/components/decision/ConfirmedPlaceCard.tsx

<interfaces>
From `src/components/ranking/RankedResultsList.tsx`:
```ts
results: RankedVenue[];
onAddToShortlist?: (venue: RankedVenue) => void;
onReact?: (venue: RankedVenue, reaction: "accept" | "pass") => void;
```

From `src/lib/api/session-client.ts`:
```ts
export interface RankedVenue {
  venueId: string;
  name: string;
  etaParticipantA: number;
  etaParticipantB: number;
  fairnessScore: number;
  preferenceScore: number;
  totalScore: number;
  fairnessDeltaMinutes: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refine ranking and decision components for readability and action speed</name>
  <files>src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx</files>
  <read_first>src/components/ranking/RankingInputsForm.tsx, src/components/ranking/RankedResultsList.tsx, src/components/decision/ShortlistPanel.tsx, src/components/decision/ConfirmedPlaceCard.tsx, src/app/globals.css</read_first>
  <behavior>
    - Ranking input controls are grouped and labeled for quick scanning on mobile and desktop.
    - Ranked result cards place title + fairness explanation + primary actions in top visual priority.
    - Shortlist and confirmed destination sections use concise status language and clear call-to-action hierarchy.
  </behavior>
  <action>Update the four components with semantic wrappers (`section`, `header`, list/card containers), consistent class names, and concise microcopy optimized for quick decisions. Keep existing prop contracts and callback behavior intact (no API contract changes). Preserve explainability metrics and reaction controls added in Phase 5, but present them as high-signal rows/chips above secondary metadata. Ensure shortlist confirm action remains disabled only when another place is confirmed (existing lock behavior).</action>
  <acceptance_criteria>
    - `RankedResultsList.tsx` still renders `Fairness delta`, `Accept`, `Pass`, and `Add to shortlist` with improved structure/class names.
    - `RankingInputsForm.tsx` contains grouped, clearly labeled split/tag controls and status copy.
    - `ShortlistPanel.tsx` and `ConfirmedPlaceCard.tsx` retain confirm/navigation behavior while using polished layout containers.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Decision-critical components are visually clearer and action-forward without changing core behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompose session negotiation page around stage progression</name>
  <files>src/app/s/[token]/page.tsx</files>
  <read_first>src/app/s/[token]/page.tsx, src/hooks/useSessionSync.ts, src/app/globals.css</read_first>
  <behavior>
    - Joined state presents a clear progression: participant state, location, ranking inputs, ranked actions, shortlist, confirmation.
    - Status text reflects current stage and avoids conflicting messages.
    - Existing async handlers (ranking run, react, shortlist, confirm) remain wired and functional.
  </behavior>
  <action>Refactor page markup into clear stage sections with consistent headings and helper copy. Keep all existing API calls and state transitions (`runRanking`, `reactToVenue`, `addToShortlist`, `confirmShortlistedVenue`) unchanged, but present status output in dedicated message regions near each stage. Improve trust cues by showing sync state and participant readiness in a persistent summary area. Maintain host-continue and invitee join behavior unchanged (Phase 6 non-goal: no flow logic change).</action>
  <acceptance_criteria>
    - `src/app/s/[token]/page.tsx` preserves existing handler functions and API calls.
    - Joined-state render block contains explicit stage sections for ranking and decision.
    - Decision status and ranking status are shown in stable, dedicated UI regions.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Negotiation page presents a professional, stage-oriented flow while preserving current logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update ranking e2e assertions for polished readability and action flow</name>
  <files>tests/e2e/ranking-results.spec.ts</files>
  <read_first>tests/e2e/ranking-results.spec.ts, src/components/ranking/RankedResultsList.tsx, src/app/s/[token]/page.tsx</read_first>
  <behavior>
    - E2E checks confirm high-signal ranking text remains visible after redesign.
    - E2E flow verifies reaction and shortlist actions are still reachable in the polished layout.
    - Assertions remain deterministic with existing route mocks.
  </behavior>
  <action>Expand `ranking-results.spec.ts` beyond metadata visibility checks to assert explainability and action affordances in the new hierarchy: fairness text, reaction controls, shortlist CTA, and shortlist/confirmation surface visibility after click path. Keep mocked payloads and route URLs stable; only update assertions/selectors to match polished structure.</action>
  <acceptance_criteria>
    - Spec includes assertions for `Fairness delta`, `Accept`, `Pass`, and `Add to shortlist`.
    - Spec performs at least one action path (reaction or shortlist click) and asserts resulting UI state.
    - Spec still uses mocked `/v1/ranking/results` and `/v1/sessions/...` routes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/ranking-results.spec.ts --project="Desktop Chrome"</automated>
  </verify>
  <done>Automated coverage proves ranking/decision UX polish did not break key actions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/ranking-results.spec.ts --project="Desktop Chrome"`
</verification>

<success_criteria>
- Negotiation/ranking information is easier to scan and compare.
- Accept/pass/shortlist/confirm actions remain fast and obvious.
- UX-07 outcomes are backed by updated e2e assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-professional-experience/06-02-SUMMARY.md`
</output>
