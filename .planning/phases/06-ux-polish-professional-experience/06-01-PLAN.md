---
phase: 06-ux-polish-professional-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - tests/e2e/startup-polish.spec.ts
autonomous: true
requirements:
  - UX-06
  - UX-08
must_haves:
  truths:
    - "Host sees a clear startup hierarchy and confidence-first copy before creating a session"
    - "Primary create-session action is visually and semantically obvious on mobile and desktop"
    - "Startup screen shows consistent idle/loading/success/error states"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Polished startup hero, CTA, and explicit state sections"
    - path: "src/app/globals.css"
      provides: "Shared visual tokens, spacing, focus-visible, and startup layout styles"
    - path: "tests/e2e/startup-polish.spec.ts"
      provides: "Automated startup UX assertions for hierarchy and state messaging"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "global stylesheet import"
      pattern: "import \"./globals.css\""
    - from: "src/app/page.tsx"
      to: "src/lib/api/session-client.ts"
      via: "createSession action"
      pattern: "createSession"
    - from: "tests/e2e/startup-polish.spec.ts"
      to: "src/app/page.tsx"
      via: "CTA and state-copy assertions"
      pattern: "Create session|Creating session|Status"
---

<objective>
Polish the create-session startup experience with stronger visual hierarchy, trust-building copy, and state clarity.

Purpose: Deliver UX-06 (and startup slice of UX-08) without changing API/session behavior.
Output: Styled startup page with consistent states and e2e startup UX coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-professional-experience/06-RESEARCH.md
@src/app/layout.tsx
@src/app/page.tsx
@src/lib/api/session-client.ts

<interfaces>
From `src/lib/api/session-client.ts`:
```ts
export async function createSession(): Promise<{
  sessionId: string;
  joinUrl: string;
  participantId: string;
}>;
```

From `src/app/page.tsx`:
```ts
type HostState = "idle" | "creating" | "created" | "error";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish global visual baseline and startup layout styling</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>Create `src/app/globals.css` and import it from `layout.tsx`. Add a minimal design system for Phase 6 polish: neutral page background, card surface, typography scale, spacing utilities, button variants (`.btn-primary`, `.btn-secondary`), status badges (`.status-idle`, `.status-loading`, `.status-success`, `.status-error`), and visible keyboard focus style using `:focus-visible` (per UX-06 and UX-10 baseline direction in state decisions).</action>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import "./globals.css"`.
    - `src/app/globals.css` defines classes `.btn-primary`, `.status-loading`, and a `:focus-visible` style.
    - `src/app/globals.css` includes responsive breakpoint rules for narrow/mobile and desktop widths.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Global CSS baseline is loaded and available for startup polish components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign startup page hierarchy and add startup UX e2e coverage</name>
  <files>src/app/page.tsx, tests/e2e/startup-polish.spec.ts</files>
  <read_first>src/app/page.tsx, tests/e2e/funnel-compatibility.spec.ts</read_first>
  <behavior>
    - Idle state shows confidence-first headline, short explanatory copy, and one dominant Create Session CTA.
    - Creating state disables CTA and announces progress text.
    - Success state presents session metadata and two clear next actions (copy/share link, continue as host) with non-error styling.
  </behavior>
  <action>Refactor `src/app/page.tsx` markup into semantic sections (`header`, `section`, status region) with polished copy and class names from `globals.css`. Keep existing `createSession` behavior and host local/session storage logic unchanged per the Phase 6 scope guardrail. Add `tests/e2e/startup-polish.spec.ts` to mock `/v1/sessions` and assert visible hierarchy text, primary button label, loading text while request is inflight, and success-state fields (`Session ID`, join URL/continue URL affordances).</action>
  <acceptance_criteria>
    - `src/app/page.tsx` contains a prominent heading and descriptive startup copy distinct from raw status labels.
    - `src/app/page.tsx` retains `createSession()` invocation and host continuation URL generation.
    - `tests/e2e/startup-polish.spec.ts` asserts idle, loading, and created-state UI text.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/startup-polish.spec.ts --project="Desktop Chrome"</automated>
  </verify>
  <done>Startup flow feels intentional and trustworthy, with automated proof for hierarchy and state clarity.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx playwright test tests/e2e/startup-polish.spec.ts --project="Desktop Chrome"`
</verification>

<success_criteria>
- Startup screen has clear hierarchy, confidence copy, and one obvious primary CTA.
- Startup state messaging is consistent across idle/loading/success/error.
- UX-06 startup expectations are covered by automated e2e checks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-professional-experience/06-01-SUMMARY.md`
</output>
